---
description: SYZC 项目全局业务架构、标杆原型标注规范、风控与安全约束
globs: *
alwaysApply: true
---

# SYZC 全局协作与原型标注标杆规范

本项目唯一真源统一维护在根目录 `@AGENTS.md`。

## 核心业务与技术规则：
1. **原型标注标杆模板**：所有 PC 和 H5 原型页面的打点、字段清单、需求文档、规则规格必须严格对齐【设备预警信息】【订单/押品预警信息】标杆模板（见 `AGENTS.md` 第 4 节）。
2. **文档三件套标准**：页面必须挂载主 PRD (`prd`)、字段清单 (`fields`)、业务规则规格 (`rules`)。
3. **Mermaid 表达**：页面级打点必须提供全生命周期流转图或状态机变迁图（强制 Mermaid 语法）。
4. **门禁与审批专道**：
   - 双路径获取密码（先匹配审批配置，命中走申请，未命中走免审）；
   - 人脸门禁绝不调用短信 API（R31）；
   - 审批人自审禁止（P06/R11）。
5. **预警与物联穿透**：
   - 预警等级 2~20 档动态字典；
   - 订单配置一站式 6 类策略逐条保存（一单一配 R04，平仓线等级权重 >= 补仓线）；
   - 物联穿透告警由设备台账核销联动解除。
6. **PC 工作中心布局**：工作中心无 Sidebar；审批中心首页为三大区块单行卡片 + 子类型下 5 条预览 +「查看更多」（见 `AGENTS.md` §4.3）。
