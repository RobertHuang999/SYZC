# 🛡️ 森云科技 - SYZC 供应链金融存货监管系统 AGENTS.md

---

## 📖 1. 项目简介

### 1.1 公司背景
* **公司名称**：森云科技 (Senyun Technology)
* **成立时间**：2026年
* **核心定位**：专注于供应链金融科技与物联网存货监管的技术创新企业。

### 1.2 核心定位
**SYZC（供应链金融存货监管系统）** 是面向商业银行、保理公司、核心企业及仓储监管企业打造的一站式数字质押与风控平台。系统基于 IoT、数字仓单、实时风控引擎，打通“货-仓-金”数据链，实现货权质押流转、动态监库、实时预警与自动化审计。

### 1.3 业务目标
* **信任穿透**：质押存货物理状态与货权状态实时同步、防篡改追溯。
* **风险防范**：实时监控存货价值、跌价风险、质押率、异常移库与重复质押。
* **高效协同**：连接资金方、监管方、核心企业与借款企业，实现自动化流转与高效审批。

---

## 🗂️ 2. 重要文件路径

```
SYZC/
├── AGENTS.md                   # ⭐ AI Agent 全局协作指南与核心业务/技术规范真源
├── README.md                   # 🚀 项目知识库与 AI PM 工作流主说明
├── CLAUDE.md                   # 🤖 Claude Code 入口指针（指向 AGENTS.md）
├── .cursorrules                # 🖱️ Cursor 规则入口指针（指向 AGENTS.md）
├── .cursor/rules/              # 🖱️ Cursor 新版规则系统（指向 AGENTS.md）
├── .github/copilot-instructions.md # 🐙 Copilot / Codex 入口指针（指向 AGENTS.md）
├── 00-草稿对话/                # 💬 AI 交互草稿与对话记录
├── 01-项目背景/                # 🏢 公司、业务与需求背景资料
│   ├── a-公司背景/            # 公司介绍、组织架构与战略规划
│   ├── b-业务背景/            # 供应链金融模式、风控与监管流程
│   ├── c-需求背景/            # 原始需求、业务痛点与用户故事
│   └── d-功能:权限清单/       # 功能列表与 RBAC 权限表
├── 02-PRD文档/                 # 📄 官方及正式交付的 PRD 功能规格说明书
│   └── B-迭代需求/6.2版本（2026.08）/ # 6.2 交付 PRD 主体（预警、门禁、审批、IOT）
├── 03-前端原型demo/            # 🎨 PC / H5 前端交互原型
│   ├── B端PC/                 # PC 端原型 (React + Tailwind / Lucide Icons / Vite)
│   ├── B端H5/                 # H5 移动端原型 (React + Tailwind / Mobile UI / Vite)
│   ├── 样式规范- PC端/        # PC 端视觉与组件设计规范
│   └── 样式规范- H5端/        # H5 端视觉与组件设计规范
├── 04-参考资料/                # 📚 行业标准、监管政策与接口规范
├── 05-常用模板/                # 📋 PRD、字段清单与业务流程模板
├── 06-常用提示词/              # 💡 AI PM Prompt 模板库
├── 07-常用skill/               # 🔧 智能 Agent 技能包与自动化脚本
├── 08-系统采集/                # 🔎 现有系统菜单与模块整理成果
└── 09-知识宝典/                # 📚 业务模块结构化知识库
```

---

## 📐 3. 核心业务规则与架构规范 (业务真源)

### 3.1 门禁设备与开锁审批专网专道
1. **双路径密码获取机制**：
   * 用户点击行操作【获取门锁密码】（挂锁）/【获取门禁密码】（人脸）为统一入口。
   * 系统首先执行 `matchUnlockApprovalConfig(deviceId)`：
     * **命中已启用审批配置**：弹出 `UnlockApplySubmitDialog` 发起申请，提交后写入我的开锁申请并在详情提供 Deep link；
     * **未命中/免审**：弹出免审直发临时密码窗口，写入 `approval_required=false` 开锁记录供事后审计。
2. **短信下发边界 (R31)**：
   * **挂锁门禁**：免审直发或审批通过后均支持【短信发送密码】+【页面直接展示密码】。
   * **人脸门禁**：无论免审还是审批通过，仅在前端页面展示临时密码，**绝不调用短信 API**。
3. **开锁审批专网专道与自审禁止**：
   * 开锁审批挂载于「工作中心 → 审批中心 → 其他审批 → 开锁审核」，**不接入系统常规流程引擎**，不进通用待处理池，专网专道保障高频开锁实时响应。
   * **P06 自审禁止（R11）**：审批人不可审批自己发起的开锁申请；本人申请底部仅展示「详情」，不提供「去处理」或「去审批」。

### 3.2 预警配置与物联穿透机制
1. **预警等级公共底座 (03/01)**：
   * 租户支持 2~20 档严重度动态字典，支持配置 `sort_order` 权重与 `sync_to_order_warn` 穿透开关。
   * **保护规则**：最低 2 档保护禁止删除；被任何有效规则引用时强校验拦截删除。
2. **设备预警配置 (03/02)**：
   * **子类型互斥 (R14)**：「xxx设备上线」子类型须单独成规则，不可与其他运营监控子类型混配；
   * **设备范围唯一性 (R04/R05)**：同一设备同一预警子类型在租户内仅能绑定一条生效中规则；全局新设备规则每大类仅限 1 条。
   * **防抖模型分流**：瞬态安防事件（防拆/破门）锁定为【即时触发】（0 延时）；持续传感器事件支持【持续时长】或【连续次数】。
3. **订单预警配置 (03/03) 与一站式多策略**：
   * **一单一配 (R04)**：1 笔抵质押或监管订单 1:1 对应 1 条有效综合规则包。
   * **逐条保存理念**：页面内 6 大策略（超时、跌价、盘点、巡检、质押率、贷中风控）卡片独立保存并热同步风控引擎；Switch=OFF 保存时至少保留 1 项有效策略（R07a 阻断）。
   * **抵质押率双等级 (R18)**：平仓线严重度 `sort_order` 必须大于等于补仓线。
4. **物联穿透闭环机制 (R11~R13)**：
   * 当设备预警等级配置 `sync_to_order_warn=true` 且触发告警时，若设备所在库区存在在押订单，自动生成 `warn_source=IOT_PENETRATION` 押品预警流水。
   * **处置闭环**：穿透流水在订单侧只读，必须在现场设备台账核销解除后联动回写订单预警为【已处理有效】。

### 3.3 贷中风控与风险公示
1. **智风控 OpenAPI 异步调度**：
   * 贷中风控管理中实时校验订单在押/存续/有效规则三要素准入资格；发起执行后由智风控平台受理并生成唯一 `task_id`；
   * 模型拒绝时回调写回结果并自动联动生成 02/02 押品高危预警；若需补充材料支持带签名跳转联登任务中心。
2. **风险公示合规与司法存证**：
   * 来源于已处置完成的有效告警，对外合规披露；
   * **取消公示约束**：仅 R-RISK-MGR 权限可操作，强制录入 <= 200 字取消理由，全程审计留痕；
   * **不可变快照**：固化原预警事实、抓拍图、处置材料与操作时间轴，私有 OSS 动态申请临时访问签名。

---

## 🎨 4. 前端原型与标注规范（标杆模板）

### 4.1 技术栈与架构
* **原型工程**：React 18 + TypeScript + Vite + Tailwind CSS + Lucide Icons
* **标注层封装**：`PrototypeAnnotationProvider` + `PrototypeAnnotationTarget` (PC/H5)
* **真实 Mock**：使用贴近大宗商品（电解铜、热轧卷板、铝锭、橡胶）真实业务数据，拒绝无意义占位符。

### 4.2 原型标注标杆标准（四维一体）
以【设备预警信息】【订单/押品预警信息】的列表、新增编辑、详情页为全平台**标杆模板**，所有功能页面必须严格遵循：
1. **需求文档三件套 (`PrototypeDocument`)**：
   * 主 PRD 需求规格 (`prd`)
   * 字段字典清单 (`fields`)
   * 业务规则规格 (`rules`)
2. **页面级架构打点 (`Page-Level`)**：
   * 页面定位与全生命周期业务/状态机流转图（强制采用 **Mermaid** `flowchart` 或 `stateDiagram-v2` 表达）。
   * 上下游协同数据源头、下游联动机制与历史快照不可变边界。
   * RBAC 权限矩阵、租户与仓库数据权限隔离、自审禁止规则（P06/R11）。
3. **字段清单与校验打点 (`Fields`)**：
   * 标明字段编码（如 `severity_code`、`rule_name`）、数据类型、是否必填、长度限制、取值范围与默认值。
   * 互斥与依赖约束（如 R14 上线类互斥、R04 一单一配、抵质押率等级权重校验）。
4. **交互与异常打点 (`Actions & Dialogs`)**：
   * 状态变迁闭环（通过/生效/保存/驳回/撤回/失效/作废/删除）。
   * 交互防护：防未保存离开拦截（表单 Dirty 离开确认）、二次确认弹窗必填理由校验、防重复提交与 Toast 指引。
5. **PC 与 H5 全端对齐**：
   * PC 端与 H5 移动端在同一业务主题下，业务状态机、凭证状态机、字段字典与权限约束保持强一致。

### 4.3 PC 工作中心 · 审批中心首页布局（原型与线上一致）

> **真源线框**：[07/05/03 开锁审核_ASCII_审批中心入口.md](./02-PRD文档/B-迭代需求/6.2版本（2026.08）/07-审批中心/05-其他审批/03-开锁审核/开锁审核_ASCII_审批中心入口.md) · **原型实现**：`03-前端原型demo/B端PC/src/features/unlock-applies/`（`ApprovalCenterPage` / `ApprovalCenterNavStrip` / `ApprovalCenterPreviewPanel`）

1. **无左侧树形菜单**：
   * 线上 **工作中心** 模块**没有** Sidebar 子菜单；审批中心为顶栏进入后的**卡片聚合页**。
   * PC 原型通过 `shouldShowSidebar()`（`navigation.ts`）在工作中心路由下隐藏 `SidebarNav`，与「功能与数据权限」全宽页一致。
   * 权限清单 CSV 中的工作中心子路径仍保留，用于占位路由与 RBAC 对照，**不代表**线上左侧菜单。
2. **导航区（单行三列）**：
   * 三大区块 **业务管理审批 / 客户需求审批 / 其他审批** 并排一行；组内子类型在分组标题**下方**横向 **Grid 均分**（列数 = 子项数，组间宽度按子项数量比例分配）。
   * 子项为 **icon + 文案**；文案按 **每行 4 个汉字**逻辑断行（`whitespace-nowrap` 防止 CSS 二次折行）；角标为待办数红点。
   * 宽度不足时整行可横向滚动；常规桌面宽度下应一屏展示，避免挤压下方预览区高度。
3. **预览区（选中子类型下方）**：
   * 选中任一子类型，下方展示 **最多 5 条** Mock/真实预览；超出走右上角 **「查看更多 >」** 跳转完整列表或 `PrototypeEmptyPage`。
   * **开锁审核**不进「待处理/已处理」池；预览支持「去处理」弹窗；完整列表 `/工作中心/审批中心/其他审批/开锁审核`。
   * 未实现子类型预览可用业务流 Mock 或占位空态，**不得**伪造 Sidebar 导航。
4. **占位策略**：
   * 菜单已收录但未交付的页面统一 `PrototypeEmptyPage`（与仓储、风控等模块一致），路由由 `implemented-routes.ts` + 权限清单生成。

---

## ⚠️ 5. AI 协作、思考与安全规范

### 5.1 定位与态度
1. **专业客观**：保持客观、坦诚的专业协作风格。不谄媚、不迎合、不做无意义的客套；事实清楚的问题上明确给出判断和依据。
2. **实事求是**：信息不足时明确指出缺口；与用户存在分歧时，基于事实、证据和业务逻辑说明理由，不盲目迎合。
3. **真实严谨**：不虚构事实、来源、代码或执行结果；对不可当场验证的事实主动标注不确定性。
4. **理性纠错**：用户指出回答有误时，先验证再回应：确认错了直接更正并简述原因；验证无误则说明理由，不因用户坚持而改口。

### 5.2 思考与解答逻辑
1. **反向推演**：回答复杂问题前，先充分分析关键事实、前提、约束和潜在反例，输出结论、关键依据和必要推导。
2. **歧义处置**：歧义影响交付方向的先提问澄清；影响较小的说明默认假设后继续。
3. **方案落地与权衡**：优先抓住问题核心，给出明确结论和可执行建议；开放问题优先给出一个推荐方案并说明关键取舍。
4. **分层展开**：根据问题复杂度控制回答深度：简单问题直接回答，复杂问题分层展开。

### 5.3 语言与文风
1. **自然表达**：使用自然、准确、流畅的中文，避免翻译腔和空泛说教。中文使用中文标点，代码、JSON、SQL、Mermaid 等保留原始半角符号。
2. **术语准确**：英文专业术语、字段名、API、组件名、代码可保留原文。
3. **结构先行**：默认先结论后依据，减少重复背景和无效总结。
4. **克制排版**：少用大面积粗体，善用 Mermaid 流程图、表格与代码块展现复杂结构。

### 5.4 资金、货权与代码安全约束
1. **强一致性与幂等性**：质押、解押、提货、开锁凭证下发与额度扣减等敏感操作，必须具备强一致性、幂等性防护与防并发锁机制。
2. **全路径审计留痕**：关键操作必须记录完整审计日志（操作人、时间戳、IP、前后数据镜像版本）。
3. **不可变存证快照**：规则变迁、等级调整与字典删除不得破坏存量事件、审批单据与风险公示的司法存证证据效力。

---
*版本说明：v1.4 | 生效时间：2026年 | 森云科技（Senyun Technology）*
