# [功能名称]字段清单

> **文档定位**：[功能名称]所有字段的唯一定义来源。下游 PRD、Demo 和关联单据只引用字段名称，不重复定义字段规则。
>
> **引用规则**：下游需要固化历史值时，标注「继承自[功能名称]」或「快照字段」。
>
> **修改原则**：字段定义变化只修改本文件，并同步更新版本和修订记录。
>
> **版本**：V1.0 | 更新时间：YYYY-MM-DD

---

## 一、业务字段

> 档案类可拆分为“基础识别字段”和“经营与控制字段”；单据类用于定义头部字段。状态、计算、快照和校验规则直接填写在“取值说明”或“备注”中。

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| [唯一编码] | [用户填写 / 系统生成] | [格式、字符规则；全局唯一] | [必填 / 系统自动] | [可编辑 / 不展示] | [可编辑 / 只读] | 是 | 是（Input） | 是 | [实体唯一识别编码] | [被引用后是否允许修改] |
| [名称] | 用户填写 | [字符长度和格式] | 必填 | 可编辑 | 可编辑 | 是 | 是（综合搜索） | 是 | [实体正式名称] | [与唯一编码共同构成主要识别信息] |
| [业务字段] | [字段来源] | [类型、范围、单位、精度、默认值或枚举] | [必填 / 选填 / 条件必填] | [可编辑 / 只读 / 不展示] | [可编辑 / 只读] | [是 / 否] | [是 / 否（筛选组件）] | [是 / 否] | [字段业务含义] | [校验、联动、锁定、计算或快照规则] |
| [业务状态] | 系统生成 | 枚举：`[CODE_1]=[状态名称]` / `[CODE_2]=[状态名称]` | 系统自动 | 不展示 | 只读 | 是 | 是（Select） | 是 | [当前业务状态] | 通过动作按钮修改，不支持下拉直接变更 |

---

## 二、明细字段（如适用）

> 用于订单商品行、质押物明细、库存批次、预警明细等可重复数据结构。

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 行号 | 系统生成 | 正整数；从 1 递增 | 系统自动 | 只读 | 只读 | 否 | 否 | 是 | 明细行序号 | 删除后是否重排需明确 |
| [明细业务字段] | [字段来源] | [类型、范围、单位、精度或枚举] | [必填 / 选填 / 条件必填] | [可编辑 / 只读] | [可编辑 / 只读] | [是 / 否] | [是 / 否] | [是 / 否] | [字段业务含义] | [校验、快照、回写或计算规则] |
| [明细汇总字段] | 系统计算 | [计算公式；保留小数位；尾差处理] | 系统自动 | 只读 | 只读 | [是 / 否] | [是 / 否] | 是 | [数量、金额或货值汇总含义] | 不允许手工修改 |

---

## 三、系统字段

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 创建人 | 系统生成 | 当前登录用户姓名或账号 | 系统自动 | 不展示 | 只读 | 否 | 否 | 是 | 首次创建操作人 | 详情页固定置底展示 |
| 创建时间 | 系统生成 | `YYYY-MM-DD HH:mm:ss` | 系统自动 | 不展示 | 只读 | 否 | 否 | 是 | 首次创建时间 | - |
| 最后修改人 | 系统生成 | 当前登录用户姓名或账号 | 系统自动 | 不展示 | 只读 | 否 | 否 | 是 | 最近一次修改操作人 | - |
| 最后修改时间 | 系统生成 | `YYYY-MM-DD HH:mm:ss` | 系统自动 | 不展示 | 只读 | [是 / 否] | 否 | 是 | 最近一次修改时间 | 列表辅助判断数据新鲜度 |
| [审核人/作废人/关闭人] | 系统生成 | 执行动作的用户 | 系统自动 | 不展示 | 只读 | 否 | 否 | 是 | 关键动作操作人 | 无对应流程时删除 |
| [审核时间/作废时间/关闭时间] | 系统生成 | `YYYY-MM-DD HH:mm:ss` | 系统自动 | 不展示 | 只读 | 否 | 否 | 是 | 关键动作时间 | 无对应流程时删除 |

---

## 四、展示与引用规则

- 列表优先展示列：`[字段 1]` → `[字段 2]` → `[字段 3]` → `[状态]` → `[关键数量/金额/风险指标]` → `[最后修改时间]`。
- 数量类字段：[单位、精度、是否千分位展示]；金额类字段：[币种、保留小数位、是否千分位展示]。
- 日期类字段：`YYYY-MM-DD`；日期时间类字段：`YYYY-MM-DD HH:mm:ss`；空值统一展示为 `-`。
- 下游[功能/单据]引用本功能时，展示格式为`[编码] + [名称]`。
- 需要固化的字段包括：[字段名称]；来源档案变化不影响已保存历史数据。
- 停用或失效数据不允许在新业务中继续选择，历史数据仍允许查看。

---

## Changelog

| 日期 | 变更内容 | 影响范围 |
| :--- | :--- | :--- |
| YYYY-MM-DD | 初版发布，完成[功能名称]字段定义 | [本功能及下游功能] |
