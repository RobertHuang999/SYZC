// 自动生成的 6.2 历史迁移与割接方案 PRD 文档数据集合

export interface MigrationDocumentItem {
  id: string
  title: string
  shortTitle: string
  category: string
  filePath: string
  badge: string
  routePath: string
  version: string
  author: string
  updatedAt: string
  description: string
  content: string
}

export const migrationDocuments: MigrationDocumentItem[] = [
  {
    "id": "migration-index",
    "title": "历史迁移总索引",
    "shortTitle": "历史迁移总索引",
    "category": "统一入口",
    "filePath": "历史迁移总索引.md",
    "badge": "割接入口",
    "routePath": "/历史迁移与割接/历史迁移总索引",
    "version": "V1.1",
    "author": "AI PM 工作流",
    "updatedAt": "2026-08-25",
    "description": "6.1→6.2 一次性割接工作的统一入口与全局约束说明",
    "content": "# 6.2 历史迁移与割接方案总索引\n\n> **版本**：V1.0 | **更新日期**：2026-08-25\n>\n> **文档定位**：6.1→6.2 一次性割接工作的统一入口。01/02/03 三大主干描述**目标态**产品架构；本目录描述**过渡态**历史配置清洗、流水迁移、业务兼容映射与割接验收。\n>\n> **上级索引**：[6.2 版本总览 README](../README.md)\n>\n> **割接完成后**：本目录可整体归档，不必与 01/02/03 长期并列维护。\n\n---\n\n## 一、目录结构\n\n```text\n04-历史迁移与割接方案/\n├── 历史迁移总索引.md                    # 🌟 割接统一入口\n├── 6.1→6.2 三旧模块兼容总说明.md          # ⭐ 一页式总说明（产品/实施必读）\n├── 01设备侧规则与流水迁移/\n│   └── 03历史数据迁移与回滚技术方案.md    # IoT 规则 + 事件流水 SQL 迁移\n├── 02订单侧规则与流水迁移/\n│   └── 6.1→6.2 订单规则与押品流水迁移.md  # 订单规则 + 押品流水迁移\n├── 03业务兼容映射/\n│   └── 6.1→6.2 门禁事务与设备事务通知兼容映射.md  # 三旧模块合流对照\n└── 04割接验收与推演/\n    └── 6.2跨域用例数据推演.md            # 迁移后联调与验收推演\n```\n\n---\n\n## 二、文档清单与阅读顺序\n\n| 序号 | 文档 | 读者 | 用途 |\n| :---: | :--- | :--- | :--- |\n| 0 | [三旧模块兼容总说明](./6.1→6.2 三旧模块兼容总说明.md) | **产品、实施、全员** | **一页速览**：菜单怎么换、日常怎么用、与 6.1 差异 |\n| 1 | [门禁事务与设备事务通知兼容映射](./03业务兼容映射/6.1→6.2 门禁事务与设备事务通知兼容映射.md) | 产品、研发、测试 | 完整字段/事务映射、验收清单 |\n| 2 | [设备侧历史数据迁移与回滚](./01设备侧规则与流水迁移/03历史数据迁移与回滚技术方案.md) | DBA、后端 | IoT 规则 `iot_rule_config` + 流水 `iot_event_ledger` |\n| 3 | [订单侧规则与押品流水迁移](./02订单侧规则与流水迁移/6.1→6.2 订单规则与押品流水迁移.md) | DBA、后端 | 订单规则 + 押品预警台账迁移 |\n| 4 | [6.2 跨域用例数据推演](./04割接验收与推演/6.2跨域用例数据推演.md) | 测试、产品 | 割接后端到端验收用例 |\n\n**推荐阅读顺序**：**总说明（对齐认知）** → 完整兼容映射（需字段/SQL 时）→ 03/01 预警等级就绪 → 设备侧迁移 → 订单侧迁移 → 跨域推演验收。\n\n---\n\n## 三、迁移范围一览\n\n| 6.1 源 | 6.2 目标表/模块 | 负责文档 |\n| :--- | :--- | :--- |\n| `t_dev_warn_cfg` + `t_dev_event_cfg` | `iot_rule_config` | 设备侧迁移 §Step 1 |\n| 设备预警流水 + 事务通知 + 门禁事务 | `iot_event_ledger` | 设备侧迁移 §Step 2 |\n| `t_door_access_log` | `iot_event_ledger` | 设备侧迁移 + 兼容映射 §R10 |\n| `t_order_warn_cfg` | `order_rule_config` + `order_rule_strategy` | 订单侧迁移 |\n| `t_order_warn_log` | `collateral_warn_ledger` | 订单侧迁移 |\n\n---\n\n## 四、全局约束（所有迁移文档共同遵守）\n\n1. **批次幂等**：所有写操作带 `migration_batch_id`，支持重跑与整批回滚。\n2. **历史不触发新业务**：迁移事件标记 `event_source=HISTORY_MIGRATION`，不触发通知、不触发订单穿透。\n3. **等级解析**：必须使用租户内 `severity_level_id`，禁止 SQL 写死 `L1~L5`。\n4. **快照不回写**：迁移固化触发时等级/规则快照，后续字典变更不得回写历史流水。\n5. **异常隔离**：未知子类型写入 `migration_exception`，以 `legacy_sub_type` 保留，禁止静默丢弃。\n\n---\n\n## 五、关联文档（目标态，非迁移）\n\n| 文档 | 路径 | 说明 |\n| :--- | :--- | :--- |\n| 6.2 全局契约与 RBAC | [00-全局契约与RBAC权限矩阵.md](../00-全局契约与RBAC权限矩阵.md) | 运行时跨模块契约 |\n| 通知模板与协同规范 | [通知模板.md](../02-预警信息/协同规范/通知模板.md) | 运行时穿透与通知（非一次性迁移） |\n| 设备预警信息业务规则 R10 | [业务规则规格](../02-预警信息/01设备预警信息/设备预警信息业务规则规格.md) | 门禁历史迁移口径引用 |\n\n---\n\n## 修订记录\n\n| 日期 | 版本 | 变更说明 |\n| :--- | :---: | :--- |\n| 2026-08-25 | **V1.0** | 初版：从 02/03 分散迁移文档 Consolidate 为 04 统一入口 |\n| 2026-08-25 | **V1.1** | 新增 [三旧模块兼容总说明](./6.1→6.2 三旧模块兼容总说明.md) 一页式导读 |\n"
  },
  {
    "id": "three-legacy-modules-compatibility",
    "title": "6.1→6.2 三旧模块兼容总说明",
    "shortTitle": "三旧模块兼容总说明",
    "category": "核心兼容架构",
    "filePath": "6.1→6.2 三旧模块兼容总说明.md",
    "badge": "全员必读",
    "routePath": "/历史迁移与割接/三旧模块兼容总说明",
    "version": "V1.0",
    "author": "AI PM 工作流",
    "updatedAt": "2026-08-25",
    "description": "面向产品/实施/测试的一页式总说明：门禁事务与设备事务通知去哪了、怎么用新菜单",
    "content": "# 6.1→6.2 三旧模块兼容总说明\n\n> **文档定位**：面向产品、实施、测试与研发的**一页式总说明**。快速回答「门禁事务记录、设备事务通知信息/配置去哪了、怎么用新菜单、哪些能力变了」。\n>\n> **版本**：V1.0 | **编制日期**：2026-08-25\n>\n> **上级索引**：[历史迁移总索引](./历史迁移总索引.md)\n>\n> **详细规格**（字段映射、15 种事务枚举、迁移 SQL、验收清单）见 [门禁事务与设备事务通知兼容映射](./03业务兼容映射/6.1→6.2 门禁事务与设备事务通知兼容映射.md)。\n\n---\n\n## 一、总体兼容策略\n\n6.2 对以下三个 6.1 模块采取 **「菜单取消 + 数据合流 + 规则统一」**，不是三套系统并行运行。\n\n\n| 6.1 原模块      | 6.2 新归属              | 一句话                               |\n| ------------ | -------------------- | --------------------------------- |\n| **门禁事务记录**   | `02-预警信息 / 01设备预警信息` | 独立菜单取消；通行流水合入统一台账，类型为 `常规通行与操作事务` |\n| **设备事务通知信息** | `02-预警信息 / 01设备预警信息` | 与设备预警、门禁事务合并为同一套事件流水              |\n| **设备事务通知配置** | `03-预警配置 / 02设备预警配置` | 与设备预警配置合并为统一规则表                   |\n\n\n**底层两张表承接全部 IoT 侧能力：**\n\n\n| 层级  | 6.2 模块               | 数据表                |\n| --- | -------------------- | ------------------ |\n| 配置  | 物联网 IoT → **设备预警配置** | `iot_rule_config`  |\n| 流水  | 物联网 IoT → **设备预警信息** | `iot_event_ledger` |\n\n\n```mermaid\nflowchart LR\n    subgraph old[6.1 三套模块]\n        A[门禁事务记录]\n        B[设备事务通知信息]\n        C[设备事务通知配置]\n    end\n\n    subgraph new[6.2 统一体系]\n        R[设备预警配置<br/>iot_rule_config]\n        E[设备预警信息<br/>iot_event_ledger]\n    end\n\n    A --> E\n    B --> E\n    C --> R\n    R -->|规则命中| E\n```\n\n\n\n---\n\n\n\n## 二、菜单与入口变化\n\n\n| 6.1 菜单              | 6.2 怎么办                                         |\n| ------------------- | ----------------------------------------------- |\n| 设备管理 → **门禁事务记录**   | **取消**；改到「设备预警信息」全量筛选，或从门禁设备「设备数据 → 事务记录」Tab 查看 |\n| 风控 → **设备事务通知信息**   | **取消**；合并进「设备预警信息」                              |\n| 配置管理 → **设备事务通知配置** | **取消**；合并进「设备预警配置」                              |\n| 风控 → 设备预警信息         | 物联网 IoT → **设备预警信息**（承接全部 IoT 流水）               |\n| 配置管理 → 设备预警配置       | 物联网 IoT → **设备预警配置**（承接全部 IoT 规则）               |\n\n\n**保留的入口：**\n\n- 门禁设备 → **设备数据 → 事务记录 Tab**：仍可用，数据源已切换为 `iot_event_ledger`，与全量列表一致。\n- 移动端独立「门禁事务记录」菜单：**取消**，改由设备预警信息移动端承接。\n\n---\n\n\n\n## 三、三类旧能力怎么对应\n\n\n\n### 3.1 门禁事务记录 → 设备预警信息\n\n- 原 **15 种门禁事务名称全部保留**，作为子类型展示。\n- 正常通行/开关锁等归入 `常规通行与操作事务`；拆壳、撬锁、门打开超时等异常归入挂锁/人脸预警大类。\n- 6.1 的人员追溯、监控抓拍、只读不可删改等规则**继续生效**（R05~R10）。\n- **双入口**：全量列表 + 门禁设备 Tab，共用同一 `iot_event_ledger`。\n\n\n\n### 3.2 设备事务通知信息 → 设备预警信息\n\n\n| 6.1 事务类型    | 6.2 对应                          |\n| ----------- | ------------------------------- |\n| 开锁通知 / 关锁通知 | 子类型 `正常开关锁事务`                   |\n| 设备上线        | 子类型 `监控设备上线`（可配「仅针对新设备」）        |\n| 设备移除        | 附录 A 无独立子类型；移除联动由 IoT「移除设备」流程承担 |\n\n\n\n\n### 3.3 设备事务通知配置 → 设备预警配置\n\n\n| 6.1 配置能力     | 6.2 对应                   |\n| ------------ | ------------------------ |\n| 开锁/关锁/上线通知规则 | 设备预警配置中选对应子类型            |\n| 选择设备 / 针对新设备 | 关联设备 / 仅针对新设备（仅上线子类型）    |\n| 通知渠道与对象      | 保留；新增绑定 **预警等级**（2~20 档） |\n| 启用/停用        | 扩展为 生效中 / 停用 / 已失效 / 已删除 |\n\n\n---\n\n\n\n## 四、关键行为差异（必读）\n\n\n| 场景                 | 6.1                | 6.2                                                  |\n| ------------------ | ------------------ | ---------------------------------------------------- |\n| 正常开关锁 / 刷脸通行       | 事务通知，默认「未处理」，可人工核查 | **瞬态落账，直接「已处理（有效）」**，无需操作                            |\n| 物理破坏 / 离线 / 越界等风险  | 设备预警，需解除           | **不变**，仍在设备预警信息人工或自动解除                               |\n| 事务通知的「情况说明 + 现场照片」 | 所有未处理事务可填          | **仅风险类**展示解除入口；日常通行流水无解除按钮                           |\n| 升级通知               | 事务通知无              | 仅**持续型风险**支持超时升级                                     |\n| 历史数据               | 各模块独立表             | 迁入 `iot_rule_config` + `iot_event_ledger`，迁移批次不触发新通知 |\n\n\n**记忆口诀：**\n\n> **日常通行 = 流水落账即结案；物理风险 = 仍在设备预警信息做解除。**\n\n---\n\n\n\n## 五、用户操作指引\n\n\n| 我要做什么         | 6.1 路径             | 6.2 路径                              |\n| ------------- | ------------------ | ----------------------------------- |\n| 查某门禁设备的开关锁流水  | 门禁设备 → 设备数据 → 事务记录 | **不变**                              |\n| 全量检索门禁事务      | 设备管理 → 门禁事务记录      | 物联网 IoT → **设备预警信息**，筛选「常规通行与操作事务」  |\n| 查看开锁/上线通知记录   | 风控 → 设备事务通知信息      | 物联网 IoT → **设备预警信息**                |\n| 配置开锁/关锁通知     | 配置管理 → 设备事务通知配置    | 物联网 IoT → **设备预警配置**，子类型选 `正常开关锁事务` |\n| 配置新设备上线通知     | 同上，选「设备上线」         | **设备预警配置**，子类型 `监控设备上线`，可勾选「仅针对新设备」 |\n| 处理拆壳、越界、离线等告警 | 设备预警信息 → 解除        | **不变**                              |\n| 核查日常通行记录      | 门禁事务记录（只读）         | 设备预警信息 → 详情（默认已处理，**无需操作**）         |\n\n\n---\n\n\n\n## 六、与详细文档的关系\n\n\n| 需要了解…                | 阅读文档                                                                                                |\n| -------------------- | --------------------------------------------------------------------------------------------------- |\n| 本文：谁该读什么、菜单怎么换、日常怎么用 | **本文件**                                                                                             |\n| 15 种事务映射表、字段对照、验收清单  | [兼容映射（完整版）](./03业务兼容映射/6.1→6.2 门禁事务与设备事务通知兼容映射.md)                                                  |\n| IoT 规则与流水 SQL 迁移     | [设备侧迁移方案](./01设备侧规则与流水迁移/03历史数据迁移与回滚技术方案.md)                                                        |\n| 割接后联调验收              | [跨域用例数据推演](./04割接验收与推演/6.2跨域用例数据推演.md)                                                              |\n| 6.2 目标态功能规格          | [设备预警信息主 PRD](../02-预警信息/01设备预警信息/设备预警信息主PRD.md) · [设备预警配置主 PRD](../03-预警配置/02设备预警配置/设备预警配置主PRD.md) |\n\n\n---\n\n\n\n## 修订记录\n\n\n| 日期         | 版本       | 变更说明            |\n| ---------- | -------- | --------------- |\n| 2026-08-25 | **V1.0** | 初版：三旧模块一页式兼容总说明 |\n\n\n"
  },
  {
    "id": "device-side-migration-rollback",
    "title": "01. 设备侧历史数据迁移与回滚技术方案",
    "shortTitle": "设备侧规则与流水迁移",
    "category": "设备侧迁移",
    "filePath": "01设备侧规则与流水迁移/03历史数据迁移与回滚技术方案.md",
    "badge": "DBA/后端",
    "routePath": "/历史迁移与割接/设备侧规则与流水迁移",
    "version": "V3.0",
    "author": "AI PM 工作流",
    "updatedAt": "2026-08-21",
    "description": "IoT 规则与事件台账迁移、动态等级解析、未处理聚合与回滚 SQL",
    "content": "# 03/02 设备预警配置历史数据迁移与回滚技术方案\n\n> **文档定位**：指导 6.1 设备预警配置、事务通知配置和设备事件流水迁移到 6.2 统一 IoT 规则与事件台账。本文只负责设备侧；订单侧配置和押品流水迁移见 [订单侧历史数据迁移方案](../02订单侧规则与流水迁移/6.1→6.2 订单规则与押品流水迁移.md)。三旧模块（门禁事务记录、设备事务通知信息/配置）的业务兼容映射见 [6.1→6.2 门禁事务与设备事务通知兼容映射](../03业务兼容映射/6.1→6.2 门禁事务与设备事务通知兼容映射.md)。\n>\n> **上级索引**：[历史迁移总索引](../历史迁移总索引.md)\n>\n> **版本**：V3.0 | **编制日期**：2026-08-21 | **适用环境**：MySQL 8.0+ / TiDB\n>\n> **等级依赖**：[03/01 预警等级](../../03-预警配置/01预警等级/预警等级业务规则规格.md)；等级解析必须使用租户内 `severity_level_id`，不得在 SQL 中写死 `L1~L5`。\n\n## 一、迁移目标\n\n1. 合并老设备规则和事务通知规则到 `iot_rule_config`；保留合法的 05 附录 A 子类型，非法/未知子类型进入异常清单。\n2. 设备规则和事件流水均保存 `severity_level_id`，并固化编码、名称、颜色、`sort_order` 快照；严重度只按 `sort_order` 比较。\n3. 同一租户、设备、子类型的未处理历史事件可按原有 05 聚合规则合并，时间轴保留每一条原始触发；已处理流水不改变业务结果。\n4. 迁移批次可重跑、可回滚；图片仅保留 OSS key；历史数据不触发新的 06 穿透通知。\n\n## 二、源类型与合法子类型\n\n| 设备大类 | 迁移后的合法子类型示例 | 事件性质 |\n| :--- | :--- | :--- |\n| 图像识别预警 | 行人入侵、车辆入侵、形态变化 | 持续/瞬态按 05 附录 A |\n| 设备物联预警 | 温度异常、湿度异常、气体异常、设备离线 | 持续 |\n| 智能挂锁预警 | 拆壳、被剪、撬锁、非法开箱、正常开关锁事务、低电量、门锁离线 | 瞬态/持续 |\n| 人脸门禁预警 | 人脸不匹配、门打开超时、正常刷脸通行 | 瞬态/持续 |\n| GPS 预警 | 出围栏、超速、设备拆除、设备离线 | 持续/瞬态 |\n\n旧系统中不在 05 权威枚举中的挂锁撬锁编码只能写入 `legacy_sub_type`，并进入 `migration_exception`，不得作为 6.2 可配置子类型或用例数据。\n\n## 三、动态等级解析\n\n每条老规则和每条历史事件分别执行以下优先级：\n\n1. 老数据已有等级 ID，校验租户归属后使用；\n2. 按同租户 `severity_code` 精确匹配；\n3. 按同租户等级名称唯一匹配；\n4. 使用迁移批次前经业务确认的 `tenant_legacy_severity_map`；\n5. 仍无法匹配则写异常清单，保留原始等级展示快照，标记 `UNRESOLVED`，不伪造默认等级。\n\n新规则/事件必须包含：\n\n```text\nseverity_level_id\nseverity_code\nseverity_name\nseverity_color\nsort_order_snapshot\nsync_to_order_warn_snapshot\n```\n\n## 四、目标表关键结构\n\n```sql\nCREATE TABLE IF NOT EXISTS iot_rule_config (\n  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,\n  iot_rule_id VARCHAR(64) NOT NULL,\n  tenant_id VARCHAR(64) NOT NULL,\n  rule_name VARCHAR(50) NOT NULL,\n  device_category VARCHAR(32) NOT NULL,\n  sub_types JSON NOT NULL,\n  device_ids JSON NULL,\n  severity_level_id VARCHAR(64) NULL,\n  severity_code VARCHAR(16) NULL,\n  severity_name VARCHAR(32) NULL,\n  severity_color VARCHAR(16) NULL,\n  sort_order_snapshot INT NULL,\n  sync_to_order_warn_snapshot TINYINT(1) NOT NULL DEFAULT 0,\n  debounce_params JSON NULL,\n  notify_channels JSON NULL,\n  notify_user_ids JSON NULL,\n  upgrade_days INT NOT NULL DEFAULT 0,\n  upgrade_user_ids JSON NULL,\n  status VARCHAR(16) NOT NULL DEFAULT 'ACTIVE',\n  migration_batch_id VARCHAR(64) NULL,\n  legacy_rule_ids JSON NULL,\n  version INT NOT NULL DEFAULT 1,\n  is_deleted TINYINT(1) NOT NULL DEFAULT 0,\n  created_time DATETIME NOT NULL,\n  updated_time DATETIME NOT NULL,\n  PRIMARY KEY (id),\n  UNIQUE KEY uk_iot_rule_id (iot_rule_id),\n  KEY idx_tenant_status (tenant_id, status, is_deleted),\n  KEY idx_migration_batch (migration_batch_id)\n) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;\n\n-- iot_event_ledger 需增加下列迁移字段；其他原有事件字段沿用 05 字段清单\nALTER TABLE iot_event_ledger\n  ADD COLUMN IF NOT EXISTS severity_level_id VARCHAR(64) NULL,\n  ADD COLUMN IF NOT EXISTS severity_code VARCHAR(16) NULL,\n  ADD COLUMN IF NOT EXISTS severity_name VARCHAR(32) NULL,\n  ADD COLUMN IF NOT EXISTS severity_color VARCHAR(16) NULL,\n  ADD COLUMN IF NOT EXISTS sort_order_snapshot INT NULL,\n  ADD COLUMN IF NOT EXISTS sync_to_order_warn_snapshot TINYINT(1) NOT NULL DEFAULT 0,\n  ADD COLUMN IF NOT EXISTS legacy_sub_type VARCHAR(64) NULL,\n  ADD COLUMN IF NOT EXISTS severity_resolution_status VARCHAR(24) NOT NULL DEFAULT 'RESOLVED',\n  ADD COLUMN IF NOT EXISTS migration_batch_id VARCHAR(64) NULL;\n```\n\n## 五、迁移步骤\n\n### Step 0：建立批次和映射\n\n```sql\nINSERT INTO migration_batch (migration_batch_id, module, source_version, status, started_at)\nVALUES ('MIG-IOT-20260821-001', 'IOT_WARNING', '6.1', 'RUNNING', NOW());\n\nSELECT tenant_id, COUNT(*)\nFROM tenant_severity_level\nWHERE status = 'ENABLED' AND is_deleted = 0\nGROUP BY tenant_id;\n```\n\n在批次开始前维护 `tenant_legacy_severity_map`；任一租户的旧等级映射不完整，批次标记 `BLOCKED`，不得按子类型静默推断等级。\n\n### Step 1：规则合流\n\n将 `t_dev_warn_cfg` 与 `t_dev_event_cfg` 规范化到 `stg_iot_rule`，按 `tenant_id + rule_name + device_category + sub_type` 去重；设备和子类型冲突、未知子类型、跨租户等级引用写 `migration_exception`。\n\n```sql\nINSERT INTO iot_rule_config (\n  iot_rule_id, tenant_id, rule_name, device_category, sub_types, device_ids,\n  severity_level_id, severity_code, severity_name, severity_color,\n  sort_order_snapshot, sync_to_order_warn_snapshot, debounce_params,\n  notify_channels, notify_user_ids, upgrade_days, upgrade_user_ids,\n  status, migration_batch_id, legacy_rule_ids, version, created_time, updated_time\n)\nSELECT\n  CONCAT('MIG-IOT-', :batch_id, '-', SHA2(CONCAT(tenant_id, ':', source_rule_key), 256)),\n  tenant_id, MAX(rule_name), device_category, JSON_ARRAYAGG(sub_type),\n  JSON_MERGE_PRESERVE(JSON_ARRAY(), JSON_ARRAYAGG(device_id)),\n  MAX(severity_level_id), MAX(severity_code), MAX(severity_name), MAX(severity_color),\n  MAX(sort_order), MAX(sync_to_order_warn), MAX(debounce_params),\n  MAX(notify_channels), MAX(notify_user_ids), MAX(upgrade_days), MAX(upgrade_user_ids),\n  'ACTIVE', :batch_id, JSON_ARRAYAGG(legacy_rule_id), 1,\n  MIN(created_time), MAX(updated_time)\nFROM stg_iot_rule\nWHERE migration_status = 'READY'\nGROUP BY tenant_id, source_rule_key, device_category;\n```\n\n事务通知规则按 05 附录 A 合流为同一 `iot_rule_config` 的子类型，不新建一个虚假的通知等级。历史正常通行事务仍按事件规则落账，但默认状态遵循 05 的瞬态自动处理规则。\n\n### Step 2：事件流水迁移与聚合\n\n- 已处理事件：按事件 ID 1:1 迁移，保留原处理人、时间和材料。\n- 未处理持续型事件：按 `tenant_id + device_id + sub_type` 聚合为 1 条 `PENDING_VALID`，`trigger_count` 取原条数，首末时间取 MIN/MAX，所有明细写 `iot_event_timeline`。\n- 瞬态事务：每条事件单独落账并置 `PROCESSED`，处理方式为 `SYSTEM`，不进入频次聚合。\n- 事件等级、同步开关和规则快照取触发时解析结果；字典后续变化不回写。\n- 历史迁移事件 `event_source=HISTORY_MIGRATION`，不触发 06 穿透通知；如需补发必须走单独重放任务并使用幂等事件 ID。\n\n## 六、校验与验收\n\n```sql\n-- 规则数量和批次完整性\nSELECT COUNT(*) FROM iot_rule_config WHERE migration_batch_id = :batch_id;\nSELECT COUNT(*) FROM migration_exception WHERE migration_batch_id = :batch_id;\n\n-- 等级必须同租户，且解析成功的记录不允许缺快照\nSELECT e.event_id\nFROM iot_event_ledger e\nLEFT JOIN tenant_severity_level l ON l.severity_level_id = e.severity_level_id\nWHERE e.migration_batch_id = :batch_id\n  AND e.severity_resolution_status = 'RESOLVED'\n  AND (l.severity_level_id IS NULL OR l.tenant_id <> e.tenant_id\n       OR e.sort_order_snapshot IS NULL);\n\n-- 未处理聚合后的唯一性\nSELECT tenant_id, device_id, sub_type, COUNT(*)\nFROM iot_event_ledger\nWHERE migration_batch_id = :batch_id AND process_status = 'PENDING_VALID'\nGROUP BY tenant_id, device_id, sub_type HAVING COUNT(*) > 1;\n```\n\n通过条件：未知子类型均有异常记录；已解析等级 ID 归属正确；持续型未处理事件无重复；时间轴条数等于原始触发条数；迁移不产生新穿透通知；图片可通过签名服务访问；抽样覆盖温度自动恢复、挂锁破坏人工解除、正常开锁事务、无权限仓库四类场景。\n\n## 七、回滚\n\n```sql\nSTART TRANSACTION;\nDELETE FROM iot_event_timeline WHERE migration_batch_id = :batch_id;\nDELETE FROM iot_event_ledger WHERE migration_batch_id = :batch_id;\nDELETE FROM iot_rule_config WHERE migration_batch_id = :batch_id;\nDELETE FROM migration_exception WHERE migration_batch_id = :batch_id;\nUPDATE migration_batch SET status = 'ROLLED_BACK', finished_at = NOW()\nWHERE migration_batch_id = :batch_id;\nCOMMIT;\n```\n\n禁止 `TRUNCATE`、禁止按时间或状态删除、禁止删除上线后非本批次新增数据。重跑必须使用新批次号；回滚前停止该批次规则监听和补偿任务。\n\n## 修订记录\n\n| 日期 | 版本 | 变更 |\n| :--- | :---: | :--- |\n| 2026-08-21 | **V3.0** | 统一 03/01 动态等级 ID/快照、合法 05 子类型、批次化迁移、异常隔离与可恢复回滚 |\n| 2026-08-20 | V2.5 | 废弃旧版固定等级和旧枚举迁移方案 |\n"
  },
  {
    "id": "order-side-migration",
    "title": "02. 6.1→6.2 订单规则与押品流水迁移",
    "shortTitle": "订单规则与押品流水迁移",
    "category": "订单侧迁移",
    "filePath": "02订单侧规则与流水迁移/6.1→6.2 订单规则与押品流水迁移.md",
    "badge": "DBA/后端",
    "routePath": "/历史迁移与割接/订单规则与押品流水迁移",
    "version": "V2.0",
    "author": "AI PM 工作流",
    "updatedAt": "2026-08-24",
    "description": "订单预警规则与押品流水向 6.2 策略化架构迁移与幂等清洗",
    "content": "# 03/03 订单预警配置历史数据迁移与回滚技术方案\n\n> **文档定位**：指导 6.1 订单预警配置与押品预警流水迁移到 6.2 的**综合规则主表 + 策略子项表 + 押品预警台账**模型。本文是迁移实施、校验、回滚和异常处置的唯一依据。\n>\n> **版本**：V2.0 | **编制日期**：2026-08-21 | **适用环境**：MySQL 8.0+ / TiDB\n>\n> **上级索引**：[历史迁移总索引](../历史迁移总索引.md)\n>\n> **关联基准**：[订单预警配置字段清单](../../03-预警配置/03订单预警配置/订单预警配置字段清单.md) · [订单预警配置业务规则规格](../../03-预警配置/03订单预警配置/订单预警配置业务规则规格.md) · [03/01 预警等级业务规则规格](../../03-预警配置/01预警等级/预警等级业务规则规格.md)\n\n## 一、迁移目标与不可违反的约束\n\n1. 同一租户同一订单的历史配置先聚合为**一条** `order_rule_config`，再按预警类型拆成 `order_rule_strategy` 子项；不能把历史每条配置直接写成一条新主规则。\n2. 6.2 规则等级只保存 04 的 `severity_level_id`，展示字段由该 ID 回填。严重度比较只使用 `sort_order`；`L1~L5` 仅可作为某些租户的旧编码示例。\n3. 历史配置中的通知渠道、对象、升级天数/对象迁入对应策略子项；抵/质押率历史配置必须拆成补仓线、平仓线及各自等级。\n4. IoT 五类继续区分 `LEGACY_IOT` 与 `IOT_PENETRATION`，不得伪装成 06 商业策略；有设备事件关联的历史记录保留 `related_device_event_id`。\n5. 6.2 运行时 `fuse_status` 统一写 `NONE`。旧熔断值仅写 `legacy_fuse_status` 审计字段，不得在迁移时锁定出库或解押。\n6. 迁移必须按 `migration_batch_id` 幂等；回滚只允许删除本批次产生的数据，禁止按状态、时间或全表删除。\n\n## 二、目标模型\n\n```mermaid\nflowchart LR\n    OLD_CFG[\"t_order_warn_cfg<br/>历史多条配置\"] --> AGG[\"按 tenant_id + order_id 聚合\"]\n    AGG --> RULE[\"order_rule_config<br/>一订单一综合规则\"]\n    AGG --> STRATEGY[\"order_rule_strategy<br/>按 strategy_type 拆子项\"]\n    OLD_LOG[\"t_order_warn_log<br/>历史流水\"] --> LEDGER[\"collateral_warn_ledger<br/>等级/规则/来源快照\"]\n    LEVEL[\"04 tenant_severity_level<br/>ID + sort_order\"] --> STRATEGY\n    LEVEL --> LEDGER\n    IOT[\"05 iot_event_ledger\"] -.关联校验.-> LEDGER\n    EX[\"migration_exception\"] -.阻断生效.-> RULE\n```\n\n### 2.1 主表和子项表最小字段\n\n```sql\nCREATE TABLE IF NOT EXISTS order_rule_config (\n  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,\n  rule_id VARCHAR(64) NOT NULL,\n  tenant_id VARCHAR(64) NOT NULL,\n  order_id VARCHAR(64) NOT NULL,\n  order_no VARCHAR(64) NOT NULL,\n  order_type VARCHAR(16) NOT NULL,\n  rule_name VARCHAR(50) NOT NULL,\n  status VARCHAR(16) NOT NULL DEFAULT 'ACTIVE',\n  migration_batch_id VARCHAR(64) NULL,\n  migration_status VARCHAR(24) NOT NULL DEFAULT 'MIGRATED',\n  source_rule_count INT NOT NULL DEFAULT 0,\n  version INT NOT NULL DEFAULT 1,\n  is_deleted TINYINT(1) NOT NULL DEFAULT 0,\n  created_by VARCHAR(64) NULL,\n  created_time DATETIME NOT NULL,\n  updated_by VARCHAR(64) NULL,\n  updated_time DATETIME NOT NULL,\n  PRIMARY KEY (id),\n  UNIQUE KEY uk_rule_id (rule_id),\n  UNIQUE KEY uk_active_tenant_order (tenant_id, order_id, status, is_deleted),\n  KEY idx_migration_batch (migration_batch_id)\n) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;\n\nCREATE TABLE IF NOT EXISTS order_rule_strategy (\n  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,\n  strategy_id VARCHAR(64) NOT NULL,\n  rule_id VARCHAR(64) NOT NULL,\n  tenant_id VARCHAR(64) NOT NULL,\n  strategy_type VARCHAR(32) NOT NULL,\n  is_enabled TINYINT(1) NOT NULL DEFAULT 1,\n  strategy_params JSON NOT NULL,\n  severity_level_id VARCHAR(64) NULL,\n  severity_code VARCHAR(16) NULL,\n  severity_name VARCHAR(32) NULL,\n  severity_color VARCHAR(16) NULL,\n  sort_order_snapshot INT NULL,\n  margin_call_severity_level_id VARCHAR(64) NULL,\n  close_out_severity_level_id VARCHAR(64) NULL,\n  notify_channels JSON NULL,\n  notify_user_ids JSON NULL,\n  upgrade_days INT NOT NULL DEFAULT 0,\n  upgrade_user_ids JSON NULL,\n  migration_batch_id VARCHAR(64) NULL,\n  source_rule_ids JSON NULL,\n  version INT NOT NULL DEFAULT 1,\n  created_time DATETIME NOT NULL,\n  updated_time DATETIME NOT NULL,\n  PRIMARY KEY (id),\n  UNIQUE KEY uk_strategy_id (strategy_id),\n  UNIQUE KEY uk_rule_strategy_type (rule_id, strategy_type),\n  KEY idx_strategy_batch (migration_batch_id)\n) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;\n```\n\n`strategy_params` 的迁移结构示例：\n\n```json\n{\n  \"timeout_items\": [{\"business_key\": \"QR-001\", \"days\": 30}],\n  \"price_drop_ratio\": 0.10,\n  \"inspection_items\": [{\"user_id\": \"U-005\", \"cycle_days\": 7}],\n  \"margin_call_ltv\": 0.75,\n  \"close_out_ltv\": 0.85,\n  \"model_product_id\": \"MODEL-001\"\n}\n```\n\n迁移控制表必须先建好，保证异常和回滚范围可审计：\n\n```sql\nCREATE TABLE IF NOT EXISTS migration_batch (\n  migration_batch_id VARCHAR(64) NOT NULL,\n  module VARCHAR(32) NOT NULL,\n  source_version VARCHAR(16) NOT NULL,\n  status VARCHAR(24) NOT NULL,\n  started_at DATETIME NOT NULL,\n  finished_at DATETIME NULL,\n  PRIMARY KEY (migration_batch_id)\n);\n\nCREATE TABLE IF NOT EXISTS migration_exception (\n  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,\n  migration_batch_id VARCHAR(64) NOT NULL,\n  tenant_id VARCHAR(64) NULL,\n  source_table VARCHAR(64) NOT NULL,\n  source_id VARCHAR(64) NULL,\n  exception_type VARCHAR(64) NOT NULL,\n  detail JSON NOT NULL,\n  resolution_status VARCHAR(24) NOT NULL DEFAULT 'OPEN',\n  resolved_by VARCHAR(64) NULL,\n  resolved_at DATETIME NULL,\n  PRIMARY KEY (id),\n  KEY idx_exception_batch (migration_batch_id, resolution_status)\n);\n```\n\n### 2.2 历史台账字段补充\n\n`collateral_warn_ledger` 必须至少具备：`warn_id`、租户/订单快照、`rule_id`、`strategy_type`、`severity_level_id`、`severity_code`、`severity_name`、`severity_color`、`sort_order_snapshot`、`warn_source`、`legacy_warn_type`、`related_device_event_id`、`trigger_snapshot`、`process_status`、`fuse_status`、`legacy_fuse_status`、`publicity_status`、`event_source`、`migration_batch_id`。\n\n历史流水若无法解析等级 ID，不得伪造一个 `L3`。应保留旧的名称/颜色/编码快照，写入 `migration_exception` 并将 `severity_resolution_status=UNRESOLVED`，由业务确认后补齐 ID；不能阻断历史台账可追溯性。\n\n## 三、源类型与等级映射\n\n### 3.1 11 类源类型到策略子项\n\n| 6.1 源类型 | 6.2 处理 | 目标 `strategy_type` / `warn_source` |\n| :--- | :--- | :--- |\n| 解抵/质押/监管超时 | 保留并聚合 | `TIMEOUT` / `ORDER_CONFIG` |\n| 价格下跌 | 保留并聚合 | `PRICE_DROP` / `ORDER_CONFIG` |\n| 盘点异常 | 保留并聚合 | `INVENTORY_CHECK` / `ORDER_CONFIG` |\n| 巡检异常 | 保留并聚合 | `INSPECTION` / `ORDER_CONFIG` |\n| 抵/质押率异常 | 保留并拆双阈值 | `LTV` / `ORDER_CONFIG` |\n| 贷中风控预警 | 保留并聚合 | `LOAN_RISK` / `ORDER_CONFIG` |\n| 图像识别、物联设备、智能挂锁、人脸门禁、GPS | 不进入有效商业策略 | `IOT_PENETRATION`（有设备事件）或 `LEGACY_IOT`（无设备事件） |\n\n### 3.2 等级解析优先级\n\n对每个配置子项和每条历史流水分别执行以下优先级：\n\n1. 源数据已有 `severity_level_id`：校验租户归属、启用/历史可引用状态后直接使用。\n2. 源数据只有旧编码：按同租户 `severity_code` 精确匹配。\n3. 源数据只有旧名称：按同租户名称匹配；多匹配或未匹配进入异常。\n4. 业务确认的租户级映射表：仅允许在迁移批次开始前配置，不允许 SQL 内写死 `L1~L5`。\n5. 仍无法匹配：写异常清单，规则子项不进入 `ACTIVE`，历史流水保留原快照并标记待补齐。\n\n## 四、分批迁移步骤\n\n### Step 0：创建批次与前置检查\n\n```sql\nINSERT INTO migration_batch (migration_batch_id, module, source_version, status, started_at)\nVALUES ('MIG-20260821-001', 'ORDER_WARNING', '6.1', 'RUNNING', NOW());\n\n-- 03/01 等级主键、租户隔离和最小启用档检查\nSELECT tenant_id, COUNT(*) AS enabled_cnt\nFROM tenant_severity_level\nWHERE is_deleted = 0 AND status = 'ENABLED'\nGROUP BY tenant_id\nHAVING enabled_cnt = 0;\n\n-- 目标表不得存在同批次残留；存在则先停止，不得重复插入\nSELECT COUNT(*) FROM order_rule_config WHERE migration_batch_id = 'MIG-20260821-001';\n```\n\n同时生成 `tenant_legacy_severity_map`，记录每个租户的旧编码/名称到 `severity_level_id` 的显式映射；映射不完整时批次进入 `BLOCKED`，不自动降级为固定等级。\n\n### Step 1：源数据规范化与异常分流\n\n把旧表转换到临时表 `stg_order_warn_config`：统一租户、订单类型、源类型、规则名称、原始等级、参数 JSON、通知 JSON 和来源配置 ID。以下情况写 `migration_exception`，不直接写目标有效数据：租户/订单不存在、源类型无法识别、同一订单类型冲突、等级多匹配、LTV 双阈值缺一、监管订单带 LTV/贷中策略。\n\n### Step 2：按订单聚合生成综合规则\n\n```sql\nINSERT INTO order_rule_config (\n  rule_id, tenant_id, order_id, order_no, order_type, rule_name, status,\n  migration_batch_id, migration_status, source_rule_count, version,\n  created_by, created_time, updated_by, updated_time\n)\nSELECT\n  CONCAT('MIG-', :batch_id, '-', SHA2(CONCAT(tenant_id, ':', order_id), 256)),\n  tenant_id, order_id, MAX(order_no), MAX(order_type),\n  LEFT(MAX(rule_name), 50),\n  CASE WHEN SUM(CASE WHEN migration_status = 'READY' THEN 1 ELSE 0 END) > 0\n       THEN 'ACTIVE' ELSE 'INACTIVE' END,\n  :batch_id,\n  CASE WHEN SUM(CASE WHEN migration_status <> 'READY' THEN 1 ELSE 0 END) = 0\n       THEN 'MIGRATED' ELSE 'PENDING_REVIEW' END,\n  COUNT(*), 1, 'MIGRATION', MIN(created_time), 'MIGRATION', MAX(updated_time)\nFROM stg_order_warn_config\nGROUP BY tenant_id, order_id;\n```\n\n`PENDING_REVIEW` 主规则不得被引擎监听；仅当该订单所有待迁移子项完成等级和业务约束校验后，才由批次收尾任务置为 `ACTIVE`。\n\n### Step 3：按类型生成策略子项\n\n```sql\nINSERT INTO order_rule_strategy (\n  strategy_id, rule_id, tenant_id, strategy_type, is_enabled, strategy_params,\n  severity_level_id, severity_code, severity_name, severity_color,\n  sort_order_snapshot, margin_call_severity_level_id,\n  close_out_severity_level_id, notify_channels, notify_user_ids,\n  upgrade_days, upgrade_user_ids, migration_batch_id, source_rule_ids,\n  version, created_time, updated_time\n)\nSELECT\n  CONCAT('MIG-ST-', :batch_id, '-', SHA2(CONCAT(s.tenant_id, ':', s.order_id, ':', s.strategy_type), 256)),\n  r.rule_id, s.tenant_id, s.strategy_type, 1, s.strategy_params,\n  CASE WHEN s.strategy_type <> 'LTV' THEN lv.severity_level_id END,\n  CASE WHEN s.strategy_type <> 'LTV' THEN lv.severity_code END,\n  CASE WHEN s.strategy_type <> 'LTV' THEN lv.severity_name END,\n  CASE WHEN s.strategy_type <> 'LTV' THEN lv.severity_color END,\n  CASE WHEN s.strategy_type <> 'LTV' THEN lv.sort_order END,\n  CASE WHEN s.strategy_type = 'LTV' THEN margin_lv.severity_level_id END,\n  CASE WHEN s.strategy_type = 'LTV' THEN close_lv.severity_level_id END,\n  s.notify_channels, s.notify_user_ids, s.upgrade_days, s.upgrade_user_ids,\n  :batch_id, s.source_rule_ids, 1, MIN(s.created_time), MAX(s.updated_time)\nFROM stg_order_warn_strategy s\nJOIN order_rule_config r ON r.tenant_id = s.tenant_id AND r.order_id = s.order_id\nLEFT JOIN tenant_severity_level lv ON lv.severity_level_id = s.severity_level_id\nLEFT JOIN tenant_severity_level margin_lv ON margin_lv.severity_level_id = s.margin_call_severity_level_id\nLEFT JOIN tenant_severity_level close_lv ON close_lv.severity_level_id = s.close_out_severity_level_id\nWHERE s.migration_status = 'READY'\nGROUP BY s.tenant_id, s.order_id, s.strategy_type;\n```\n\n实际实现可按数据库方言拆成多条语句，但必须保持 `rule_id + strategy_type` 唯一以及主表/子项同批次可追溯。\n\n### Step 4：迁移押品预警流水\n\n- 商业六类：`warn_source=ORDER_CONFIG`，通过历史规则 ID 或订单+策略类型回填综合规则与子项；等级取触发时原始等级映射，不取当前规则等级覆盖历史。\n- 有 `related_device_event_id` 的五类 IoT：`warn_type=IOT_PENETRATION`、`warn_source=IOT_PENETRATION`。\n- 无设备事件关联的五类 IoT：`warn_type=LEGACY_IOT`、`warn_source=LEGACY_IOT`，保留 `legacy_warn_type`，只读展示。\n- 所有历史流水 `event_source=HISTORY_MIGRATION`、`migration_batch_id=:batch_id`；`fuse_status=NONE`，原值写 `legacy_fuse_status`。\n- 已处理/未处理/无效状态按旧处置状态一一映射；不得因规则删除或历史熔断值改变处置状态。\n\n### Step 5：批次收尾与启用\n\n1. 校验每条 `ACTIVE` 主规则至少有一条 `is_enabled=1` 子项。\n2. 校验监管订单没有 `LTV`/`LOAN_RISK` 子项。\n3. 校验每个启用子项等级 ID 均属于对应租户；LTV 两个等级均存在且 `close_out.sort_order >= margin_call.sort_order`。\n4. 校验主规则唯一性、子项唯一性、通知对象可用性和升级字段完整性。\n5. 所有校验通过后将批次置 `COMPLETED`；异常订单保持 `PENDING_REVIEW`，输出人工处理清单。\n\n## 五、对账与验收 SQL\n\n```sql\n-- 1. 源配置按订单聚合数与目标主规则数\nSELECT COUNT(*) FROM (\n  SELECT tenant_id, order_id FROM t_order_warn_cfg\n  WHERE is_deleted = 0 GROUP BY tenant_id, order_id\n) x;\nSELECT COUNT(*) FROM order_rule_config WHERE migration_batch_id = :batch_id;\n\n-- 2. 主规则下至少一条策略，且同订单不超过一条 ACTIVE\nSELECT r.rule_id\nFROM order_rule_config r\nLEFT JOIN order_rule_strategy s ON s.rule_id = r.rule_id AND s.is_enabled = 1\nWHERE r.migration_batch_id = :batch_id AND r.status = 'ACTIVE'\nGROUP BY r.rule_id HAVING COUNT(s.strategy_id) = 0;\n\nSELECT tenant_id, order_id, COUNT(*) AS active_cnt\nFROM order_rule_config\nWHERE migration_batch_id = :batch_id AND status = 'ACTIVE' AND is_deleted = 0\nGROUP BY tenant_id, order_id HAVING active_cnt > 1;\n\n-- 3. 等级 ID 租户归属和 LTV 严重度校验\nSELECT s.strategy_id\nFROM order_rule_strategy s\nLEFT JOIN tenant_severity_level lv ON lv.severity_level_id = COALESCE(s.severity_level_id, s.close_out_severity_level_id)\nWHERE s.migration_batch_id = :batch_id\n  AND (lv.severity_level_id IS NULL OR lv.tenant_id <> s.tenant_id);\n\nSELECT s.strategy_id\nFROM order_rule_strategy s\nJOIN tenant_severity_level m ON m.severity_level_id = s.margin_call_severity_level_id\nJOIN tenant_severity_level c ON c.severity_level_id = s.close_out_severity_level_id\nWHERE s.strategy_type = 'LTV' AND c.sort_order < m.sort_order;\n\n-- 4. 熔断与异常完整性\nSELECT COUNT(*) AS non_none_fuse\nFROM collateral_warn_ledger\nWHERE migration_batch_id = :batch_id AND fuse_status <> 'NONE';\nSELECT exception_type, COUNT(*) FROM migration_exception\nWHERE migration_batch_id = :batch_id GROUP BY exception_type;\n```\n\n验收要求：数量可解释、无重复主规则、无跨租户等级引用、无非法策略组合、无非 `NONE` 运行时熔断、每个异常均有责任人和处理结论。\n\n## 六、回滚与重跑\n\n回滚必须先将批次置 `ROLLBACK_RUNNING`，停止该批次规则监听和补偿任务，再按外键顺序删除：流水 → 策略子项 → 主规则 → 归档/异常记录；全部语句带 `migration_batch_id=:batch_id`，并在事务或可恢复分片中执行。\n\n```sql\nSTART TRANSACTION;\nDELETE FROM collateral_warn_ledger WHERE migration_batch_id = :batch_id;\nDELETE FROM order_rule_strategy WHERE migration_batch_id = :batch_id;\nDELETE FROM order_rule_config WHERE migration_batch_id = :batch_id;\nDELETE FROM order_rule_config_migration_archive WHERE migration_batch_id = :batch_id;\nDELETE FROM migration_exception WHERE migration_batch_id = :batch_id;\nUPDATE migration_batch SET status = 'ROLLED_BACK', finished_at = NOW()\nWHERE migration_batch_id = :batch_id;\nCOMMIT;\n```\n\n禁止 `TRUNCATE`、禁止按 `status='ACTIVE'` 批量删除、禁止删除上线后人工新建且不属于本批次的数据。重跑必须使用新批次号，不能复用已完成或已回滚批次号。\n\n## 七、执行顺序与责任边界\n\n| 顺序 | 步骤 | 责任 | 通过条件 |\n| :---: | :--- | :--- | :--- |\n| 1 | 03/01 等级与租户映射 | 配置管理员/数据管理员 | 每个租户映射完整，ID 可解析 |\n| 2 | 05 IoT 事件关联准备 | IoT 服务 | 事件 ID 可对账，无法关联的进入历史标签 |\n| 3 | 06 源数据清洗与异常分流 | 订单服务/数据管理员 | 无未解释的源类型、等级、订单冲突 |\n| 4 | 主表+子项迁移 | 订单服务 | 一订单一综合规则，策略子项唯一 |\n| 5 | 押品流水迁移 | 风险信息服务 | 历史快照保留，运行时熔断全为 `NONE` |\n| 6 | 对账、抽样、回放 | 测试/风控 | 通过第五章全部校验和跨域用例 |\n| 7 | 切换与观察 | 发布负责人 | 批次完成，异常清单闭环 |\n\n## 修订记录\n\n| 日期 | 版本 | 变更 |\n| :--- | :---: | :--- |\n| 2026-08-21 | **V2.0** | 重做迁移方案：按订单聚合综合规则、拆策略子项、动态等级 ID 映射、LTV 双等级、批次化回滚、异常隔离与熔断审计 |\n| 2026-08-20 | V1.1 | 废弃：旧版单 `warn_type + severity_level` 迁移模型 |\n"
  },
  {
    "id": "access-and-event-notice-mapping",
    "title": "03. 门禁事务与设备事务通知兼容映射",
    "shortTitle": "门禁与设备事务通知兼容映射",
    "category": "业务兼容映射",
    "filePath": "03业务兼容映射/6.1→6.2 门禁事务与设备事务通知兼容映射.md",
    "badge": "业务映射",
    "routePath": "/历史迁移与割接/门禁与设备事务通知兼容映射",
    "version": "V2.0",
    "author": "AI PM 工作流",
    "updatedAt": "2026-08-25",
    "description": "15种门禁事务枚举归流、设备事务通知合入设备预警信息与配置的对照矩阵",
    "content": "# 6.1→6.2 门禁事务与设备事务通知兼容映射\n\n> **文档定位**：说明 6.1 基准版中【门禁事务记录】【设备事务通知信息】【设备事务通知配置】三个模块，在 6.2 迭代中的归属、菜单变化、业务规则继承、字段映射与历史数据迁移口径。供产品、研发、测试与实施团队统一理解「旧模块去哪了、能力是否保留、怎么用新菜单操作」。\n>\n> **版本**：V1.0 | **编制日期**：2026-08-25\n>\n> **上级索引**：[历史迁移总索引](../历史迁移总索引.md) · **一页速览**：[三旧模块兼容总说明](../6.1→6.2 三旧模块兼容总说明.md)\n>\n> **关联文档**：\n> - [6.2 版本总览 README](../../README.md) 第三节「历史基准 vs 6.2 迭代对照」\n> - [设备预警信息主 PRD](../../02-预警信息/01设备预警信息/设备预警信息主PRD.md) · [业务规则规格](../../02-预警信息/01设备预警信息/设备预警信息业务规则规格.md)\n> - [设备预警配置主 PRD](../../03-预警配置/02设备预警配置/设备预警配置主PRD.md)\n> - [03 历史数据迁移与回滚技术方案](../01设备侧规则与流水迁移/03历史数据迁移与回滚技术方案.md)\n> - [00-全局契约与 RBAC 权限矩阵](../../00-全局契约与RBAC权限矩阵.md)\n\n---\n\n## 一、结论摘要\n\n6.2 对三个旧模块采取 **「菜单取消 + 数据合流 + 规则统一」** 策略，不是三套系统并行运行。\n\n| 6.1 原模块 | 6.2 新归属 | 一句话说明 |\n| :--- | :--- | :--- |\n| **门禁事务记录** | `02-预警信息 / 01设备预警信息` | 独立菜单取消；15 种门禁事务流水合入 `iot_event_ledger`，预警类型为 `常规通行与操作事务` |\n| **设备事务通知信息** | `02-预警信息 / 01设备预警信息` | 与设备预警、门禁事务合并为同一套事件流水台账 |\n| **设备事务通知配置** | `03-预警配置 / 02设备预警配置` | 与设备预警配置合并为 `iot_rule_config` 统一规则表 |\n\n**底层两张表承接全部能力：**\n\n- **配置层**：`iot_rule_config`（设备预警配置）\n- **流水层**：`iot_event_ledger`（设备预警信息）\n\n```mermaid\nflowchart TB\n    subgraph old[\"6.1 三套独立模块\"]\n        O1[\"门禁事务记录\"]\n        O2[\"设备事务通知信息\"]\n        O3[\"设备事务通知配置\"]\n        O4[\"设备预警信息\"]\n        O5[\"设备预警配置\"]\n    end\n\n    subgraph new[\"6.2 统一 IoT 预警体系\"]\n        R[\"iot_rule_config<br/>设备预警配置\"]\n        E[\"iot_event_ledger<br/>设备预警信息\"]\n    end\n\n    O1 --> E\n    O2 --> E\n    O4 --> E\n    O3 --> R\n    O5 --> R\n    R -->|\"规则命中\"| E\n\n    TAB[\"门禁设备·设备数据→事务记录 Tab\"] -->|\"只读过滤\"| E\n```\n\n---\n\n## 二、模块归属总览\n\n### 2.1 菜单路径对照\n\n| 6.1 菜单路径 | 6.2 菜单路径 | 变化类型 |\n| :--- | :--- | :--- |\n| 仓储 / 设备管理 / **门禁事务记录** | — | **取消独立菜单** |\n| 风控 / 风险信息 / **设备事务通知信息** | — | **取消独立菜单** |\n| 配置管理 / 风险管理 / **设备事务通知配置** | — | **取消独立菜单** |\n| 风控 / 风险信息 / 设备预警信息 | 物联网 IoT / **设备预警信息** | **合并承接**（含门禁 + 事务 + 预警） |\n| 配置管理 / 风险管理 / 设备预警配置 | 物联网 IoT / **设备预警配置** | **合并承接**（含事务通知规则） |\n\n### 2.2 保留的查询入口\n\n| 入口 | 6.2 状态 | 数据源 | 说明 |\n| :--- | :--- | :--- | :--- |\n| 门禁设备列表 → **设备数据 → 事务记录 Tab** | ✅ 保留 | `iot_event_ledger` | 按当前设备编码只读过滤；字段与全量列表一致 |\n| 设备预警信息列表（全量筛选） | ✅ 新增统一入口 | `iot_event_ledger` | 替代原「门禁事务记录」独立菜单的全量检索 |\n| 移动端独立「门禁事务记录」菜单 | ❌ 取消 | — | 改由设备预警信息移动端列表承接 |\n\n---\n\n## 三、【门禁事务记录】兼容说明\n\n### 3.1 定位变化\n\n| 维度 | 6.1 | 6.2 |\n| :--- | :--- | :--- |\n| 模块定位 | 独立的只读通行日志模块 | 设备预警信息中的第 6 大类流水 |\n| 预警类型 | 无（独立「事务名称」枚举） | `常规通行与操作事务`（及部分归入挂锁/人脸预警大类） |\n| 数据表 | 门禁事务独立表（如 `t_door_access_log`） | `iot_event_ledger` |\n| 是否可配置 | 不可配置，100% 只读 | 大类不可在配置端新建；异常类可走设备预警配置 |\n\n### 3.2 15 种事务类型映射（规则 R05）\n\n原 6.1 的 15 种门禁事务名称**全部保留**，合流至 6.2 预警大类如下：\n\n| 序号 | 6.1 事务名称 | 6.2 预警大类 | 事件性质 | 6.2 默认处置 |\n| :---: | :--- | :--- | :---: | :--- |\n| 1 | 获取密码成功 | 常规通行与操作事务 | 瞬态 | R06 已处理（有效） |\n| 2 | 获取密码失败 | 人脸门禁预警 | 瞬态 | 按规则；异常可未处理 |\n| 3 | 密码认证成功 | 常规通行与操作事务 | 瞬态 | R06 已处理（有效） |\n| 4 | 密码认证失败 | 人脸门禁预警 | 瞬态 | 按规则 |\n| 5 | 人脸认证成功 | 常规通行与操作事务 | 瞬态 | R06 已处理（有效） |\n| 6 | 人脸认证失败 | 人脸门禁预警 | 瞬态 | 按规则 |\n| 7 | 蓝牙开锁成功 | 智能挂锁预警 / 常规通行与操作事务 | 瞬态 | R06 已处理（有效） |\n| 8 | 蓝牙关锁成功 | 常规通行与操作事务 | 瞬态 | R06 已处理（有效） |\n| 9 | 远程开锁成功 | 常规通行与操作事务 | 瞬态 | R06 已处理（有效） |\n| 10 | 远程关锁成功 | 常规通行与操作事务 | 瞬态 | R06 已处理（有效） |\n| 11 | 门打开超时 | 人脸门禁预警 | 持续 | 未处理 → 人工/自动解除 |\n| 12 | 非法开箱 | 智能挂锁预警 | 瞬态 | 未处理 → 人工解除 |\n| 13 | 拆壳报警 | 智能挂锁预警 | 瞬态 | 未处理 → 人工解除 |\n| 14 | 剪杆破坏 | 智能挂锁预警 | 瞬态 | 未处理 → 人工解除 |\n| 15 | 撬锁报警 | 智能挂锁预警 | 瞬态 | 未处理 → 人工解除 |\n\n### 3.3 继承的业务规则\n\n| 规则 ID | 6.1 能力 | 6.2 兼容方式 |\n| :--- | :--- | :--- |\n| **R05** | 15 种事务类型枚举 | 子类型名称沿用 6.1，映射至 6 大类 |\n| **R06** | — | 正常通行/开关锁等瞬态事件：防抖=0，直接 `已处理（有效）`，处理人=`系统自动处理` |\n| **R07** | 挂锁开锁人员动态追溯 | 完全继承：关联最近一次及同密码「获取密码成功」操作人 |\n| **R08** | 监控联动抓拍 | 完全继承；图片走 R13 时效签名 |\n| **R09** | 双入口共用数据集 | 设备数据 Tab 与设备预警信息列表共用 `iot_event_ledger` |\n| **R10** | 历史数据 | `t_door_access_log` 迁入时保留子类型名称与抓拍 Key |\n\n### 3.4 字段映射（门禁事务 → 设备预警信息）\n\n| 6.1 门禁事务字段 | 6.2 设备预警信息字段 | 映射说明 |\n| :--- | :--- | :--- |\n| 事务名称 | 预警内容（子类型部分） | 15 种名称作为子类型展示 |\n| 说明 | 预警内容（触发内容部分） | 获取密码事由/备注等拼装逻辑保留 |\n| 人员 | 预警内容（人员部分） | R07 追溯算法不变 |\n| 设备抓拍图 | 预警抓拍图 | 时效签名机制 |\n| 时间 | 首次预警时间 / 最近预警时间 | 瞬态事件两者相同 |\n| 系统内名称、设备编码、绑定仓库等 | 设备与位置快照 | 触发时固化，设备移除后仍可见 |\n| — | 预警类型 | 新增：`常规通行与操作事务` 等大类 |\n| — | 预警等级 | 新增：默认 L1；异常类按规则快照 |\n| — | 预警状态 | 新增：瞬态默认 `已处理（有效）` |\n| — | 预警次数 | 新增：瞬态固定 Count=1 |\n\n---\n\n## 四、【设备事务通知信息】兼容说明\n\n### 4.1 定位变化\n\n| 维度 | 6.1 | 6.2 |\n| :--- | :--- | :--- |\n| 模块定位 | 瞬时事务消息台账（非风险预警） | 设备预警信息统一流水的一部分 |\n| 事务类型 | 4 类：开锁/关锁/上线/移除 | 映射为设备预警配置附录 A 子类型 |\n| 状态机 | 未处理 / 已处理 | 未处理（有效/无效）/ 已处理（有效） |\n| 人工处理 | 所有未处理事务均可填写情况说明 | 仅风险类子类型展示解除入口（R14） |\n| 升级通知 | 无 | 仅持续型风险告警支持（C05） |\n| 频次聚合 | 不合流 | 持续型未处理事件支持 Count 聚合（C01） |\n\n### 4.2 四类事务类型映射\n\n| 6.1 事务类型 | 6.2 预警大类 | 6.2 子类型 | 配置入口 |\n| :--- | :--- | :--- | :--- |\n| **开锁通知** | 智能挂锁预警 | `正常开关锁事务` | 设备预警配置 |\n| **关锁通知** | 智能挂锁预警 | `正常开关锁事务` | 设备预警配置 |\n| **设备上线** | 设备图像识别预警 | `监控设备上线` | 设备预警配置（可勾选「仅针对新设备」） |\n| **设备移除** | — | 附录 A 无对应子类型 | 见 §4.4 特殊说明 |\n\n### 4.3 行为差异对照\n\n| 场景 | 6.1 行为 | 6.2 行为 |\n| :--- | :--- | :--- |\n| 正常开锁/关锁 | 生成事务通知，默认未处理，可人工核查 | R06 瞬态落账，直接 `已处理（有效）`，无解除按钮 |\n| 设备上线 | 即时通知 + 人工处理可选 | 瞬态落账 + 按规则推送；全局新设备规则保留（R05/R13） |\n| 通知推送 | 命中事务通知配置即发 | 仍按规则快照推送；L2+ 且启用渠道时可发短信 |\n| 规则删除后历史 | 历史通知保留 | 历史流水保留；未处理流水可置 `未处理（无效）` |\n| 设备移除后历史 | 历史通知保留 | 历史流水保留；设备快照仍可见 |\n\n### 4.4 「设备移除通知」特殊说明\n\n6.2 设备预警配置附录 A **未单独定义「设备移除」子类型**。兼容策略如下：\n\n1. **迁移期**：原「设备移除」事务通知规则若无法映射到合法子类型，写入 `migration_exception`，以 `legacy_sub_type` 保留展示快照。\n2. **运行时**：设备移除的联动（规则解绑、未处理预警置无效、订单解绑等）由 **IoT 设备管理「移除设备」** 流程承担，不再单独生成「设备移除通知」流水。\n3. **历史数据**：已产生的设备移除通知记录在迁移时 1:1 迁入 `iot_event_ledger`，不影响审计追溯。\n\n### 4.5 字段映射（事务通知信息 → 设备预警信息）\n\n| 6.1 事务通知字段 | 6.2 设备预警信息字段 | 映射说明 |\n| :--- | :--- | :--- |\n| 事务规则名称 | 规则名称 | 触发时规则快照 |\n| 事务类型 | 预警类型 + 子类型 | 4 类映射为附录 A 子类型 |\n| 事务内容 | 预警内容 | 位置+设备+触发内容格式保留 |\n| 抓拍图 | 预警抓拍图 | 时效签名 |\n| 创建时间 | 首次预警时间 | 业务含义=事件触发时间 |\n| 处理时间 | 处理时间 | 瞬态事件=触发时间（系统自动处理） |\n| 处理人 | 处理人 | 瞬态=`系统自动处理`；风险类=人工解除人 |\n| 状态（未处理/已处理） | 预警状态 | 扩展为 未处理（有效/无效）/ 已处理（有效） |\n| 情况说明 | 情况说明 | 仅人工解除类型展示 |\n| 现场照片 | 现场照片 | 仅人工解除类型 |\n| 事务处理抓拍图 | 解除预警抓拍图 | 仅人工解除提交时触发 |\n| 事务通知信息唯一标识 | 事件唯一标识 (Event ID) | 主键口径统一 |\n| 来源事件唯一标识 | 幂等键（系统字段） | 去重逻辑保留 |\n| 通知发送结果 | 操作审计日志（部分） | 发送结果写入审计，不回滚流水 |\n\n---\n\n## 五、【设备事务通知配置】兼容说明\n\n### 5.1 定位变化\n\n| 维度 | 6.1 | 6.2 |\n| :--- | :--- | :--- |\n| 模块定位 | 瞬时事务通知规则（启用/停用） | 设备预警配置统一规则的一部分 |\n| 数据表 | `t_dev_event_cfg` | `iot_rule_config`（与 `t_dev_warn_cfg` 合流） |\n| 事务类型 | 4 类固定枚举 | 5 大类 + 附录 A 权威子类型 |\n| 等级体系 | 无独立等级 | 绑定 `03-预警配置/01预警等级`（2~20 档） |\n| 状态 | 启用 / 停用 | 生效中 / 停用 / 已失效 / 已删除 |\n| 升级策略 | 无 | 持续型可配；瞬态（上线/开关锁）隐藏升级区（R13） |\n\n### 5.2 配置项映射\n\n| 6.1 配置字段/能力 | 6.2 对应项 | 说明 |\n| :--- | :--- | :--- |\n| 事务通知规则名称 | 规则名称 | 同一字段语义 |\n| 事务类型（开锁/关锁/上线/移除） | 预警类型 + 预警子类型 | 见 §4.2 映射表 |\n| 选择设备 | 关联设备 | N:M 多选；权限过滤逻辑保留 |\n| 针对新设备 | 仅针对新设备 | 仅 `监控设备上线` 子类型展示 |\n| 通知渠道（站内信/短信/邮件） | 通知渠道（短信/邮件） | 6.2 移除「站内信」可配置项；迁移时从 `notify_channels` 剔除「站内信」；**系统小角标**在预警命中时自动更新，不可配置 |\n| 通知对象 | 预警对象 | 字段语义一致 |\n| 启用/停用 | 生效中/停用 | 生命周期扩展为四态 |\n| — | 预警等级 | 新增：必选，来自 03/01 预警等级字典 |\n| — | 防抖判定模式 | 新增：瞬态锁定「立即触发」 |\n| — | 升级预警天数/对象 | 新增：仅持续型风险类可配 |\n\n### 5.3 原「一条主规则 + 多条事务明细」模型\n\n6.1 采用「主规则 + 事务类型明细」结构；6.2 迁移时按 `tenant_id + rule_name + device_category + sub_type` 去重合流为 `iot_rule_config` 单条规则记录，详见 [03 历史数据迁移方案 Step 1](../01设备侧规则与流水迁移/03历史数据迁移与回滚技术方案.md)。\n\n---\n\n## 六、权限与角色兼容\n\n| 6.1 权限场景 | 6.2 权限归属 | 说明 |\n| :--- | :--- | :--- |\n| 门禁事务记录 · 查看 | 设备预警信息 · 查看 | 菜单合并；仓库数据权限 P02 不变 |\n| 设备事务通知信息 · 查看/处理 | 设备预警信息 · 查看/解除 | 瞬态流水无解除；风险类需 IoT 处理权限 |\n| 设备事务通知配置 · 增删改 | 设备预警配置 · 增删改 | 统一 IoT 写权限 |\n| 未绑定仓库设备事务 | 具备菜单权限即可见 | 与 6.1 一致 |\n\n角色矩阵详见 [00-全局契约与 RBAC 权限矩阵](../../00-全局契约与RBAC权限矩阵.md) 第四章。\n\n---\n\n## 七、历史数据迁移口径\n\n> 完整 SQL 与批次化方案见 [03 历史数据迁移与回滚技术方案](../01设备侧规则与流水迁移/03历史数据迁移与回滚技术方案.md)。\n\n### 7.1 源表与目标表\n\n| 6.1 源表 | 6.2 目标表 | 迁移策略 |\n| :--- | :--- | :--- |\n| `t_dev_warn_cfg` | `iot_rule_config` | 与事务通知配置合流去重 |\n| `t_dev_event_cfg` | `iot_rule_config` | 按附录 A 合法子类型映射 |\n| `t_door_access_log` | `iot_event_ledger` | 保留子类型名称与抓拍 Key（R10） |\n| 设备预警流水 + 事务通知流水 | `iot_event_ledger` | 合流落账 |\n\n### 7.2 事件迁移分类处理\n\n| 事件性质 | 迁移方式 | 迁移后状态 |\n| :--- | :--- | :--- |\n| **瞬态**（开关锁、通行、上线等） | 1:1 逐条迁移 | `已处理（有效）`，Count=1 |\n| **持续型未处理** | 按 `tenant + device + sub_type` 聚合 | `未处理（有效）`，时间轴明细全保留 |\n| **已处理** | 按 event_id 1:1 迁移 | 保留原处理人、时间、材料 |\n\n### 7.3 迁移约束\n\n1. 迁移批次标记 `migration_batch_id`，支持幂等重跑与整批回滚。\n2. 历史迁移事件标记 `event_source=HISTORY_MIGRATION`，**不触发新通知、不触发订单穿透**。\n3. 无法映射的子类型写入 `migration_exception`，以 `legacy_sub_type` 保留，不得伪造默认等级。\n4. 图片仅保留 OSS Key，通过签名服务访问。\n\n---\n\n## 八、用户操作指引（6.2 新菜单怎么用）\n\n| 我要做什么 | 6.1 操作路径 | 6.2 操作路径 |\n| :--- | :--- | :--- |\n| 查某门禁设备的开关锁流水 | 门禁设备 → 设备数据 → 事务记录 | **不变**（数据源已切换为 `iot_event_ledger`） |\n| 全量检索门禁事务 | 设备管理 → 门禁事务记录 | 物联网 IoT → **设备预警信息**，筛选「预警类型 = 常规通行与操作事务」 |\n| 查看开锁/上线通知记录 | 风控 → 设备事务通知信息 | 物联网 IoT → **设备预警信息**，按子类型筛选 |\n| 配置开锁/关锁通知 | 配置管理 → 设备事务通知配置 | 物联网 IoT → **设备预警配置**，选子类型 `正常开关锁事务` |\n| 配置新设备上线通知 | 同上，选「设备上线」 | **设备预警配置**，选 `监控设备上线`，可勾选「仅针对新设备」 |\n| 处理物理破坏类告警 | 设备预警信息 → 解除 | **不变**（仍在设备预警信息） |\n| 核查日常通行记录 | 门禁事务记录（只读） | 设备预警信息 → 详情（状态默认「已处理」，无需操作） |\n\n---\n\n## 九、验收检查清单\n\n| # | 检查项 | 通过标准 |\n| :---: | :--- | :--- |\n| 1 | 门禁 15 种事务名称 | 迁移后子类型名称与 6.1 一致，可在详情页展示 |\n| 2 | 人员追溯 | 挂锁开锁成功仍展示关联获取密码操作人（R07） |\n| 3 | 抓拍联动 | 开关锁事件有关联抓拍图或「无抓拍图」状态 |\n| 4 | 双入口一致性 | 设备数据 Tab 与全量列表同一 event_id 数据一致（R09） |\n| 5 | 瞬态默认状态 | 正常开关锁/通行/上线迁移后为 `已处理（有效）` |\n| 6 | 事务通知规则 | 原开锁/关锁/上线规则可在设备预警配置中找到对应子类型 |\n| 7 | 历史不可篡改 | 设备移除后历史流水仍展示触发时设备/仓库快照 |\n| 8 | 迁移无副作用 | 历史迁移不触发新短信/系统小角标更新/订单穿透 |\n| 9 | 权限过滤 | 仓库数据权限、未绑定仓库可见规则与 6.1 一致 |\n| 10 | 未知子类型 | 「设备移除」等无法映射项进入异常清单，不静默丢弃 |\n\n---\n\n## 十、相关文档索引\n\n| 文档 | 路径 | 用途 |\n| :--- | :--- | :--- |\n| 6.2 版本总览 | [README.md](../../README.md) | 三大主干架构与历史对照 |\n| 设备预警信息主 PRD | [设备预警信息主PRD.md](../../02-预警信息/01设备预警信息/设备预警信息主PRD.md) | 合流后的功能范围与状态机 |\n| 设备预警信息业务规则 | [设备预警信息业务规则规格.md](../../02-预警信息/01设备预警信息/设备预警信息业务规则规格.md) | R05~R10 门禁合流规则唯一定义来源 |\n| 设备预警配置附录 A | [设备预警配置业务规则规格.md](../../03-预警配置/02设备预警配置/设备预警配置业务规则规格.md) | 合法子类型权威枚举 |\n| 历史数据迁移方案 | [03历史数据迁移与回滚技术方案.md](../01设备侧规则与流水迁移/03历史数据迁移与回滚技术方案.md) | SQL、批次、回滚 |\n| 6.1 基准：门禁事务 | [门禁事务主PRD.md](../../../../🌟🌟🌟-最新基准版/B端PC/02-仓储/20-设备管理-门禁事务记录/门禁事务主PRD.md) | 原模块完整规格 |\n| 6.1 基准：事务通知信息 | [设备事务通知信息主PRD.md](../../../../🌟🌟🌟-最新基准版/B端PC/05-风控/07-风险信息-设备事务通知信息/设备事务通知信息主PRD.md) | 原模块完整规格 |\n| 6.1 基准：事务通知配置 | [设备事务通知配置主PRD.md](../../../../🌟🌟🌟-最新基准版/B端PC/08-配置管理/21-风险管理-设备事务通知配置/设备事务通知配置主PRD.md) | 原模块完整规格 |\n\n---\n\n## 修订记录\n\n| 日期 | 版本 | 变更说明 |\n| :--- | :---: | :--- |\n| 2026-08-25 | **V1.0** | 初版：三模块归属、菜单、规则继承、字段映射、迁移口径与用户操作指引 |\n"
  },
  {
    "id": "cross-domain-data-deduction",
    "title": "04. 6.2 跨域用例数据推演",
    "shortTitle": "跨域用例数据推演",
    "category": "割接验收与推演",
    "filePath": "04割接验收与推演/6.2跨域用例数据推演.md",
    "badge": "测试/验收",
    "routePath": "/历史迁移与割接/跨域用例数据推演",
    "version": "V1.0",
    "author": "AI PM 工作流",
    "updatedAt": "2026-08-25",
    "description": "割接后 IoT、订单与押品风控端到端业务闭环跨域推演与验收用例",
    "content": "# 6.2 跨域用例数据推演：综合规则 + 动态等级 + 物联穿透\n\n> **版本**：V3.0 | **更新日期**：2026-08-21\n> **推演范围**：03/01 预警等级 → 03/03 订单综合规则 → 02/02 押品预警信息 ↔ 03/02 设备预警配置 → 02/01 设备预警信息\n> **上级索引**：[历史迁移总索引](../历史迁移总索引.md)\n>\n> **关联规范**：[00-全局契约与 RBAC 权限矩阵](../../00-全局契约与RBAC权限矩阵.md) · [订单预警与通知体系协同规范](../../02-预警信息/协同规范/通知模板.md)\n> **熔断口径**：Part A 是 6.2 本期可执行用例，`fuse_status` 恒 `NONE`；Part B 仅验证 OPT-FUSE-01 未来接口，不计入本期通过率。\n\n## Part A — 6.2 本期验收推演\n\n### A.1 预置数据\n\n#### 租户、用户与权限\n\n| 对象 | 取值 |\n| :--- | :--- |\n| 租户 | `TEN_SENYUN_001` |\n| 风控专员 | `U-RISK-001` / 张三；已启用；角色 `R-RISK-OFF`；订单范围含 `ORD-202608-001` |\n| 订单操作员 | `U-ORDER-001` / 李四；角色 `R-ORDER-OP`；有订单处理权限 |\n| IoT 运维员 | `U-IOT-001` / 王五；角色 `R-IOT-OPS`；有 `WH-A01` 仓库权限 |\n| 通知对象 | 订单规则子项的 `notify_user_ids=[U-RISK-001]`；不使用未配置的姓名 |\n\n#### 03/01 动态等级字典\n\n| `severity_level_id` | `severity_code` | 名称 | `sort_order` | 颜色 | `sync_to_order_warn` | 状态 |\n| :--- | :--- | :--- | :---: | :--- | :---: | :--- |\n| `SL-001` | `INFO` | 提示 | 1 | `#409EFF` | 否 | ENABLED |\n| `SL-002` | `NOTICE` | 关注 | 2 | `#E6A23C` | 否 | ENABLED |\n| `SL-003` | `RISK` | 严重风险 | 3 | `#F56C6C` | 是 | ENABLED |\n| `SL-004` | `CRITICAL` | 紧急危险 | 4 | `#C03639` | 是 | ENABLED |\n\n用例故意使用非 `L1~L5` 编码，验证下游按 ID 和 `sort_order` 工作，而不是按字符串解析等级。\n\n#### 订单与 03/03 综合规则\n\n| 对象 | 取值 |\n| :--- | :--- |\n| 订单 | `ORD-202608-001` / `PO202608-001`；类型 `PLEDGE`；状态 `PLEDGING` |\n| 仓库空间 | `WH-A01` / `RM-01` / `ZN-A` |\n| 货值/贷款 | 初始货值 1,000 万；贷款 700 万；初始 LTV 70% |\n| 综合规则 | `rule_id=OR-202608-001`；规则名称“001号订单综合预警”；`status=ACTIVE`；`version=1` |\n| 子项 1 | `strategy_id=ORS-001-LTV`；`strategy_type=LTV`；补仓线 75%→`SL-002`，平仓线 85%→`SL-003`；解除方式分别为“补仓/部分结清”和“平仓/部分结清” |\n| 子项 2 | `strategy_id=ORS-001-PRICE`；`strategy_type=PRICE_DROP`；下跌比例 5%；等级 `SL-002` |\n| 通知 | 两个子项 `notify_channels=[SITE]`；`upgrade_days=3`；升级对象 `[U-RISK-001]` |\n\n同一订单只有这一条综合规则；不得另建第二条“价格下跌规则”。\n\n#### 03/02 设备规则与事件\n\n| 对象 | 取值 |\n| :--- | :--- |\n| 设备规则 | `iot_rule_id=IOT-R-101`；类型“智能挂锁预警”；子类型“剪杆破坏”；规则状态 `ACTIVE`；等级引用 `SL-004` |\n| 设备 | `device_id=LOCK-8891`；绑定 `WH-A01/RM-01/ZN-A` |\n| 接收人 | 设备规则通知对象 `U-IOT-001`，已配置且账号启用 |\n\n“剪杆破坏”是 05 附录 A 的合法子类型；不使用旧系统未注册的挂锁撬锁编码。\n\n### A.2 T0 — 保存综合规则\n\n1. `POST /order-warning/rules` 传入一个 `rule_id` 语义的主对象和两个策略子项；等级字段只传 `SL-002`、`SL-003`，不传 `L3/L4` 字符串。\n2. 服务端校验 `tenant_id + order_id` 唯一、监管限制、LTV 平仓 `sort_order(3) >= 补仓 sort_order(2)`、通知对象和 Version。\n3. 写入主表、两个子项、审计日志；响应 `rule_id=OR-202608-001`、`version=1`。\n4. 预期：规则列表只出现 1 行，已启用预警项为“抵/质押率异常、价格下跌”。\n\n### A.3 T1 — 命中补仓线并生成商业预警\n\n| 步骤 | 输入/结果 |\n| :--- | :--- |\n| 货值变化 | 当前货值 920 万，LTV = 700 / 920 = 76.09%，严格大于 75% |\n| 幂等事件 | `event_id=VAL-202608-001`；幂等键=`TEN_SENYUN_001 + ORD-202608-001 + LTV + VAL-202608-001` |\n| 生成流水 | `warn_id=COL-202608-001`；`strategy_type=LTV`；命中线 `MARGIN_CALL`；`severity_level_id=SL-002`；快照 `sort_order=2`；来源 `ORDER_CONFIG` |\n| 状态/通知 | `PENDING_VALID`；系统小角标更新给张三；规则已配短信则另发；`fuse_status=NONE` |\n\n重复投递相同 `event_id` 只返回原 `warn_id`，不新增流水。\n\n### A.4 T2 — 命中平仓线并升级同一记录\n\n| 步骤 | 输入/结果 |\n| :--- | :--- |\n| 货值变化 | 当前货值 800 万，LTV = 87.50%，严格大于 85% |\n| 同记录升级 | `COL-202608-001` 保持不变；命中线改为 `CLOSE_OUT`；等级快照从 `SL-002/sort_order=2` 更新为 `SL-003/sort_order=3`；触发快照保留补仓命中历史 |\n| 订单动作 | `OUT-202608-001` 出库请求通过；6.2 不阻断出库 |\n\n### A.5 T3 — L4 物联事件穿透并按设备事件 ID 跳转\n\n| 步骤 | 输入/结果 |\n| :--- | :--- |\n| 设备事件 | `device_event_id=DEV-E-501`；`IOT-R-101` 命中“剪杆破坏”；等级 `SL-004`；02/01 写入 `PENDING_VALID` |\n| 03/01 穿透判断 | `SL-004.sync_to_order_warn=true`；空间匹配 `WH-A01/RM-01/ZN-A`；订单状态 `PLEDGING` |\n| 押品流水 | `warn_id=COL-P-001`；`warn_type=IOT_PENETRATION`；`related_device_event_id=DEV-E-501`；等级快照 `SL-004/CRITICAL/sort_order=4`；`fuse_status=NONE` |\n| 页面跳转 | 02/02 点击“查看设备事件”携带 `device_event_id`、`warn_id`、`return_route`；02/01 按 `device_event_id` 定位，不按设备编号或列表序号定位 |\n| 权限失败 | 若用户缺少 IoT 查看或仓库权限，返回 `403_CROSS_DOMAIN_SCOPE`，不跳转且不改变押品流水 |\n\n### A.6 T4 — 设备核销联动解除\n\n1. 王五在 02/01 提交 `DEV-E-501` 解除，填写处理说明并通过 IoT 处理权限校验；设备流水置 `PROCESSED`。\n2. 发布 `DeviceEventReleased(tenant_id, event_id=DEV-E-501, order_id, processed_by, processed_time)`。\n3. 02/02 消费事件以 `event_id` 幂等匹配 `COL-P-001`，置 `PROCESSED`，处理人=`SYSTEM`，记录关联事件 ID；重复消费不重复处理。\n4. `COL-202608-001` 是商业类 LTV 预警，保持未处理，不能被设备核销误解除。\n\n### A.7 T5 — 补仓后商业预警解除\n\n货值恢复为 970 万，LTV=72.16%，业务订单处理页提交补仓完成并返回 `warn_id=COL-202608-001`。订单服务完成幂等校验后将该流水置 `PROCESSED`；返回 02/02 时按 `warn_id` 重新查询，列表显示已处理。若用户只有预警查看权限而无项目单据权限，点击“去处理”应固定提示且不跳转。\n\n### A.8 边界用例\n\n| 编号 | 场景 | 预期 |\n| :--- | :--- | :--- |\n| EC-01 | `SL-003` 的 `sync_to_order_warn=false` | 仅 02/01 有设备流水，不生成 02/02 穿透流水 |\n| EC-02 | 同一 `DeviceEventReleased` 重复投递 | 02/02 仍只有 1 条 `COL-P-001`，处理接口幂等 |\n| EC-03 | API 直接解除 `COL-P-001` | 返回“不支持人工解除物联穿透告警”，状态不变；统一走设备事件联动解除契约 |\n| EC-04 | 订单已办结后设备事件到达 | 02/01 可保留设备事实；空间穿透不生成 02/02 押品流水，并记录拒绝原因 |\n| EC-05 | 03/03 编辑普通阈值/等级 | 仅影响后续触发；`COL-202608-001` 的历史快照不变 |\n| EC-06 | 关闭 `PRICE_DROP` 子项 | 主规则仍 `ACTIVE`；仅价格下跌未处理流水无效，LTV 流水不变 |\n| EC-07 | 同订单尝试新增第二条综合规则 | R04 阻断并引导编辑 `OR-202608-001` |\n| EC-08 | 抵/质押率平仓等级 `sort_order` 低于补仓等级 | 保存阻断；不比较 `severity_code` 字符串 |\n\n## Part B — OPT-FUSE-01 未来预览（不纳入 6.2 验收）\n\n只有业务中台锁定/解锁接口、幂等键、审计和独立版本上线后，才允许增加以下断言：\n\n| 时点 | 未来行为 |\n| :--- | :--- |\n| T2 | 按最高 `sort_order` 和业务策略写入 `OUTBOUND_BLOCK` 或 `FULL_BLOCK` |\n| T3 | 设备核销只触发风险重算，不直接解锁；由业务中台幂等解锁 |\n| T5 | 商业预警解除后重新计算订单锁定范围 |\n\nPart B 不得回写 Part A 的通过结果；6.2 页面和接口仍只展示/返回 `fuse_status=NONE`。\n\n## 验收结论模板\n\n| 检查项 | 通过条件 |\n| :--- | :--- |\n| 03/03 数据模型 | 一订单一综合规则；策略子项唯一；等级均为租户内 `severity_level_id` |\n| 动态等级 | 用例可使用 `INFO/NOTICE/RISK/CRITICAL` 等非 L 编码；比较只按 `sort_order` |\n| 商业闭环 | 补仓→平仓同记录升级→补仓解除，幂等且快照可追溯 |\n| 物联闭环 | 05 事件→空间穿透→06 流水→按事件 ID 跳转→05 核销→06 联动解除 |\n| 权限/路由 | 无权限不跳转；返回按 ID 重新查询；路由参数齐全 |\n| 熔断 | Part A 全部 `fuse_status=NONE`，无用户筛选、按钮或阻断断言 |\n"
  }
];

export const migrationDocumentMap = new Map<string, MigrationDocumentItem>(
  migrationDocuments.map((doc) => [doc.id, doc])
);

export const migrationDocumentByPathMap = new Map<string, MigrationDocumentItem>(
  migrationDocuments.map((doc) => [doc.routePath, doc])
);
