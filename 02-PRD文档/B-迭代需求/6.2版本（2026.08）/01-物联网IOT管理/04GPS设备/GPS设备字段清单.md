# GPS设备字段清单

> **文档定位**：GPS设备主数据和系统字段的唯一定义来源。行操作字段由对应操作子清单维护；实时定位、运动轨迹等结果字段由对应操作子清单维护；下游需求文档、原型和关联功能按字段所属层级引用，不重复定义字段规则。
>
> **引用规则**：下游需要引用GPS设备字段时，标注“继承自GPS设备”；需要保留设备历史信息时，标注“快照字段”。
>
> **修改原则**：字段定义变化只修改本文件，并同步更新版本和修订记录。
>
> **版本**：V1.4 | 更新时间：2026-08-14
>
> **页面范围**：当前版本涉及GPS设备筛选区、列表页、行操作、实时定位、运动轨迹及页面级三方平台入口；新增页、编辑页和设备详情页暂不纳入本字段清单。

## 0. 字段层级说明

本文件只维护 GPS 设备主数据字段和系统字段。“实时定位”和“运动轨迹”的查询、地图、详情及第三方返回字段统一归档到[GPS设备操作字段清单索引](./操作字段清单/操作字段清单索引.md)及其子清单；操作流程和规则见 GPS 设备主 PRD、业务规则规格。

---

## 一、业务字段

> 本节用于定义01-物联网IOT管理/GPS设备页面的筛选字段和列表字段。当前字段包括序号、设备原名称、系统内名称、设备编码、车辆信息、设备状态、绑定仓库、具体位置、修改人员和更新时间；当前不定义“绑定状态”字段及对应筛选项。

### 1. 基础识别字段

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 序号 | 系统生成 | 整数；按当前列表页排序从1开始 | 系统自动 | 不适用 | 不涉及 | 是 | 否 | 不适用 | 当前列表结果的行序号 | 仅为页面展示序号，不作为设备持久化主键；固定为列表第一列，翻页后按当前页重新计算 |
| 设备原名称 | 设备接入/三方平台 | 文本；三方平台同时返回设备名称 | 系统自动 | 不适用 | 不涉及 | 是 | 否 | 不适用 | 设备在三方平台中的原始展示名称 | 三方平台同时返回设备名称 |
| 系统内名称 | 物联网IOT管理配置/系统带出/用户维护 | 文本；首次默认取设备原名称；用户重命名后以保存的系统内名称为准 | 系统自动或用户维护 | 不适用 | 不涉及 | 是 | 是（通过“设备名称”；模糊匹配） | 不适用 | SYZC 内部使用的设备名称 | 页面“设备名称”筛选实际查询本字段；三方名称变化不得覆盖已人工维护的系统内名称；最多50个字，唯一性和允许字符按重命名规则校验 |
| 设备编码 | 设备接入/系统带出 | 文本；编码格式、长度和唯一性待确认 | 系统自动 | 不适用 | 不涉及 | 是 | 否 | 不适用 | 设备的唯一识别编码 | 当前页面未确认设备编码生成方；设备维护操作应使用设备编码作为幂等关联键，禁止重复绑定 |
| 车辆信息 | 设备接入/系统带出 | 三方平台返回的车牌号 | 系统自动 | 不适用 | 不涉及 | 是 | 是（输入框；模糊匹配） | 不适用 | GPS设备关联的车辆信息 | 当前资料仅明确展示车牌号；车辆信息字段结构、车牌号格式和数据变更规则待确认 |
| 设备状态 | 三方平台实时返回 | 第三方返回状态枚举；原型当前展示离线、停车、行驶、静止及对应编码，但正式枚举、编码、判定来源和优先级待确认 | 系统自动 | 不适用 | 不涉及 | 否 | 是（下拉选择；精确匹配） | 不适用 | GPS设备当前定位状态 | 仅作为列表筛选项，不参与列表展示；实时定位和运动轨迹页面以三方正式状态口径为准，确认前不得固化编码 |
| 绑定仓库 | 用户选择/系统带出 | 下拉选择仓库；提交筛选时使用仓库唯一标识，列表展示仓库名称；无绑定时为空 | 选填 | 不适用 | 不涉及 | 是 | 是（下拉选择；精确） | 不适用 | 设备当前绑定的监管仓库 | 页面筛选标签为“仓库”；可选范围受租户、机构和仓库权限限制；绑定关系和权限校验以仓库唯一标识为准 |
| 具体位置 | 仓库档案/系统带出 | 文本；展示设备在绑定仓库中的安装或布设位置；位置层级和长度待确认 | 选填 | 不适用 | 不涉及 | 是 | 是（输入框；模糊匹配） | 不适用 | 设备在仓库内的具体位置 | 当前资料未确认位置是否来自仓库档案；无值时建议展示“未设置”，不使用空白造成歧义 |
| 修改人员 | 系统生成 | 最近一次更新设备信息的操作人姓名或账号 | 系统自动 | 不适用 | 不涉及 | 是 | 否 | 不适用 | 修改设备信息的最近一次操作人 | 以服务端认证身份为准，客户端不得直接写入 |
| 更新时间 | 系统生成 | 最近一次更新设备信息的时间；日期时间格式为 `YYYY-MM-DD HH:mm:ss` | 系统自动 | 不适用 | 不涉及 | 是 | 否 | 不适用 | 设备信息最近一次更新操作的时间 | 以服务端时间为准；绑定关系、名称或位置发生实际变更时更新，具体触发范围待确认 |

---

## 二、系统字段

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 可筛选 | 详情展示 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 创建人 | 系统生成 | 创建设备记录的用户姓名或账号 | 系统自动 | 不适用 | 不涉及 | 否 | 否 | 不适用 | 设备记录创建人 | 以服务端认证身份为准；客户端不可覆盖 |
| 创建时间 | 系统生成 | 设备记录创建时间；格式为 `YYYY-MM-DD HH:mm:ss` | 系统自动 | 不适用 | 不涉及 | 否 | 否 | 不适用 | 设备记录创建时间 | 以服务端时间为准；创建后不可修改 |
| 操作审计日志 | 系统生成 | 记录操作人、角色、时间、网络地址、请求标识、操作结果及前后数据版本 | 系统自动 | 不适用 | 不涉及 | 否 | 否 | 不适用 | GPS设备全路径操作审计信息 | 查询、绑定、解绑、修改名称或位置、状态变更、移除设备及失败请求均需留痕；敏感信息按角色脱敏 |

---

## 三、展示与引用规则

- 列表业务字段展示顺序：序号、设备原名称、系统内名称、设备编码、车辆信息、绑定仓库、具体位置、修改人员、更新时间。
- 筛选字段：设备名称（实际查询系统内名称）、车辆信息（模糊匹配）、设备状态（下拉精确匹配）、仓库（实际查询绑定仓库）、具体位置（模糊匹配）；其中“仓库”引用字段“绑定仓库”。
- GPS设备当前不定义“绑定状态”字段，也不提供“绑定状态”筛选项；设备是否绑定仅通过“绑定仓库”字段展示和筛选。
- “操作”属于列表交互列，不作为业务数据字段。当前行操作包括：重命名、绑定仓库、实时定位、运动轨迹、移除设备；字段见本文件“行操作字段索引”，流程和规则见对应主 PRD、业务规则规格。页面级按钮“GPS平台规则配置”和“GPS平台基础数据”属于独立的三方平台入口，不计入当前行操作。
- `设备原名称`、`系统内名称`、`设备编码` 和 `车辆信息` 的真实字段编码及接口来源待研发确认；字段编码未确认前，下游文档以中文字段名称引用，不自行固化英文属性名。
- GPS设备维护操作需进行租户、机构和菜单权限校验；涉及绑定关系、名称或位置更新时，应采用服务端版本校验、锁或等效并发控制，并将操作前后数据写入操作审计日志。
- 设备编码作为设备维护操作的幂等关联键；绑定、解绑、名称或位置更新不得仅使用列表行号作为请求关联条件。
- GPS平台规则配置和GPS平台基础数据通过联登跳转三方平台，按钮是否展示由对应的独立配置权限决定；三方平台内部页面、字段和操作规则不在本清单内定义。

---

## 四、行操作字段索引

> 按钮本身不是字段。操作的输入、查询、展示、返回和审计字段统一归档到对应子清单；操作流程和规则见 GPS 设备主 PRD、业务规则规格。

| 操作字段子清单 | 操作类型 | 说明 |
| :--- | :--- | :--- |
| [重命名字段清单](./操作字段清单/01重命名字段清单.md) | 编辑 | 继承设备通用重命名规则 |
| [仓库绑定字段清单](./操作字段清单/02仓库绑定字段清单.md) | 编辑 | 继承设备通用仓库绑定规则 |
| [实时定位字段清单](./操作字段清单/03实时定位字段清单.md) | 查询 | 维护地图、定位弹窗和页面状态字段 |
| [运动轨迹字段清单](./操作字段清单/04运动轨迹字段清单.md) | 查询 | 维护查询条件、轨迹分段和详情字段 |
| [移除设备字段清单](./操作字段清单/05移除设备字段清单.md) | 变更 | 维护移除说明、联动结果和操作结果 |
| GPS平台规则配置、GPS平台基础数据 | 页面级入口 | 三方平台入口上下文和联登规则见 GPS 设备主 PRD及业务规则，不纳入行操作字段子清单 |

## 修订记录

| 日期 | 变更内容 | 影响范围 |
| :--- | :--- | :--- |
| 2026-08-06 | 新增GPS设备字段清单，补充基础识别字段、系统字段、列表展示规则、筛选引用规则及操作审计要求 | GPS设备筛选区、列表字段、系统字段和数据维护约束 |
| 2026-08-06 | 明确“车辆信息”当前展示三方平台返回的车牌号，并保留字段结构、格式和变更规则待确认 | GPS设备车辆信息字段及三方平台数据接入 |
| 2026-08-06 | 新增行操作清单；重命名、绑定仓库、移除设备继承监控设备规则；实时定位和运动轨迹暂仅登记入口，具体逻辑待补充 | GPS设备列表行操作及关联操作规则 |
| 2026-08-06 | 根据实时定位页面原型补充地图、信息弹窗字段及状态持续时长展示规则；明确“系统内容”与“系统名称”的文案统一建议 | GPS设备实时定位页面展示和字段引用 |
| 2026-08-06 | 根据历史轨迹页面原型补充数据权限、设备和时间查询、31天查询限制、状态分段、轨迹详情字段及地图展示规则 | GPS设备运动轨迹页面、历史轨迹查询和轨迹详情 |
| 2026-08-06 | 新增“GPS平台规则配置”和“GPS平台基础数据”页面级外部入口，明确联登跳转及独立配置权限控制展示 | GPS设备页面级按钮、三方平台入口和权限配置 |
| 2026-08-06 | 对齐物联设备和监控设备公共字段口径；明确GPS设备不定义“绑定状态”字段及对应筛选项，并更新页面范围说明 | GPS设备公共字段、筛选规则和文档范围 |
| 2026-08-06 | 统一“更新时间”不可筛选，并与物联设备、监控设备字段清单统一展示行操作规则 | GPS设备筛选规则和行操作规则 |
| 2026-08-07 | 新增设备状态筛选；车辆信息和具体位置支持输入框模糊匹配，设备状态支持下拉精确匹配 | GPS设备筛选区和设备状态字段 |
| 2026-08-14 | V1.4 明确主字段清单只维护设备主数据和系统字段；实时定位、运动轨迹及移除设备的操作字段统一由各操作子清单维护 | 主字段清单与操作字段子清单职责边界 |
