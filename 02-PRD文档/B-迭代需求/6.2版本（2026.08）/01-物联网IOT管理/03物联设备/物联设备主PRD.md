# 物联设备主需求文档

> **版本**：V1.3 | 2026-08-14
> **读者**：研发工程师、测试工程师、产品复核
> **编写依据**：物联设备字段清单、监控设备主 PRD
> **字段基准**：[`物联设备字段清单.md`](./物联设备字段清单.md) 是设备主数据和系统字段的唯一基准；行操作的输入、查询、展示、返回及特有审计字段以对应操作子清单为准；实时数据和预警记录字段按所属操作或业务模块维护。

## 1. 业务背景

物联设备是仓储监管场景中的环境数据采集设备，支持温度、湿度、烟感、氧气、二氧化碳及多功能设备。系统负责展示设备基础信息、当前物联数据，并通过“设备数据”页面查看设备关联订单产生的温湿度预警记录。

物联设备列表不承载审批单据，也不建立独立设备业务状态机。重命名、仓库绑定和移除设备属于设备维护操作；设备数据属于查询和只读展示操作。

## 2. 功能范围

**本期范围**：

- 物联设备列表、组合筛选和字段展示；
- 重命名、仓库绑定、设备数据和移除设备四个行操作；
- 设备进入“设备数据”页面时获取一次当前设备实际支持并由第三方返回的温度、湿度、二氧化碳、氧气、烟感或多功能组合指标；
- 展示当前设备关联订单产生的温湿度预警记录及处理信息；
- 设备操作的权限校验、幂等处理、并发控制和审计记录。

### 2.1 移动端范围（已确认）

移动端支持物联设备列表与筛选、重命名、仓库绑定、设备数据和移除设备；操作审计查询不纳入本次移动端范围。

**不在本期范围**：

- 物联设备接入、注册和第三方设备基础档案创建；
- 温度、湿度历史曲线或持续实时刷新；
- 温湿度预警的处理、解除和处置；
- 订单审批、订单解绑等业务办理过程；
- 物联设备独立业务状态机。

## 3. 模块定位

| 项目 | 内容 |
| :--- | :--- |
| 模块层级 | 设备基础数据与物联数据查询层 |
| 核心职责 | 维护设备名称和仓库位置，展示设备采集数据及订单温湿度预警记录 |
| 数据来源 | 三方设备平台、系统设备配置、用户仓库绑定操作、订单风险预警模块 |
| 上游依赖 | **三方设备接入**提供设备 ID、设备编码、设备分类和实时采集值；**仓库档案**提供仓库/库房/分区 ID、有效状态和设备位置；**账号与数据权限**提供租户、机构及仓库可见范围；**订单及风险预警数据**提供物联数据穿透所需的订单关系和预警流水关联。来源：[物联设备字段清单](./物联设备字段清单.md)、REF-WH、REF-ACCOUNT、REF-RBAC、[00-全局契约与RBAC权限矩阵](../../00-全局契约与RBAC权限矩阵.md)。 |
| 下游引用 | 订单风控配置、设备预警信息、押品预警信息、设备事务通知及其他风险联动模块 |
| 业务单据 | 不生成独立业务单据，不建立单据状态机 |

## 4. 页面与字段规则

### 4.1 字段引用

列表字段、筛选字段、字段来源、取值、展示顺序和字段级校验统一以[物联设备字段清单](./物联设备字段清单.md)为准；行操作输入、查询、展示、返回和特有审计字段统一以[物联设备操作字段清单索引](./02-操作字段清单/操作字段清单索引.md)及其子清单为准。本主 PRD 不重复定义字段属性。

实时物联数据、预警记录和页面状态属于“设备数据”操作字段，不属于设备主数据列表字段。

### 4.2 数据可见范围

物联设备查询和行操作继承监控设备的数据权限原则：

| 设备绑定情况 | 可见规则 |
| :--- | :--- |
| 已绑定位置 | 登录用户拥有该位置所属仓库数据权限时可见；无对应仓库权限时不返回设备数据 |
| 未绑定位置 | 当前租户内账号有效且具备物联设备菜单查看权限的用户可见；不要求仓库数据权限；仓库、库房、分区和具体位置字段返回空值 |
| 已移除设备 | 有效设备列表不展示；历史业务详情按快照规则保留 |

“所有用户可见”仍受租户隔离、账号有效性和菜单功能权限限制，不代表跨租户可见。

## 5. 操作功能

### 5.1 操作总览

| 操作 | 入口 | 是否改变设备基础数据 | 规则来源 |
| :--- | :--- | :---: | :--- |
| 重命名 | 列表“操作”列 | 是 | 继承[监控设备主 PRD·5.2 重命名](../01监控设备/监控设备主PRD.md#52-重命名) |
| 仓库绑定 | 列表“操作”列 | 是 | 继承[监控设备主 PRD·5.3 仓库绑定](../01监控设备/监控设备主PRD.md#53-仓库绑定) |
| 设备数据 | 列表“操作”列 | 否 | 本文档第5.2节 |
| 移除设备 | 列表“操作”列 | 是 | 继承[监控设备主 PRD·5.7 移除设备](../01监控设备/监控设备主PRD.md#57-移除设备) |

重命名、仓库绑定和移除设备与监控设备使用一致的弹窗、权限、校验、并发控制、幂等、跨模块联动、异常处理和审计规则。物联设备不复制或另行改写上述规则；如监控设备规则发生变更，物联设备同步继承最新版本。

### 5.2 设备数据

#### 5.2.1 页面进入与当前数据

| 规则项 | 规则 |
| :--- | :--- |
| 操作入口 | 从物联设备列表“操作”列点击“设备数据” |
| 权限校验 | 校验物联设备菜单查看权限、设备数据查看权限、租户权限、机构权限和设备数据权限 |
| 当前数据请求 | 进入页面时，根据设备实际类型实时请求该设备对应的物联采集指标（温度、湿度、二氧化碳、氧气、烟感或多功能组合值）并展示 |
| 展示内容 | 展示本次请求返回的当前实时指标数值；未返回的指标展示“暂无数据” |
| 刷新规则 | 本期仅进入页面时请求一次，不持续轮询、不自动刷新；手动刷新能力待后续确认 |
| 数据来源 | 三方设备平台实时数据接口 |
| 页面职责 | 发起数据请求并展示返回值，不修改设备基础数据、订单数据或预警处理状态 |

进入页面时，根据设备实际类型请求温度、湿度、二氧化碳、氧气、烟感或多功能组合指标；仅展示本次请求实际返回的指标，未返回的指标展示“暂无数据”，不得使用旧数据冒充本次请求结果。指标单位、精度、更新时间、异常值和风险预警边界以[设备数据字段清单](./04-操作字段清单/03设备数据字段清单.md)及物联设备业务规则规格为准；未完成三方确认的指标只展示数据，不自行生成预警。

#### 5.2.2 预警记录

设备数据页面同时展示当前设备关联订单产生的全部温湿度预警记录及处理信息。该逻辑与[监控设备主 PRD·5.5 查看回放](../01监控设备/监控设备主PRD.md#55-查看回放)中“回放页面只读展示预警列表”的逻辑一致，但不展示监控回放画面。

| 规则项 | 规则 |
| :--- | :--- |
| 数据范围 | 当前设备绑定或曾绑定订单后，在订单上产生的温度、湿度预警记录；具体历史留存范围以风险预警模块为准 |
| 展示方式 | 多条预警以列表展示，每条预警独立记录 |
| 预警处理归属 | 预警处理由风险预警模块负责，物联设备端仅只读展示 |
| 操作限制 | 不提供处理、解除、修改预警状态或补录处置结果入口 |
| 关联条件 | 查询必须使用设备编码及订单关联标识，不得使用列表行号作为关联条件 |

预警记录的订单、预警类型、监测值、阈值、状态和处理信息等返回字段以[设备数据字段清单](./04-操作字段清单/03设备数据字段清单.md)为准；预警处理动作仍归属风险预警模块。

#### 5.2.3 页面状态与异常

| 场景 | 处理规则 |
| :--- | :--- |
| 当前数据加载中 | 展示加载状态；请求未完成前不得展示旧设备或其他设备数据 |
| 当前数据为空 | 温度或湿度无返回值时分别展示“暂无数据” |
| 预警记录为空 | 展示空状态，明确当前设备暂无关联温湿度预警记录 |
| 当前数据接口失败 | 保留页面结构，提示当前设备数据获取失败；不得修改设备字段或预警记录 |
| 预警记录接口失败 | 当前数据与预警记录分区独立处理；预警区提示加载失败，不影响当前数据区展示 |
| 权限校验失败 | 不返回设备数据和预警记录，不展示可绕过权限的详情内容 |
| 设备已移除 | 页面请求时重新校验设备有效性；无效时禁止继续查询并提示设备不可用 |
| 返回设备不一致 | 三方返回的设备编码与当前请求设备编码不一致时丢弃结果并记录异常日志 |

## 6. 权限、并发与审计

### 6.1 权限规则

1. 所有列表查询和操作接口必须同时校验菜单功能权限、操作权限、租户权限及设备所属仓库数据权限；未绑定位置设备不要求仓库数据权限。
2. 前端隐藏操作按钮只用于交互优化，后端必须独立执行权限校验。
3. “设备数据”页面的当前数据和预警记录必须分别执行服务端权限校验，不得因其中一个接口有权限而放宽另一个接口的数据范围。

### 6.2 并发与幂等规则

- 重命名、仓库绑定和移除设备继承监控设备主 PRD中的版本校验、锁或等效并发控制规则。
- 设备数据请求使用设备编码作为幂等关联键；禁止使用列表行号或设备名称作为唯一请求条件。
- 页面快速重复点击“设备数据”时，前端应防重复提交；服务端仍需校验请求上下文和设备有效性。
- 设备已移除、设备编码不匹配或设备权限发生变化时，请求不得写入任何设备或预警数据。

### 6.3 审计规则

重命名、仓库绑定和移除设备继承监控设备审计要求。以下设备数据行为也需记录操作日志：

| 行为 | 审计内容 |
| :--- | :--- |
| 进入设备数据页面 | 操作人、设备编码、请求时间、权限校验结果、请求标识和操作结果 |
| 获取当前指标 | 数据请求时间、设备编码、返回成功/失败、失败原因和第三方请求标识；不篡改第三方原始值 |
| 查询预警记录 | 操作人、设备编码、订单关联条件、查询时间、返回数量、权限校验结果和操作结果 |
| 查询失败 | 记录接口名称、请求标识、失败原因、设备编码和异常上下文；敏感信息按角色脱敏 |

---

## 4. 功能清单

> 功能能力矩阵由原《物联设备功能清单》合并；规则细节见《物联设备业务规则规格》。

## 一、功能清单矩阵

| 功能编号 | 功能名称 | 适用角色 | PC端能力 | 移动端能力 | 关键控制与规则 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| I-01 | 物联设备列表与筛选 | 设备管理员、监管人员 | 类型、仓库、绑定状态筛选 | 查看列表与筛选 | 实时数据按设备权限返回 |
| I-02 | 设备名称维护 | 设备管理员 | 重命名、保存、取消 | 重命名 | 继承设备通用命名规则 |
| I-03 | 仓库位置绑定 | 设备管理员 | 绑定仓库、库房、分区和位置 | 仓库绑定 | 校验层级归属和有效业务关联 |
| I-04 | 设备数据查看 | 监管、风控 | 当前采集值、温湿度预警、空状态和异常 | 查看设备数据 | 按设备类型请求指标，不持续轮询 |
| I-05 | 移除设备 | 设备管理员 | 移除确认和跨模块联动 | 移除设备 | 失败保持原关联 |
| I-06 | 操作审计 | 审计、管理员 | 查询操作和第三方请求结果 | 未纳入本次移动端范围 | 记录前后快照、设备编码和结果 |

## 二、功能边界

- 物联设备页面负责设备主数据和实时数据展示，不负责风险预警处理。
- 温度、湿度、二氧化碳、氧气、烟感及多功能设备按第三方实际返回值展示。
- 移动端已确认支持：物联设备列表与筛选、重命名、仓库绑定、设备数据和移除设备；操作审计查询不纳入本次移动端范围。
- 操作字段见[操作字段清单索引](./02-操作字段清单/操作字段清单索引.md)。

## 8. 验收重点

| 编号 | 验收项 |
| :--- | :--- |
| A01 | 行操作仅包含重命名、仓库绑定、设备数据、移除设备四项 |
| A02 | 重命名、仓库绑定和移除设备完整继承监控设备对应规则，不产生独立分叉逻辑 |
| A03 | 点击“设备数据”进入页面时，针对当前设备请求温度、湿度、二氧化碳、氧气、烟感或多功能组合指标，仅展示本次实际返回值 |
| A04 | 设备数据页面不持续轮询、不自动刷新，不展示其他设备或上一次设备的缓存值 |
| A05 | 设备关联订单产生的温湿度预警全部以列表展示，并展示处理状态、处理人等处理信息 |
| A06 | 物联设备端只读展示预警，不提供处理、解除或修改预警状态入口 |
| A07 | 当前指标接口和预警记录接口分区处理，任一接口失败不影响另一分区的正确展示 |
| A08 | 所有请求以设备编码和权限上下文校验，设备已移除或返回设备不一致时不得展示错误数据 |
| A09 | 设备数据查询、当前值获取、预警记录查询及失败请求均写入审计日志 |

## 修订记录

| 日期 | 版本 | 变更内容 | 影响范围 |
| :--- | :--- | :--- | :--- |
| 2026-08-20 | — | 合并原《物联设备功能清单》至本文件 第4章 功能清单 |
| 2026-08-06 | V1.0 | 新增物联设备主需求文档，定义四个行操作及设备数据页面的当前温湿度、订单温湿度预警展示、权限、异常和审计规则 | 物联设备列表、设备数据页面及设备操作 |
| 2026-08-14 | V1.1 | 将实时采集字段归入设备数据操作子清单，统一温度、湿度、二氧化碳、氧气和烟感指标范围，并明确多功能设备按本次返回值展示 | 设备数据操作、指标展示和字段引用 |
| 2026-08-14 | V1.3 | 补充各项指标的单位、精度、更新时间、异常值和风险预警确认边界；确认移动端支持范围 | 指标口径、第三方接口和移动端范围 |
