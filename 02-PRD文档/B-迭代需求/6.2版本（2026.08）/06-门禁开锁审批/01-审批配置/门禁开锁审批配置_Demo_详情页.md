# 门禁开锁审批配置_Demo_详情页

> **文档定位**：AI 生成「开锁审批配置详情」前端原型的输入文档。
> **字段定义**：见《[门禁开锁审批配置字段清单](./门禁开锁审批配置字段清单.md)》。
> **线框参考**：[门禁开锁审批配置_ASCII_详情页](./门禁开锁审批配置_ASCII_详情页.md)
> **规则引用**：[门禁开锁审批配置业务规则规格](./门禁开锁审批配置业务规则规格.md) 第五章、C04–C08
> **基准来源**：`03-前端原型demo/B端PC/src/features/unlock-approval-configs/pages/UnlockApprovalConfigDetailPage.tsx`（规划路径）
> **版本**：V1.2 | 2026-09-01

---

## 1. 页面概述

| 项目 | 内容 |
| :--- | :--- |
| 页面名称 | 开锁审批配置详情 |
| 页面目标 | 只读查看开锁审批规则完整配置，并执行编辑/启停/逻辑删除（仅已停用） |
| 菜单路径 | `配置管理 → 业务流程管理 → 开锁审批 → 详情` |
| 页面路径 | `/配置管理/业务流程管理/开锁审批/详情/:configNo` |
| 骨架结构 | 页头识别区（Card）→ 基础识别与范围 → 审批策略 → 系统审计 |

> **已停用**可逻辑删除（C08，destructive 按钮 + 二次确认）。**不展示** Version、操作审计日志（后台 A01–A02 可查）。

---

## 2. DetailSection 布局规范（⚠️ 生成必遵）

与 dev 共用组件 `@/shared/components/DetailSection` + `index.css`：

| 层级 | 实现 |
| :--- | :--- |
| 分区 | `DetailSection`：标题 + 内容区 Card |
| 内容区 | `detail-section-content`：**4 列 CSS Grid**（≤1100px → 3 列），`gap: 22px 28px` |
| 单字段 | `DetailField`：**label 在上、value 在下** |
| label 样式 | 13px、`#86909c` |
| value 样式 | 14px、`#1d2129` |

**禁止**：ElDescriptions 横向「左 label 右 value」布局。

> **审批节点**：跨列只读表格，表头「节点序号 / 审批对象类型 / 指定人员或指定角色」。

---

## 3. 页头识别区

```text
{配置名称}   [ {状态Tag} ]          [ ← 返回 ]  [ 编辑 ]  [ 停用/启用 ]  [ 删除? ]
```

### 3.1 页头操作（按状态）

| 状态 | 展示的操作 |
| :--- | :--- |
| 已启用 | 返回 / 编辑 / 停用 |
| 已停用 | 返回 / 启用 / 删除 |

---

## 4. 基础识别与范围卡片（DetailSection）

| 字段 | 展示规格 | 条件 |
| :--- | :--- | :--- |
| 配置编号 | 如 `UNLOCK-CFG-001` | 始终 |
| 配置名称 | 文本 | 始终 |
| 适用设备 | 摘要 + `[查看设备清单 >]` 只读抽屉（可跨列） | 始终 |

---

## 5. 审批策略卡片（DetailSection）

| 字段 | 展示规格 |
| :--- | :--- |
| 审批方式 | 固定展示「任一人通过」 |
| 审批超时时间 | 如 `12 小时` |
| 配置版本 | 如 `v2` |
| 审批节点 | 只读表格：节点序号 / 审批对象类型 / 指定人员或指定角色 |

---

## 6. 系统审计卡片（DetailSection）

| 字段 | 展示规格 |
| :--- | :--- |
| 状态 | 状态 Tag |
| 停用原因 | 已停用时展示 |
| 创建人/时间 | 合并或分列 |
| 更新人/时间 | 合并或分列 |

---

## 7. Mock 数据（UNLOCK-CFG-001 · 已启用）

| 字段 | 取值 |
| :--- | :--- |
| 配置名称 | A库指定挂锁审批 |
| 适用设备 | 已选 3 台 |
| 审批方式 | 任一人通过 |
| 审批节点 | 1. 指定角色「仓库主管」；2. 指定人员「李四（监管部）」 |
| 审批超时时间 | 12 小时 |
| 配置版本 | v2 |
| 状态 | 已启用 |

---

## 8. 自检清单

1. ✅ 字段来自字段清单第一至三章
2. ✅ DetailSection 4 列栅格、上名下达
3. ✅ 无 Version；已停用页头含删除（C08）
4. ✅ 未新增字段或业务规则

---

## 修订记录

| 日期 | 版本 | 变更 |
| :--- | :---: | :--- |
| 2026-08-28 | V1.0 | 初版 |
| 2026-09-01 | V1.2 | 已停用页头增加逻辑删除；二次确认对齐列表 §4.3 |
