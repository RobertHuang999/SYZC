# 开锁申请_Demo_详情页_移动端

> **文档定位**：**衍生文档**——由三件套派生的原型输入；**非独立真源**。业务见[主PRD §4.4 申请人详情与凭证](./开锁申请主PRD.md#44-申请人详情与凭证)，字段见[字段清单](./开锁申请字段清单.md)，规则见[业务规则规格](./开锁申请业务规则规格.md)，线框见[ASCII](./开锁申请_ASCII_详情页_移动端.md)。变更须先更新三件套。
> **列表入口**：[03-H5我的申请记录/我的申请记录_Demo_移动端](../03-H5我的申请记录/我的申请记录_Demo_移动端.md)
> **基准来源**：`03-前端原型demo/B端H5/src/features/my-applies/pages/MyUnlockApplyDetailPage.tsx`
> **版本**：V1.3 | 2026-09-02

> **端说明**：H5 **列表**由「我的申请记录 → Tab 开锁审核」定义；本文件仅描述**详情页**。
>
> **6.2 范围说明**：详情含「凭证信息」SectionCard（密码展示/复制、重新获取密码）；页头含申请状态 + 凭证状态 Tag。

---

## 1. 页面概述

| 项目 | 内容 |
| :--- | :--- |
| 页面名称 | 开锁申请详情（H5 · 申请人侧） |
| 页面目标 | 只读查看申请/审批进度与凭证；待审批可撤回；失败时可重新获取密码 |
| 菜单路径 | `业务办理 → 我的申请记录 → Tab「开锁审核」→ 查看详情` |
| 页面路径 | `/m/my-applies/unlock/:applyNo` |
| 骨架结构 | NavBar → 页头摘要框 → SectionCard（可折叠）→ 底部动作（条件） |

---

## 2. 移动端布局规范（⚠️ 生成必遵）

| 层级 | 实现 |
| :--- | :--- |
| 只读字段 | `KeyValue`：**左 label（w-20）右 value** 横排 |
| 分区 | `SectionCard`；审批配置/审批记录默认折叠；**凭证信息默认展开** |
| 页头摘要 | 浅色背景 Card：申请单号 + 设备名称 + 申请状态 Tag + **凭证状态 Tag** |

**禁止**：套用 PC 4 列 Grid 或 DetailField 纵向堆叠。

---

## 3. NavBar

| 元素 | 规格 |
| :--- | :--- |
| 左侧 | `<` 返回列表 `?tab=unlock-applies`（保留 Tab 与筛选） |
| 标题 | 开锁申请详情 |
| 右侧 | 无 |

---

## 4. 页头摘要框

| 字段 | 规格 |
| :--- | :--- |
| 申请单号 | 列表卡片不展示，详情必展示 |
| 设备名称 | 次级标题 |
| 申请状态 | Tag |
| 凭证状态 | Tag，前缀「凭证」 |

---

## 5. SectionCard 字段

### 5.1 设备与位置（默认展开）

设备编码、设备类型、绑定仓库、库房/分区、具体位置。

### 5.2 申请内容（默认展开）

申请人、手机号（脱敏）、事由、备注、**有效期（仅人脸门禁展示，挂锁不展示）**、提交时间。

### 5.3 审批配置快照（折叠）

配置编号、版本、审批方式、审批节点、审批超时（如有）。

### 5.4 审批记录（折叠）

节点、处理人、结果、意见、时间；待审批展示「暂无记录，等待审批」。

### 5.5 凭证信息（默认展开）

> 规则见 [02查看与下发凭证字段清单](./02-操作字段清单/02查看与下发凭证字段清单.md)。

| 字段 | 规格 |
| :--- | :--- |
| 凭证状态 | KeyValue（**挂锁 2 态；人脸 5 态**） |
| 密码有效期 | **仅人脸门禁展示**（起止时间）；**挂锁门禁不展示** |
| 失败原因 | **仅人脸门禁**凭证=生成失败时展示 |
| 重新获取密码 | 全宽按钮；**仅人脸门禁**凭证=生成失败时展示 |
| 临时密码 | 仅凭证=已下发时展示 + 复制（挂锁与人脸均支持） |
| 失效提示 | **仅人脸门禁**已过期 / 被覆盖时展示 |

> **不含**「关联事务」分区（W00）。

---

## 6. 底部动作

| 申请状态 | 动作 |
| :--- | :--- |
| 待审批且需审批 | 全宽 `[ 撤回申请 ]` + 二次确认 |
| 其他终态 | 无固定底栏 |

---

## 7. Mock 数据

| 申请单号 | 设备类型 | 申请状态 | 凭证状态 | 底部动作/验证要点 |
| :--- | :--- | :--- | :--- | :--- |
| UA20260828001 | 挂锁门禁 | 待审批 | 未生成 | 撤回申请；凭证未生成；无有效期 |
| UA20260827015 | 挂锁门禁 | 已通过 | 已下发 | 挂锁已下发；可复制密码；无有效期 |
| UA20260826008 | 人脸门禁 | 已通过 | 生成失败 | 人脸·重新获取密码 |
| UA20260826011 | 人脸门禁 | 已通过 | 生成失败 | 人脸·重新获取密码 |
| UA20260826010 | 人脸门禁 | 已通过 | 已下发 | 人脸·三方下发失败仍有密码；含密码有效期 |
| UA20260827021 | 人脸门禁 | 已通过 | 已过期 | 人脸·凭证过期；不展示密码 |
| UA20260826013 | 人脸门禁 | 已通过 | 被覆盖 | 人脸·凭证被覆盖；不展示密码 |
| UA20260826006 | 挂锁门禁 | 已驳回 | 未生成 | 驳回原因；无凭证 |

---

## 8. 反馈文案

| 操作 | 文案 |
| :--- | :--- |
| 撤回成功 | 撤回成功 |
| 撤回失败 | 撤回失败：申请状态已变更 |
| 复制密码 | 已复制到剪贴板 |
| 重新获取密码 | 密码已重新获取 |

---

## 9. 自检清单

1. ✅ KeyValue 横排，非 PC Grid
2. ✅ 列表不含申请单号；详情含申请单号
3. ✅ 含凭证信息 SectionCard + 页头凭证 Tag
4. ✅ 凭证过期/被覆盖不展示密码
5. ✅ 未新增业务规则（引用业务规则规格 §4）
