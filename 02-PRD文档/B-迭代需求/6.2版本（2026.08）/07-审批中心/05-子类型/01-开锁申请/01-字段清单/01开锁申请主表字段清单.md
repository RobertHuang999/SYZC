# 开锁申请主表字段清单

> **所属对象**：开锁申请（主表）
> **索引入口**：[开锁申请字段清单](../开锁申请字段清单.md)
> **流程规则**：[开锁申请业务规则规格](../开锁申请业务规则规格.md)

---

## 一、基础识别字段

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 列表展示 | 可筛选 | 详情展示 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 序号 | 系统生成 | 整数 | 系统自动 | 是 | 否 | 不适用 | 列表行序号 | 非持久化主键 |
| 申请单号 | 系统生成 | 文本；租户内唯一 | 系统自动 | 是 | 是（精确） | 是 | 开锁申请的业务唯一编号 | 提交成功后生成；幂等重复提交返回原单号 |
| 申请状态 | 系统维护 | 枚举：`待审批`、`已通过`、`已驳回`、`已撤回`、`已失效`、`已作废` | 系统自动 | 是 | 是（多选） | 是 | 申请当前审批主状态 | 凭证处理状态不在此字段表达 |
| 凭证状态 | 系统维护 | 枚举：`未生成`、`生成中`、`已生成`、`已下发`、`生成失败`、`下发失败`、`已使用`、`已过期`、`已撤销`、`已失效（被覆盖）` | 系统自动 | 是 | 是（多选） | 是 | 关联凭证的当前生命周期状态 | 独立对象状态，详情页与申请状态分区展示；完整定义见[临时开锁凭证字段清单](./02临时开锁凭证字段清单.md) |

---

## 二、申请人字段

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 列表展示 | 可筛选 | 详情展示 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 申请人 | 系统带出并快照 | 文本；姓名（账号） | 系统自动 | 是 | 是（模糊） | 是 | 发起申请的操作人 | 提交时固化快照 |
| 申请人所属机构 | 系统带出并快照 | 文本 | 系统自动 | 否 | 否 | 是 | 申请人提交时的所属机构 | 固化快照 |
| 申请人手机号 | 系统带出并快照 | 文本；11 位手机号 | 系统自动 | 否 | 否 | 是 | 接收凭证短信的手机号 | 提交前校验已绑定；固化快照；敏感字段按角色脱敏 |

---

## 三、设备与位置快照字段

> 以下字段引用门禁设备主数据，申请提交时固化快照；字段口径继承《门禁设备字段清单》，不在此重复定义主档维护规则。

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 列表展示 | 可筛选 | 详情展示 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 设备编码 | 继承门禁设备并快照 | 文本 | 系统自动 | 是 | 是（精确） | 是 | 申请目标设备的唯一编码 | 继承自门禁设备；作为关联主键 |
| 设备名称 | 继承门禁设备并快照 | 文本 | 系统自动 | 是 | 是（模糊） | 是 | 申请时的系统内设备名称 | 对应门禁设备「系统内名称」快照 |
| 设备类型 | 继承门禁设备并快照 | 枚举：挂锁门禁、人脸门禁 | 系统自动 | 是 | 是（下拉） | 是 | 申请时的设备类型 | 决定密码有效期等扩展规则 |
| 绑定仓库 | 继承门禁设备并快照 | 文本 | 系统自动 | 是 | 是（下拉） | 是 | 申请时设备绑定的仓库 | 继承门禁设备「绑定仓库」快照 |
| 绑定库房 | 继承门禁设备并快照 | 文本集合 | 系统自动 | 否 | 否 | 是 | 申请时设备绑定的库房 | 继承门禁设备「绑定库房」快照 |
| 绑定分区 | 继承门禁设备并快照 | 文本集合 | 系统自动 | 否 | 否 | 是 | 申请时设备绑定的分区 | 继承门禁设备「绑定分区」快照 |
| 具体位置 | 继承门禁设备并快照 | 文本 | 系统自动 | 否 | 否 | 是 | 申请时设备的安装位置 | 继承门禁设备「具体位置」快照 |
| 命中位置说明 | 系统计算并快照 | 文本 | 系统自动 | 否 | 否 | 是 | 本次匹配生效的位置范围说明 | 多绑定合并时记录全部命中位置 |

---

## 四、申请内容字段

> `事由`、`备注` 继承《获取门锁密码字段清单》；本表只补充审批链路增量字段。发起入口见[发起开锁申请字段清单](../02-操作字段清单/01发起开锁申请字段清单.md)。

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 列表展示 | 可筛选 | 详情展示 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 事由 | 继承获取门锁密码 | 枚举：出库、入库、移库、参观、其他 | 必填 | 是 | 是（下拉） | 是 | 申请开锁的业务事由 | 继承《获取门锁密码字段清单·事由》 |
| 备注 | 继承获取门锁密码 | 文本；最多 50 字 | 选填 | 否 | 否 | 是 | 申请补充说明 | 继承《获取门锁密码字段清单·备注》 |
| 预计使用时段 | 用户填写 | 日期时间范围；开始 ≤ 结束 | 必填 | 否 | 否 | 是 | 申请人预计使用密码的时间段 | 审批链路增量字段；仅供审批参考，不替代凭证有效期 |

---

## 五、审批配置快照字段

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 列表展示 | 可筛选 | 详情展示 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 配置编号 | 引用开锁审批配置并快照 | 文本 | 系统自动 | 否 | 是（精确） | 是 | 命中时的审批配置编号 | 引用[门禁开锁审批配置字段清单·配置编号](../../../../06-门禁开锁审批/01-审批配置/门禁开锁审批配置字段清单.md) |
| 配置版本 | 引用开锁审批配置并快照 | 整数 | 系统自动 | 否 | 否 | 是 | 命中时的配置版本 | 配置后续变更不影响本申请 |
| 审批方式 | 引用开锁审批配置并快照 | 枚举：任一人通过、按顺序审批 | 系统自动 | 否 | 否 | 是 | 命中时的审批处理方式 | 决定审批任务生成规则 |
| 审批节点快照 | 引用开锁审批配置并快照 | 节点列表（含对象类型、人员/角色标识及名称） | 系统自动 | 否 | 否 | 是 | 命中时的审批链配置 | 保存节点序号、对象类型、指定人员或指定角色 |
| 审批人快照 | 系统解析并快照 | 人员列表 | 系统自动 | 否 | 否 | 是 | 申请提交时从各节点解析出的 eligible 处理人 | 指定人员=该账号；指定角色=展开当时启用成员；排除申请人；不因后续变更重算 |
| 审批超时时间 | 引用开锁审批配置并快照 | 整数；单位小时 | 系统自动 | 否 | 否 | 是 | 命中时的超时规则 | 用于计算失效时间 |

---

## 六、审批结果与异常字段

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 列表展示 | 可筛选 | 详情展示 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 最终审批结论 | 审批结果回写 | 枚举：通过、驳回、撤回、超时失效、作废 | 有条件必填 | 是 | 是（下拉） | 是 | 申请最终审批结果摘要 | 终态后写入；待审批时为空 |
| 驳回原因 | 审批人填写 | 文本；最多 200 字 | 条件必填 | 否 | 否 | 是 | 审批驳回时的原因 | 驳回动作必填；操作字段见[通用审批处理字段清单](../../../04-操作字段清单/01通用审批处理字段清单.md) |
| 作废/关闭原因 | 管理员填写 | 文本；最多 200 字 | 条件必填 | 否 | 否 | 是 | 管理员异常关闭或系统复核作废原因 | 管理员关闭时必填；见[异常处理字段清单](../02-操作字段清单/03开锁申请异常处理字段清单.md) |
| 撤回时间 | 系统生成 | 日期时间 | 系统自动 | 否 | 否 | 是 | 申请人撤回操作时间 | 仅已撤回状态有值 |
| 失效时间 | 系统生成 | 日期时间 | 系统自动 | 否 | 否 | 是 | 审批超时自动失效时间 | 仅已失效状态有值 |

---

## 七、系统字段

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 列表展示 | 可筛选 | 详情展示 | 字段说明 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 请求幂等键 | 系统生成或调用方传入 | 文本 | 必填 | 否 | 否 | 否 | 防止重复提交的唯一标识 | 重复请求返回原申请单号及状态 |
| 创建时间 | 系统生成 | 日期时间 | 系统自动 | 是 | 是（范围） | 是 | 申请提交时间 | 以服务端时间为准 |
| 更新时间 | 系统生成 | 日期时间 | 系统自动 | 是 | 否 | 是 | 申请最近一次状态变更时间 | 审批、撤回、凭证状态变更时更新 |
| 操作审计日志 | 系统生成 | 结构体列表 | 系统自动 | 否 | 否 | 否 | 申请全路径操作审计 | 含提交、撤回、审批、凭证、关闭；不含密码明文 |

---

## 修订记录

| 日期 | 版本 | 变更内容 |
| :--- | :---: | :--- |
| 2026-08-28 | V1.0 | 从《开锁申请字段清单》拆分为主表独立清单 |
