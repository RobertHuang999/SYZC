# 押品预警信息_Demo_详情页

> **文档定位**：AI 生成「押品预警信息详情」前端原型的输入文档。
> **字段定义**：见《[押品预警信息字段清单](./押品预警信息字段清单.md)》。
> **线框参考**：[押品预警信息_ASCII_详情页](./押品预警信息_ASCII_详情页.md)
> **移动端对照**：[押品预警信息_Demo_移动端](./押品预警信息_Demo_移动端.md)
> **基准来源**：`03-前端原型demo/B端PC/src/features/collateral-warning-events/pages/CollateralWarningDetailPage.tsx`
> **版本**：V1.1 | 2026-08-24

> **模块说明**：无新增/编辑页；商业类解除在抵质押订单模块办理；穿透类无解除入口（R23）。

---

## 1. 页面概述

| 项目 | 内容 |
| :--- | :--- |
| 页面名称 | 押品预警信息详情 |
| 页面目标 | 只读查看单条押品预警事实，并按类型跳转处置 |
| 菜单路径 | `预警信息 → 押品预警信息 → 详情` |
| 页面路径 | `/预警信息/押品预警信息/详情/:id` |
| 骨架结构 | 页头识别区（Card）→ 基本信息 → 预警事实 [→ 穿透信息] [→ 处置信息/无效说明] |

> **6.2 Demo 不展示「业务熔断状态」**；是否公示不在详情展示，用页头「公示风险」控制（C12）。
> **无页头副标题**（与列表页区分）。

---

## 2. DetailSection 布局规范（⚠️ 生成必遵）

与 dev 共用组件 `@/shared/components/DetailSection` + `index.css`：

| 层级 | 实现 |
| :--- | :--- |
| 分区 | `DetailSection`：标题 + 内容区 Card |
| 内容区 | `detail-section-content`：**4 列 CSS Grid**（≤1100px → 3 列），`gap: 22px 28px` |
| 单字段 | `DetailField`：**label 在上、value 在下**（`flex-direction: column; gap: 6px`） |
| label 样式 | 13px、`#86909c`（`detail-field-label`） |
| value 样式 | 14px、`#1d2129`（`detail-field-value`） |

```text
┌─ DetailSection: 基本信息 ─────────────────────────────────────────────┐
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   ← 4列 grid         │
│  │预警订单   │ │订单类型  │ │预警类型  │ │预警等级  │                       │
│  │PO2026…  │ │抵/质押   │ │抵质押率…│ │● L4 …  │   ← 上名下达           │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘                       │
└────────────────────────────────────────────────────────────────────────┘
```

**禁止**：ElDescriptions 横向「左 label 右 value」、或 `el-form` 一行两列的表单行布局。

---

## 3. 页头识别区

```text
{预警类型} · {预警订单}   [ {WarningStatusBadge} ]     [ ← 返回 ]  [ 解除预警* / 查看设备事件** / 公示风险*** ]
```

容器：`rounded-xl border bg-card p-4`，`lg:flex-row` 左右布局。

### 3.1 按钮规格

| 按钮 | variant | 图标 | 行为 |
| :--- | :--- | :--- | :--- |
| 返回 | outline | ArrowLeft | Link → `/预警信息/押品预警信息` |
| 解除预警 | default | — | 跳转抵质押订单信息页（R31/P04） |
| 查看设备事件 | secondary | — | Link → `/预警信息/设备预警信息/详情/:id`（P05） |
| 公示风险 | default | — | 打开 `PublishConfirmDialog`（C12） |

### 3.2 页头动作矩阵（`getDetailHeaderActions`）

| 状态 | 类型 | 页头动作 |
| :--- | :--- | :--- |
| 未处理（有效） | 商业 6 类 | 返回 / 解除预警 |
| 未处理（有效） | 物联穿透 | 返回 / 查看设备事件 |
| 已处理（有效） | 任意 | 返回 / 公示风险（未公示时） |
| 未处理（无效） | 任意 | 返回 |

---

## 4. 基本信息卡片（DetailSection）

| 字段 | 展示规格 |
| :--- | :--- |
| 预警订单 | 订单号（支持复制/跳转单据） |
| 预警类型 | 7 类之一 |
| 预警等级 | `SeverityLevelDisplay`：色块圆点 + `{severityCode} {severityName}` |
| 预警来源 | 订单配置触发 / 物联穿透 |
| 规则名称 | 触发时固化的规则名称快照 |
| 并发版本 | `Version {version}`（字段清单第四章 Version 只读展示） |

---

## 5. 订单与仓储位置快照卡片（字段清单第四章）

| 字段 | 展示规格 |
| :--- | :--- |
| 订单业务类型 | 抵/质押 / 动态监管 |
| 货主企业主体 | 借款/出质企业全称 |
| 押品货物明细 | 货物名称、规格型号及批次 |
| 货物在押数量 | 实物在押重量/数量（如 500.00 吨） |
| 存放仓储位置 | 仓库 / 库房 / 货区 / 货位拓扑层级 |
| 质物评估总值 | 质物货值总金额（¥） |
| 关联贷款余额 | 对应授信/贷款风险敞口余额（¥） |

---

## 6. 预警事实与触发判定数据卡片

| 字段 | 展示规格 | 数据来源说明 |
| :--- | :--- | :--- |
| 货物位置 | 仓库 / 库房 / 分区-货位（如 `一号钢材仓 / A库区 / 01分区-H02`） | 来源于预警订单绑定的仓单系统/仓储 WMS 货位台账快照（`orderSnapshot.storageLocation`）；预警触发时抓取固化，锁定案发物理位置 |
| 货物与数量 | 品名规格 · 在押数量+单位（如 `热轧卷板 Q235B · 250.00吨`） | 来源于预警订单项下标的物仓单货物明细快照（`orderSnapshot.cargoName` · `cargoQuantity`）；预警触发时锁定固化，作为货权资产维权依据 |
| 预警内容 | 主 PRD 第3.4节模板全文（跨列展示） | 规则引擎根据触发事实与数值指标自动组装的标准文本 |
| 预警触发时间 | 完整秒级时间 `YYYY-MM-DD HH:mm:ss` | 事件实际触发落账时间，升级计时起点 |
| 预警现场抓拍图 | 监控摄像头即时抓拍凭证；支持点击查看高清大图弹窗 | 联动货位监控摄像头即时抓拍，私有 OSS 签名时效访问（P05） |
| 触发判定数据快照 | 结构化 4 列不可变存证卡片：`监控指标项`、`实际触发值`、`规则预警阈值`、`超标判定结果` | 固化触发时刻的判定事实，不随规则后续阈值调整而重新计算 |

### 6.1 触发判定数据快照 6 大商业类型枚举规格

| 预警类型 | 监控指标项 | 实际触发值 | 规则预警阈值 | 超标判定结果 |
| :--- | :--- | :--- | :--- | :--- |
| **贷中风控预警** | 智风控综合评分 | 38.5 分 (高危) | 准入线 60.0 分 | 低于准入线 -21.5 分 (借款企业新增诉讼冻结) |
| **抵/质押率异常** | 当前抵/质押率 (LTV) | 88.50% | 85.00% (平仓线) / 75.00% (预警补仓线) | 超出平仓警戒线 +3.50% |
| **价格下跌** | 现货价格跌幅 | -12.80% | -12.00% | 超跌 -0.80% (现货基准价 ¥68,200/吨) |
| **盘点异常** | 账实盘点差异率 | 差异率 2.30% | 允许公差 2.00% | 超差 +0.30% (盘亏 2.50 吨) |
| **巡检异常** | 例行巡检时效 | 逾期 24.0 小时 | 计划时限: 08-19 08:00 | 超时未打卡 (责任人: 巡检员刘强) |
| **解抵/质押/监管超时** | 监管业务存续期限 | 逾期 15 天 | 约定期限: 2026-06-01 | 监管期满未办理解除或展期 |
| **物联穿透告警** | *硬件联动* | *无对比值* | *无静态阈值* | 展示固定提示：“由仓储现场物理设备传感器即时异常联动触发，详细指标见下方【穿透信息】” |

---

## 7. 穿透信息卡片（仅物联穿透）

| 字段 | 展示规格 |
| :--- | :--- |
| 触发设备 | 设备名称(位置) |
| 物理子类型 | 如剪杆破坏 |
| 触发位置 | 仓库/库房/分区 |

> **说明**：页面操作区提供「查看设备事件」跳转按钮，卡片内部不冗余展示【关联事件编号】与【关联事件】。须在设备预警信息现场核销后 COL-R14 自动解除；本页无解除预警。

---

## 8. 处置与核销信息卡片（仅已处理）

| 字段 | 展示规格 |
| :--- | :--- |
| 处理时间 | `formatEmptyValue(processedTime)` |
| 处理责任人 | 姓名(机构) 或系统自动 |
| 情况说明 | 文本 |
| 现场核实照片凭证 | 凭证按钮列表，点击弹窗查看大图 |
| 解除预警抓拍图 | 监控摄像头即时抓拍凭证；支持点击查看高清大图 |

---

## 9. 无效说明（仅未处理无效）

| 字段 | 展示规格 |
| :--- | :--- |
| 无效原因 | 如「关联订单预警配置已删除（C11）」；无效时 Badge 下方附 `失效原因：{invalidReason}` |

基本信息/预警事实只读；无解除、公示。

---

## 9. 404 空态

| 元素 | 规格 |
| :--- | :--- |
| 返回 | outline Button + ArrowLeft → 列表页 |
| 提示 | Card 居中「未找到对应的押品预警信息」 |

---

## 10. 场景 Mock 对照

| 场景 | 订单 | 页头动作 |
| :--- | :--- | :--- |
| 抵质押率未处理 | PO202608-01 | 返回 / 解除预警 |
| 物联穿透未处理 | PO202608-01 | 返回 / 查看设备事件 |
| 价格下跌已处理 | PO202607-12 | 返回 / 公示风险 |
| 盘点无效 | PO202606-99 | 返回 |

---

## 11. 弹窗（复用列表页规格）

- 公示风险确认（C12）
- 解除预警无权限（R31/P04）

---

## 12. 自检清单

1. ✅ 字段来自字段清单（详情展示=是）
2. ✅ 页头动作来自 `getDetailHeaderActions` / 第五章动作矩阵
3. ✅ **DetailField 上 label 下 value + 4 列 grid**，禁止横向 Descriptions 行
4. ✅ 404 空态对齐 dev CollateralWarningDetailPage
5. ✅ 未新增业务规则
