# 订单预警配置_Demo_新增编辑页

> **文档定位**：AI 生成「订单预警配置新增 / 编辑」前端原型的输入文档。
> **字段定义**：见《[订单预警配置字段清单](./订单预警配置字段清单.md)》第二章。
> **线框参考**：[订单预警配置_ASCII_新增编辑页](./订单预警配置_ASCII_新增编辑页.md)
> **基准来源**：`03-前端原型demo/B端PC/src/features/order-warning-configs/pages/OrderWarningConfigFormPage.tsx`
> **版本**：V1.1 | 2026-08-24

---

## 1. 页面概述

| 项目 | 内容 |
| :--- | :--- |
| 页面名称 | 订单预警配置新增 / 编辑 |
| 页面目标 | 为单个订单配置一站式多策略综合风控规则 |
| 菜单路径 | `预警配置 → 订单预警配置 → 新增 / 编辑` |
| 新增路径 | `/预警配置/订单预警配置/新增` |
| 编辑路径 | `/预警配置/订单预警配置/编辑/:id` |
| 骨架结构 | 页头 → 基础识别卡片 → 6 大策略卡片组（Switch + 折叠面板） |

---

## 2. 页头操作栏

| 按钮 | 规格 |
| :--- | :--- |
| 返回 / 取消 | 次操作 |
| 保存并生效 | **主操作**；至少启用 1 项策略（R07a） |

---

## 3. 新增态 vs 编辑态差异

| 对比项 | 新增 | 编辑 |
| :--- | :--- | :--- |
| 规则名称 | 空，可填 | Readonly（R06） |
| 关联订单 | 可选（过滤已有规则订单） | Readonly（R06） |
| 订单类型/货主/货物 | 选订单后自动带出（R02） | 只读 |
| 各策略 Switch | 默认 OFF | 读当前启用状态 |
| Version | 不展示 | 只读，提交携带（R07） |
| 保存后状态 | 生效中 | 保持生效中 |
| 关闭已启用子项 | — | 该子项未处理流水置无效（C01a） |

---

## 4. 基础识别卡片

| 字段 | 控件类型 | 必填 | 规格 |
| :--- | :--- | :---: | :--- |
| 规则名称 | `Input` | 是 | ≤50 字，租户唯一（R01）；编辑 Readonly |
| 关联订单 | `SelectWithSearch` | 是 | R03/R04；编辑 Readonly |
| 订单类型 | `ReadonlyText` | 自动 | 抵/质押 / 监管 |
| 货主 | `ReadonlyText` | 自动 | 脱敏手机号 |
| 货物 | `ReadonlyText` | 自动 | 大类/品类/规格/单位 |
| Version | `ReadonlyText` | 编辑 | 乐观锁 |

---

## 5. 预警策略配置区（6 大卡片）

卡片标题：**预警策略配置（至少启用 1 项 · R07a）**。

每张策略卡片结构：

```text
[Switch ON/OFF]  策略 N：{名称}  （监管订单不可用提示）  [ 收起/展开 ▼▲ ]
  └─ 策略专属参数字段
  └─ 预警等级（SelectWithSearch，提交 severity_level_id）
  └─ 通知渠道（CheckboxGroup）
  └─ 预警对象（多选，R19）
  └─ 升级预警（Checkbox + 天数 + 对象，R20）
```

### 5.1 六策略卡片清单

| # | 策略名称 | 监管订单 | 核心参数字段 |
| :---: | :--- | :---: | :--- |
| 1 | 解抵/质押/监管超时 | ✅ | 超时配置动态表格（R14） |
| 2 | 抵/质押率双控 | ❌ 置灰 R12 | 补仓线 LTV + 等级 / 平仓线 LTV + 等级 / 解除方式（R12a） |
| 3 | 仓储巡检超期 | ✅ | 巡检人 + 周期动态表格（R17） |
| 4 | 解抵/质押/监管超时监控 | ✅ | （同策略1或合并展示，以字段清单为准） |
| 5 | 盘点账实差异 | ✅ | 启用即监听（R16） |
| 6 | 贷中风控模型 | ❌ 置灰 R12 | 模型参数（R18） |

> 实际 6 卡片命名与 ASCII 线框一致：价格下跌、抵质押率双控、仓储巡检、解抵质押监管超时、盘点差异、贷中风控。

### 5.2 监管订单差异化

策略 2（抵质押率）、策略 6（贷中）Switch **disabled** + 辅助文案「监管订单不可用」。

### 5.3 LTV 双等级

抵质押率子项分别绑定：
- `margin_call_severity_level_id`（补仓线）
- `close_out_severity_level_id`（平仓线）
- R11：平仓线 LTV ≥ 补仓线

---

## 6. 卡片交互规则

- Switch OFF：折叠面板收起，内部字段不校验
- Switch ON：展开面板，校验该子项全部必填
- 新增页默认全部 OFF；编辑页读真实状态
- 下拉等级读 03/01 启用档；展示编码+名称+色块

---

## 7. 保存校验

| 规则 | 反馈 |
| :--- | :--- |
| R01 名称重复 | 阻断 |
| R04 订单已有规则 | 阻断 |
| R07a 全部 OFF | 「请至少启用一项预警策略」 |
| R08~R09 LTV 区间 | 「平仓线必须高于补仓线」 |
| R10 未选等级 | 「请选择{子项}的预警等级」 |
| R19 未选对象 | 「请配置{子项}的预警对象」 |
| R20 升级不完整 | 「请完善升级天数与对象」 |
| R12 监管提交禁用策略 | 服务端拒绝 |

**成功**：Toast「保存成功」→ 列表页。

---

## 8. Mock 数据（编辑态）

对齐 ASCII PO1002：启用跌价(L3)、质押率(L3/L4)、巡检(L2)；监管策略 OFF。

---

## 9. 自检清单

1. ✅ 字段来自字段清单第二章各策略子项
2. ✅ 卡片结构与 ASCII V2.0 一站式模式一致
3. ✅ 未新增业务规则
