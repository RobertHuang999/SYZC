# 订单预警配置_Demo_详情页

> **文档定位**：AI 生成「订单预警配置详情」前端原型的输入文档。
> **字段定义**：见《[订单预警配置字段清单](./订单预警配置字段清单.md)》。
> **线框参考**：[订单预警配置_ASCII_详情页](./订单预警配置_ASCII_详情页.md)
> **基准来源**：`03-前端原型demo/B端PC/src/features/order-warning-configs/pages/OrderWarningConfigDetailPage.tsx`
> **版本**：V1.1 | 2026-08-24

---

## 1. 页面概述

| 项目 | 内容 |
| :--- | :--- |
| 页面名称 | 订单预警配置详情 |
| 页面目标 | 只读查看订单综合风控规则及已激活策略详情 |
| 菜单路径 | `预警配置 → 订单预警配置 → 详情` |
| 页面路径 | `/预警配置/订单预警配置/详情/:id` |
| 骨架结构 | 页头识别区（Card）→ 基础识别（DetailSection）→ 已激活策略卡片组 → 未启用策略列表 → 系统审计（DetailSection） |

---

## 2. DetailSection 布局规范（⚠️ 生成必遵）

与 dev 共用组件 `@/shared/components/DetailSection` + `index.css`：

| 层级 | 实现 |
| :--- | :--- |
| 分区 | `DetailSection`：标题 + 内容区 Card |
| 内容区 | `detail-section-content`：**4 列 CSS Grid**（≤1100px → 3 列），`gap: 22px 28px` |
| 单字段 | `DetailField`：**label 在上、value 在下**（`flex-direction: column; gap: 6px`） |
| label 样式 | 13px、`#86909c`（`detail-field-label`） |
| value 样式 | 14px、`#1d2129`（`detail-field-value`） |

```text
┌─ DetailSection: 基础识别 ─────────────────────────────────────────────┐
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   ← 4列 grid         │
│  │规则 ID   │ │规则名称  │ │关联订单  │ │订单类型  │                       │
│  │rule-…   │ │PO1002…  │ │PO2026…  │ │抵/质押   │   ← 上名下达           │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘                       │
└────────────────────────────────────────────────────────────────────────┘
```

**禁止**：ElDescriptions 横向「左 label 右 value」、或 `el-form` 一行两列的表单行布局。

> **已激活策略详情**保持卡片式聚合；卡片**内部**参数字段仍用 `DetailField` 纵向排版。

---

## 3. 页头识别区

```text
{规则名称}   [ {状态Tag} ]          [ ← 返回 ]  [ 编辑 ]  [ 删除 ]
```

容器：`rounded-xl border bg-card p-4`，`lg:flex-row` 左右布局。

| 状态 | 页头动作 |
| :--- | :--- |
| 生效中 | 返回 / 编辑 / 删除 |
| 已失效 | 返回 / 删除 |

---

## 4. 基础识别卡片（DetailSection）

| 字段 | 展示规格 |
| :--- | :--- |
| 规则 ID | UUID |
| 规则名称 | 文本 |
| 关联订单 | 订单号 + 客户名 |
| 订单类型 | 抵/质押 / 监管 |
| 货主 | 姓名 + 脱敏手机 |
| 货物 | 大类/品类/规格/数量 |
| Version | 整数 |

---

## 5. 已激活风控策略详情

以卡片展示每项**运行中**策略，卡片内 `DetailField` 只读：

**策略 1 价格下跌示例**：
- 下跌比例阈值、预警等级（色块+编码）、通知渠道、预警对象、升级策略

**策略 2 抵质押率双控示例**：
- 补仓线 LTV + 等级 + 解除方式
- 平仓线 LTV + 等级 + 解除方式
- 通知渠道、预警对象、升级策略

**策略 3 巡检示例**：
- 巡检人配置列表、预警等级、预警对象

每张卡片标题：`【策略 N：{名称}】 [ 运行中 ]`

> 等级标签由 04 快照渲染；展示编码/名称/颜色，审计 `severity_level_id`。

---

## 6. 未启用策略项

轻量文本列表：

```text
• 解抵/质押/监管超时监控：未启用
• 盘点账实差异告警：未启用
• 贷中风控模型预警：未启用
```

---

## 7. 系统审计卡片（DetailSection）

| 字段 | 展示规格 |
| :--- | :--- |
| 创建人 / 创建时间 | — |
| 更新人 / 更新时间 | — |
| 失效原因 | 仅已失效展示 |

---

## 8. 二次确认

| 操作 | 内容 | 确认按钮 |
| :--- | :--- | :--- |
| 删除 | 删除后不可恢复，关联未处理押品预警将置为无效，确认删除？ | **确认删除** |

---

## 9. Mock 数据

对齐 ASCII PO1002：激活 3 项（跌价 L3、质押率 L3/L4、巡检 L2）。

---

## 10. 自检清单

1. ✅ 详情字段来自字段清单（详情展示=是）
2. ✅ 卡片结构与 ASCII V2.0 一致
3. ✅ **基础识别/系统审计 DetailField 上 label 下 value + 4 列 grid**
4. ✅ 未新增业务规则
