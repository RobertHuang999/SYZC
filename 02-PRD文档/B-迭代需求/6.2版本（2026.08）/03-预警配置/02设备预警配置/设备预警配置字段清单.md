# 设备预警配置字段清单

> **文档定位**：**三件套核心**——物联网 IoT 板块设备预警规则、阈值及通知策略配置字段的唯一定义来源。触发逻辑与状态流转规则由 [设备预警配置业务规则规格.md](./设备预警配置业务规则规格.md) 维护；页面功能与场景参见 [设备预警配置主PRD.md](./设备预警配置主PRD.md)。下游需求文档、原型和关联单据按字段所属层级引用，不重复定义字段规则。  
> **引用规则**：下游需要引用设备预警配置字段时，标注“继承自设备预警配置”；设备主数据及物联状态以物联网 IOT 管理模块为准；引用的预警等级必须关联预警等级字典的稳定标识 `severity_level_id`。  
> **修改原则**：字段定义变化只修改本文件，并同步更新版本和修订记录。  
> **版本**：V4.1 | **更新时间**：2026-09-20 | **责任人**：森云科技（Senyun Technology）  
> **页面范围**：覆盖设备预警配置筛选区、列表页（`P-IOT-CFG-LIST`）、新增规则页（`P-IOT-CFG-ADD`）、编辑规则页（`P-IOT-CFG-EDIT`）和详情展示页（`P-IOT-CFG-DETAIL`）。触发后的预警信息流水与核销由 [02/01 设备预警信息](../../02-预警信息/01设备预警信息/设备预警信息主PRD.md) 负责。

---

## 0. 对象与字段层级说明

本文件根据策略生命周期，将设备预警配置字段划分为四大层级：
1. **策略核心业务字段**：定义规则名称、预警大类、预警子类型、引用的预警等级及关联监控、传感器、门禁或 GPS 物理设备范围。
2. **动态异常与阈值字段**：根据所选预警类型动态展示的异常判定参数（如温湿度区间阈值、气体浓度区间等）。
3. **通知与超时升级字段**：定义首次通知渠道、预警对象、超时未解除的升级预警天数及升级接收责任人。
4. **系统与审计字段**：维护规则生效状态、数据版本号并发控制锁（Version 乐观锁）及全路径操作审计流水。

---

## 一、策略核心业务字段

> 本节用于定义设备预警配置的基本信息与核心控制字段。涵盖筛选区输入项、列表行主数据、新增/编辑表单头部及详情页单据概览。

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 筛选控件与匹配 | 详情展示 | 字段说明与关联规则 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 序号 | 系统生成 | 整数；按当前列表页排序从 1 开始 | 系统自动 | 不适用 | 不涉及 | 是 | 不可筛选 | 不适用 | 当前列表结果的行序号 | 仅为页面展示序号，不作为持久化主键；固定为列表第一列，翻页后重新计算 |
| 规则名称 | 用户填写 | 文本；单行文本输入框，最多输入 50 个字符 | 必填 | 可编辑 | 只读 | 是 | 是（单行文本输入框；模糊匹配） | 是 | 当前设备预警规则的业务名称 | 租户内规则名称不可重复（DEV-R03）；编辑页置灰只读展示；预警触发时固化写入流水快照 |
| 预警类型 | 用户选择 | 下拉枚举：`设备图像识别预警`、`设备物联预警`、`智能挂锁预警`、`人脸门禁预警`、`设备GPS预警` | 必填 | 可编辑 | 只读 | 是 | 是（下拉多选框；精确匹配） | 是 | 配置当前规则所监控的设备预警大类 | 编辑页置灰不可修改（DEV-R06）；选择类型后联动下方动态展示对应子类型、阈值参数与可选设备资产列表 |
| 预警子类型 | 用户选择 | 动态复选框；在已选大类下按对应枚举多选（如温度异常、烟感火警、剪杆破坏、行人入侵等） | 必填 | 可编辑 | 只读 | 否 | 是（级联二级下拉复选框；精确匹配） | 是 | 当前规则监控的具体异常行为或事件子类型 | 仅展示与所选大类相匹配的子类型；同一设备绑定相同子类型具备全局唯一性校验（DEV-R04）；**「xxx设备上线」类子类型须单独成规，禁止与其他子类型混选（DEV-R14）** |
| 预警等级 | 用户选择 | 下拉单选；读取当前租户预警等级字典中状态为“启用”的等级列表（2~20 档） | 必填 | 可编辑 | 可编辑 | 是 | 是（下拉单选框；精确匹配） | 是 | 当前规则命中时赋予给预警流水的严重等级 | 下拉展示等级编码、显示名称与色块预览，提交等级唯一标识 `severity_level_id`（DEV-R10）；修改后对新触发的预警生效 |
| 处置策略 | 用户选择 | 单选卡片组（字段编码 `disposition_mode`）：`触发即结案` (RECORD_ONLY)、`人工解除结案` (ACTION_REQUIRED)、`恢复自动结案` (AUTO_RECOVER) | 必填 | 可编辑 | 可编辑 | 是 | 是（展开筛选区多选框；精确匹配） | 是 | 规则命中后写入设备预警信息的结案路径模式 | 详见【处置策略模式配置规范】(DEV-R15)；三档均写入 02/01 流水快照；仅白名单子类型允许选择恢复自动结案（DEV-R15c）；GPS 进/出围栏、普通限速、怠速滞留、路线偏离、非法拆除禁止 AUTO_RECOVER |
| 仅针对新设备 | 用户选择 | 复选框；勾选即启用全局增量监听；默认为“未勾选” | 选填 | 可编辑 | 可编辑 | 否 | 不可筛选 | 是 | 全局新设备自动监听规则开关 | 仅当子类型**唯一选中**当前大类的「xxx设备上线」时展示（DEV-R14）；勾选后无需手动勾选设备列表，后续新入库设备自动套用；同一大类全局新设备规则租户内仅限 1 条有效规则（DEV-R05）；升级预警配置自动隐藏（DEV-R13） |
| 关联设备范围 | 用户选择 | 设备穿梭框多选；展示设备类型、系统内名称及绑定仓库/具体位置 | 条件必填 | 条件可编辑 | 可编辑 | 是 | 不可筛选 | 是 | 当前预警规则所监控的具体设备资产列表 | 仅展示与所选“预警类型”相匹配且在当前用户仓库管辖范围内的设备（DEV-R12）；未勾选“仅针对新设备”时必填（DEV-R08）；编辑页回显已关联设备并支持增删改 |

---

## 二、动态异常阈值字段

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 筛选控件与匹配 | 详情展示 | 字段说明与关联规则 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 最低阈值 (Min) | 用户填写 | 数字输入框；支持 2 位小数；温度允许负数，湿度 $\ge 0$，二氧化碳 $\ge 0$ | 条件必填 | 条件可编辑 | 条件可编辑 | 是 | 不可筛选 | 是 | 正常安全闭区间的下边界值 | 仅持续型数值子类型展示；严格校验 $T_{\text{min}} < T_{\text{max}}$（DEV-R09）；低于该值触发越限告警 |
| 最高阈值 (Max) | 用户填写 | 数字输入框；支持 2 位小数；湿度 $\le 100 \%RH$ | 条件必填 | 条件可编辑 | 条件可编辑 | 是 | 不可筛选 | 是 | 正常安全闭权间的上边界值 | 仅持续型数值子类型展示；超出该值触发越限告警；列表中合并展示为 `[最低值, 最高值]` |

> **二氧化碳字段口径**：`二氧化碳超限`使用 `ppm`，新增/编辑页分别录入“最低浓度 (Min)”和“最高浓度 (Max)”；列表触发条件展示为 `二氧化碳 < 最低值 或 > 最高值`（默认推荐闭区间为 `[400.00, 1500.00] ppm`），预警触发时实际采集值与阈值一同写入流水快照。

---

## 三、通知与超时升级配置字段

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 筛选控件与匹配 | 详情展示 | 字段说明与关联规则 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 通知渠道 | 用户选择 | 复选框组：`短信`、`邮件` | 选填 | 可编辑 | 可编辑 | 否 | 不可筛选 | 是 | 预警触发时向责任人发送的外部通知途径 | 短信与邮件由用户按需勾选，可不选；预警命中时移动端「设备预警信息」入口自动展示待处置红点，无需配置；未勾选任何渠道时不展示预警对象与升级配置 |
| 预警对象 | 用户选择 | 人员多选穿梭框；读取当前租户及其所有机构下的启用人员列表 | 条件必填 | 条件可编辑 | 条件可编辑 | 否 | 不可筛选 | 是 | 预警触发时接收首次通知的人员责任人 | 仅勾选短信/邮件后展示并必填（DEV-R11）；下发通知前自动校验接收人启用状态，停用账号自动跳过 |
| 升级预警天数 | 用户填写 | 非负整数；单位：天；取值范围 $1 \sim 30$ | 条件必填 | 条件可编辑 | 条件可编辑 | 否 | 不可筛选 | 是 | 初次告警后处于未结案状态时进行升级通知的等待天数 | 仅勾选短信/邮件且开启超时升级时展示并必填；超时升级**仅通过短信通道**逐级上报；勾选“仅针对新设备”时本项隐藏并强制置为 0（DEV-R13） |
| 升级预警对象 | 用户选择 | 人员多选穿梭框；读取当前租户下管理责任人列表 | 条件必填 | 条件可编辑 | 条件可编辑 | 否 | 不可筛选 | 是 | 超时未解除预警时接收升级预警短信的管理人员 | 仅勾选短信/邮件且开启超时升级时展示并必填；仅支持短信下发；发送前校验账号启用状态 |

---

## 四、系统与审计字段

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 筛选控件与匹配 | 详情展示 | 字段说明与关联规则 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 规则状态 | 系统维护 | 枚举：`生效中` (`ACTIVE`)、`停用` (`DISABLED`)、`已失效` (`INACTIVE`) | 系统自动 | 不展示 | 只读 | 是 | 是（下拉单选框；精确匹配） | 是 | 预警规则当前的运行与生效状态 | 仅生效中规则对设备事件生效；列表提供启停用行操作按钮（DEV-C03）；表单内不设状态修改控件 |
| Version | 系统维护 | 整数；初始新增为 1，每次修改递增 | 系统自动 | 不展示 | 只读 | 否 | 不可筛选 | 是 | 规则数据版本号与并发控制锁 | 编辑保存时服务端强校验版本一致性（DEV-R07），防止并发修改覆盖；详情页只读展示当前版本 |
| 创建人 | 系统生成 | 文本；格式为“姓名（所属机构）” | 系统自动 | 不展示 | 只读 | 是 | 不可筛选 | 是 | 创建该预警策略的操作人员账号与姓名 | 服务端根据当前登录身份自动写入，客户端不可传值 |
| 创建时间 | 系统生成 | 日期时间；格式为 `YYYY-MM-DD HH:mm:ss` | 系统自动 | 不展示 | 只读 | 是 | 不可筛选 | 是 | 预警策略记录的创建落库时间 | 以服务端系统时间为准，创建后永久不可修改 |
| 更新人 | 系统生成 | 文本；格式为“姓名（所属机构）” | 系统自动 | 不展示 | 只读 | 是 | 不可筛选 | 是 | 最近一次更新预警规则内容的操作人员 | 每次修改规则、阈值、设备列表或通知对象保存后均自动更新 |
| 更新时间 | 系统生成 | 日期时间；格式为 `YYYY-MM-DD HH:mm:ss` | 系统自动 | 不展示 | 只读 | 是 | 是（日期范围选择器；精确到秒） | 是 | 最近一次更新预警规则的时间 | 以服务端受理操作的时间戳为准；列表默认按本字段降序排列 |
| 操作审计日志 | 系统生成 | 结构体列表；记录操作人、角色、时间、IP、前后数据镜像版本 | 系统自动 | 不展示 | 不展示 | 否 | 不可筛选 | 否 | 记录预警配置全生命周期的操作审计流水 | 包含新增规则、编辑阈值、增删关联设备、启用停用及删除操作的全路径不可篡改留痕 |

---

## 五、展示与引用规则

- **列表展示顺序**：序号、规则名称、预警类型、处置策略、预警等级、关联设备范围、触发条件摘要、规则状态、创建人/创建时间、更新人/更新时间、操作。
- **编辑控制规范**：
  - `规则名称`、`预警类型`、`预警子类型` 在编辑页**强制置灰只读**；
  - `处置策略`、预警等级、关联设备穿梭框、监控阈值区间、通知与升级配置**支持自由编辑修改**；
  - 关联设备穿梭框支持动态增删绑定的实体设备资产。
- **版本生效规则**：配置被修改保存后 Version 递增，**严格不回写**既有未处理预警（DEV-C08）；后续设备事件回调严格按最新规则 Version 匹配并固化新快照；已触发的历史预警流水完整保留触发时刻的规则快照。

---

## 修订记录

| 日期 | 版本 | 变更内容说明 | 影响范围 | 修订人 |
| :--- | :---: | :--- | :--- | :--- |
| 2026-09-08 | V3.7 | 初版 6.2 设备预警配置字段清单发布。 | 列表与表单 | AI PM / 森云科技 |
| 2026-09-16 | V3.9 | 统一字段字典表头规范，汉化交互用语。 | 字段表头 | AI PM / 森云科技 |
| 2026-09-18 | **V4.0** | **标杆排版全面升级（对齐《入库记录》《出库记录》字段清单）**：<br/>1. 规范《0. 对象与字段层级说明》四大层级体系；<br/>2. 统一全表 11 列标准表头规格，补齐详情展示与关联规则；<br/>3. 精炼字段说明文案，消除冗余开发描述，增强可读性。 | 字段清单全文 | AI PM / 森云科技 |
| 2026-09-20 | **V4.1** | 处置策略字段备注补充 GPS 行车事件禁配【恢复自动结案】（三方无恢复回调）。 | `disposition_mode` | AI PM / 森云科技 |
