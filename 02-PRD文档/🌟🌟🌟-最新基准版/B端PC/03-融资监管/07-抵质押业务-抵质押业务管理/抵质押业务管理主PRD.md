# 抵质押业务管理 主PRD

> **版本**：V2.6 | **更新时间**：2026-09-16 | **责任人**：森云科技（Senyun Technology）  
> **读者对象**：研发工程师、测试工程师、金融风控产品经理、现场监管员、商业银行资方评审团队  
> **所属模块**：03-融资监管 / 07-抵质押业务-抵质押业务管理 | **菜单路径**：`融资监管 → 抵质押业务 → 抵质押业务管理`  
> **模板选型**：台账流水与业务流转类（[4-2-2 (输出)台账流水类PRD模板](../../../../05-常用模板/需求处理/4-2-2%20(输出)台账流水类PRD模板.md)）
>
> **文档体系（三件套为唯一真源）**：
>
> | 层级 | 文档 | 职责说明 |
> | :--- | :--- | :--- |
> | **核心** | 本文（主 PRD） | 业务背景、功能范围、系统架构、**页面功能说明（PC/移动端）**、状态机摘要、规则摘要、权限矩阵、验收标准 |
> | **核心** | [抵质押业务管理字段清单](./抵质押业务管理字段清单.md) | 列表、看板、筛选区、全景详情、系统审计快照字段的**唯一定义来源**（标准 11 列矩阵） |
> | **核心** | [抵质押业务管理业务规则规格](./抵质押业务管理业务规则规格.md) | 状态机本体、单据互斥锁、流转表、R/C/P 规则规范与业务时序图的**唯一定义来源** |
> | **子核心** | [操作业务全景导航](./操作业务/README.md) | 存续期 24 项具体行操作模态弹窗/表单输入、返回与特有审计字段清单与规则说明 |
> | *衍生* | 采集资料与规约全集 | 归档于 [采集的资料/](./采集的资料/README.md)，包含 18 个操作规约与 50+ 原始业务截图映射 |
>
> **当前业务开关**：监管订单当前关闭。当前不展示监管订单标签页、卡片或菜单入口，不允许新建监管订单及发起监管订单业务写操作；历史监管订单仅允许按权限只读查询、审计和资料查看。`REGULATORY` 及监管服务相关枚举仅作为历史数据兼容值保留。

---

## 1. 业务背景与监管目的

### 1.1 台账定位
**抵质押业务管理**是森云科技存货监管系统面向商业银行、保理公司、核心借款企业及仓储监管方打造的**在押资产全生命周期履约监管与处置中枢**。

本模块承接上游 [06-抵质押业务办理](../06-抵质押业务-抵质押业务办理/抵质押业务办理主PRD.md) 终审生单后的存续期资产管理，实时监控抵押、质押项下实物存货与标准电子仓单的物理状态、货权状态、估值波动、信贷敞口与物联网（IoT）监控，贯穿从“放款生效 $\rightarrow$ 动态置换/加补仓 $\rightarrow$ 监管巡检 $\rightarrow$ 风险预警处置 $\rightarrow$ 到期解押或司法平仓”的全流程。监管订单不属于当前可运营业务范围。

### 1.2 监管痛点与业务目标
- **杜绝一货多办与数据倒挂（互斥锁机制）**：引入单据级排他互斥锁与资产在途锁，禁止在同一订单上并发发起多项资产变更，避免账实脱节与货值计算错乱；
- **货权与现场实物穿透闭环**：通过物联网智能设备（球机、温湿度、门禁挂锁）实时锁定物理货位，货物发生移库、出库、解押时，系统自动联动摄像头抓拍存证并自动换绑/解绑监控；
- **行情波动与动态抵押率敏捷风控**：接入上海有色网（SMM）、生意社等权威行情源，对未定价货品给予 `未定价 ⚠️` 强警示；通过贷款余额动态冲减与公允货值实时联动，动态压降并呈现真实抵押率；
- **司法合规与全国中仓登联动**：支持电子合同全量线上签署与数字证书认证（CA）存证，仓单发生置换、补仓、出库、移库、解押时，实时调用中仓登 1.3.2 接口完成全国性动产质押登记同步。

---

## 2. 功能范围

### 2.1 包含功能 (In Scope)
- **顶层架构**：PC 端当前仅提供【抵押订单】、【质押订单】两个业务单据标签页；
- **监管订单关闭约束**：不展示监管订单入口、不允许新建或发起监管订单写操作；历史监管订单仅保留按权限只读查询、审计和资料查看，相关兼容枚举不得作为当前运营类型使用；
- **统计看板**：全部单据数、在押单据数、在押货值总额（未定价 ⚠️ 强警示）、存在风险订单数；
- **组合筛选与列表**：单号、货主、状态、担保方式、风险标识、所属仓库、生效时间组合检索，支持表格列自定义与导出；
- **全景详情**：基础信息、资产明细（存货/仓单两标签页切换）、IoT 设备矩阵、风险预警流水、流程时间轴、二维码资产追溯抽屉；
- **存续期核心操作（24 大类）**：
  1. **货物置换**（新旧货值比对，新 $\ge$ 老倒挂阻断，四方电子签章）；
  2. **加/补仓**（同仓合格存货/仓单并入，缺货空态保护）；
  3. **货物出库**（原提货；严禁全单全提、单仓单保留至少 1 个条码底线、单码在途锁）；
  4. **货物移库**（绝对禁止跨仓移库、移库前自动抓拍存证、终审自动换绑 IoT 监控）；
  5. **解除抵/质押**（动态文案映射、全额还本核验、全单打包签章、中仓登注销与设备彻底解绑）；
  6. **风险处置与平仓**（严禁原货主接收买受红线、二元查重建档、所得款冲减借款余额与差额追偿）；
  7. **结算管理与部分结清**（还款金额开区间 $0 < A < \text{余额}$，还款后扣减余额重算降低抵押率）；
  8. **安全线管理**（基准安全线公式、调整审批与跌价告警）；
  9. **贷款结果录入**（放款信息确权录入、激活列表抵押率计算引擎）；
  10. **行业参考价与货物估值**（行情源路由、未定价 ⚠️ 告警、估值单价/整批总价倒算双模式）；
  11. **货物盘点与盘点报告**（仓库权限强校验、逐条/扫码/批量盘点、盘亏触发押品预警）；
  12. **设备管理与查看监控**（多品类硬件矩阵、专属预警开关、实时视频流播放、云台 PTZ 控制盘与现场抓拍）；
  13. **查看风险与风险公示**（未处理/已处理双标签页、订单红标清零闭环、仅已处理可公示、取消公示合规留痕）；
  14. **加工管理**（深加工场景原料领料审批、加工在途监控、新入库产成品货值保全不倒挂）；
  15~24. 扩展涵盖抵质押入库、盘点报告、查看监控、操作记录、查看详情、监管报告、货物异动、日常监管、资料管理等。

### 2.2 不包含功能 (Out of Scope)
- 办理开单选货业务逻辑（由上游 [06-抵质押业务办理](../06-抵质押业务-抵质押业务办理/抵质押业务办理主PRD.md) 承接）；
- 底层多级审批流节点与模板配置（由工作中心流程引擎平台维护）；
- 商业银行核心信贷出账与直接银企直联扣款。

---

## 3. 总体架构与业务流转

### 3.1 总体业务流转全景图

```mermaid
flowchart TD
    subgraph S01["1. 上游单据开立"]
        A1["抵质押业务办理终审生单"] --> A2["普通存货 / 电子仓单 (支持跨仓圈选)"]
        A2 --> A3["生成抵质押业务管理在押单据 (ACTIVE)"]
    end

    subgraph S02["2. 抵质押业务管理中枢"]
        B0["当前可运营标签页: 【抵押订单】 / 【质押订单】"]
        B0 -. "历史只读兼容，不形成入口" .-> B0H["REGULATORY 监管订单历史数据"]
        B0 --> B1["顶层 4 大看板指标 (在押货值含未定价 ⚠️ 警示)"]
        B1 --> B2["订单综合查询台账与行操作"]
    end

    subgraph S03["3. 流程发起排他互斥锁"]
        B2 --> C0{"校验是否存在未完结在途审批?"}
        C0 -- "是且同类操作" --> C1["同类防重/撤销模态弹窗: 提示已有在途任务，支持撤回"]
        C0 -- "是且异类操作" --> C2["排他强阻断: 正在进行【XX】操作，审核前禁止其他操作！"]
        C0 -- "否" --> C3["放行进入各操作业务流"]
    end

    subgraph S04["4. 核心存续期操作流"]
        C3 --> D1["【货物置换】(新值 $\\ge$ 老值倒挂阻断)"]
        C3 --> D2["【加/补仓】(选可用存货/仓单并入在押池)"]
        C3 --> D3["【货物出库】(严禁全提/单码在途锁)"]
        C3 --> D4["【货物移库】(严禁跨仓/自动抓拍/监控换绑)"]
        C3 --> D5["【货物估值】(单价模式 vs 整批总价倒算)"]
        C3 --> D6["【货物盘点】(扫码/实拍/批量/盘亏预警)"]
        C3 --> D7["【解除抵/质押】(贷款结清/四方签章/设备解绑)"]
        C3 --> D8["【风险处置/平仓】(禁原货主/买受人建档/冲减借款)"]
    end

    subgraph S05["5. 资金与风控日常监控"]
        B2 --> E1["【贷款结果录入】(放款反写/激活抵押率分子)"]
        B2 --> E2["【安全线管理】(调整审批/跌价监控)"]
        B2 --> E3["【部分结清】(还款扣余额重算降低抵押率)"]
        B2 --> E4["【日常监管】(GPS围栏打卡/实时相机实拍)"]
        B2 --> E5["【设备管理/查看监控】(IoT矩阵/实时流/云台PTZ)"]
        B2 --> E6["【查看风险/风险公示】(双标签页/订单红标消除闭环)"]
        B2 --> E7["【加工管理】(深加工原料领用/成品货值保全)"]
    end

    subgraph S06["6. 终态流转与熔断"]
        D7 & D8 --> F1["订单终态: 解抵押 / 解质押 (RELEASED)"]
        F1 --> F2["关联 IoT 设备全面彻底解绑"]
        F1 --> F3["中仓登 1.3.2 接口上报质权注销或过户备案"]
        F1 --> F4["列表操作按钮熔断收缩: 仅保留【查看详情】与【操作记录】"]
    end
```

### 3.2 核心并发控制：单据级排他互斥锁
1. **同类任务防重与撤回**：若订单已有待审批的加/补仓申请，经办人再次点击【加/补仓】时，弹出提示并提供【查看详情】与【撤回申请】（仅原发起人或超级管理员可撤回）；
2. **异类任务排他强阻断**：若订单已有【移库】审批在途，经办人点击【货物出库】或【货物置换】等异类操作时，系统直接阻断弹出警告模态弹窗，提示必须等待当前在途审批完结或撤回；
3. **已结案终态熔断**：订单流转至终态（`解抵押` / `解质押`，含平仓结案）后，所有业务办理类按钮全部熔断隐藏，仅保留只读类功能（查看详情、操作记录）。

---

## 4. 页面功能说明 (PC 端与移动端)

### 4.1 PC 端列表与看板页规格
* **路径**：`融资监管 → 抵质押业务 → 抵质押业务管理`
* **顶层业务大类标签页与操作按钮**：
  - 【抵押订单】：默认激活高亮（橙黄色背景），展示该大类下固定抵押与浮动抵押单据；
  - 【质押订单】：展示静态质押与流动质押单据；
  - 【监管订单】：当前关闭，不形成标签页、卡片或菜单入口；历史监管订单仅通过授权历史查询链路只读查看；
  - 右上角全局按钮：【导出为Excel】（橙黄色操作按钮，导出当前筛选条件下的全量订单明细）。
* **四大统计看板卡片 (随标签页切换实时联动刷新)**：
  1. `全部单据`：当前选中业务标签页下受权限过滤的单据总笔数；
  2. `进行中单据数`：统计当前处于履约监管进行中的有效单据笔数，随选中标签页自适应呈现为“抵押中”或“质押中”；
  3. `货物评估价值总额(万)`：当前选中业务标签页下所有有效订单的货物评估价值累计总和（万元），数值右侧附信息提示图标 `ℹ️`，鼠标悬浮（Hover）提示价值核算与未定价说明；
  4. `存在风险`：当前选中业务标签页下存在未结案待处置风险预警的单据笔数，点击卡片快捷联动筛选【风险预警 = 有风险】并执行查询。
* **组合查询筛选区 (两行)**：
  - 第一行：`抵押单号/质押单号`（单行文本输入框）、`货主名称/标识`（单行文本输入框）、`抵押状态/质押状态`（下拉单选框：全部/抵押中/解抵押）、`抵押方式/质押方式`（下拉单选框：全部/固定/浮动/静态/流动）、`风险预警`（下拉单选框：全部/有风险/无风险）；
  - 第二行：`货品名称`（下拉单选框）、`流程相关机构`（单行文本输入框）、`抵押时间/质押时间`（日期范围选择器）、【查询】（高亮主按钮）、【重置】（次级平白按钮）。
* **表格展示与列定义**：
  - 包含序号、单据编号、货主主体、标的物摘要、确权时间、业务状态-业务方式复合列、贷款信息三行结构、安全线、行业参考价、评估总值、实时抵押率、关联流程、操作列；
  - 状态复合列：如 `抵押中-浮动抵押`、`解抵押-固定抵押`。
* **行操作列规范**：
  - 常驻外露 6 项：【查看详情】、【操作记录】、【资料管理】、【部分结清】、【日常监管】、【监管报告】；
  - 折叠展开项：点击【更多操作 ▾】呼出下拉浮层，包含其余 18 项存续期业务操作。

### 4.2 移动端页面功能说明
1. **列表卡片与 6 宫格面板**：
   - 卡片头部展示单据编号、货主名称与右上角高亮抵押率卡片；
   - 6 宫格展示贷款金额、期数、余额、货物评估价值、行业参考价、货值安全线；
   - 底部提供【查看详情】、【操作记录】与【更多操作 ▾】。
2. **【更多操作 ▾】4 列网格全景抽屉**：
   - 点击呼出底部半屏抽屉，以 4 列网格均分形式平铺展示所有已赋权的业务功能，包括货物置换、补仓、出库、解押、盘点、估值、设备监控等。

---

## 5. 核心业务规则追溯索引表

| 规则类型 | 规则编码与业务名称 | 规则核心逻辑摘要 | 关联业务操作 | 交互表现与异常反馈 |
| :--- | :--- | :--- | :--- | :--- |
| **并发控制** | 【单据排他互斥锁与防重复规则】(DZY-R01) | 订单存在在途审批时严禁发起异类业务变更；同类业务支持查看在途与撤回申请 | 置换、补仓、出库、移库、还款、解押 | 异类操作阻断提示模态弹窗；同类操作提示在途并支持撤回 |
| **业务风控** | 【货物置换新旧价值不倒挂规则】(DZY-R02) | 换入新质物必须为同货主同仓合格资产，且换入货物总估值必须 $\ge$ 换出货物总估值 | 01-货物置换 | 价值倒挂时提交按钮置灰禁用并红色提示拦截 |
| **业务风控** | 【货物出库严禁全额提空规则】(DZY-R03) | 货物出库仅支持部分提货，在押数量严禁全部提空 | 03-货物出库 | 勾选全单时弹出阻断模态弹窗，引导走解除抵/质押流程 |
| **业务风控** | 【电子仓单保留至少一个条码资产包规则】(DZY-R04) | 电子仓单出库时，单张仓单项下必须保留至少 1 个有效二维码资产包 | 03-货物出库 | 仓单拟空心化时即刻阻断提交 |
| **业务风控** | 【货物移库严禁跨仓调拨规则】(DZY-R05) | 移库仅限同仓内调整货位，移入仓库字段只读锁定等于移出仓库；提审自动抓拍现场存证 | 08-货物移库 | 移入仓库不可下拉选择；终审后自动换绑物联监控 |
| **业务风控** | 【风险处置与平仓严禁原货主接收规则】(DZY-R06) | 平仓买受受让方统一社会信用代码严禁等于借款原货主，防范逃废债 | 10-平仓 | 输入原货主信用代码时红色边框报错并阻断提交 |
| **业务风控** | 【结算管理部分结清金额严格开区间规则】(DZY-R07) | 还本金额必须严格满足 $0 < A_{\text{还款}} < \text{当前贷款本金余额}$ | 17-部分结清 | 超额或全额还款拦截，提示走全面解押解控 |
| **业务风控** | 【货物盘点账实差异与盘亏联动预警规则】(DZY-R08) | 现场实盘数量小于账面数量判定为盘亏，提交后自动向押品预警写入告警流水并点亮红标 | 06-货物盘点 | 列表订单号旁点亮红色 🔴 风险标签，需补仓后消除 |
| **业务风控** | 【未定价商品公允价值缺失风险警示规则】(DZY-R09) | 缺失行情指导价时单价显示 `未定价 ⚠️`，看板总额旁展示警告图标并提示排除未定价货品 | 05-货物估值 | 鼠标悬浮展示警示提示，不计入货值总额 |
| **业务风控** | 【安全线管理与动态抵押率试算规则】(DZY-R10) | 抵押率 = (贷款余额 / 货物总估值) * 100%；放款录入后激活；跌破安全线触发补仓预警 | 14-放款, 18-安全线 | 抵押率动态重算并在详情提供历次变动走势 |
| **业务风控** | 【加工管理深加工货值保全规则】(DZY-R11) | 原料领用出库加工施加在途锁，产成品二次入库总值不得低于原领用原料抵押值 | 24-加工管理 | 超期未入库或价值倒挂触发严重风控预警 |
| **业务风控** | 【日常监管时空防伪与原生相机调用规则】(DZY-R12) | 移动端 GPS 坐标距离监管仓超过 200 米阻断打卡；强制调用原生相机并压入时空水印 | 21-日常监管 | 底层禁用本地相册选取，彻底杜绝虚假巡仓 |
| **业务风控** | 【终态订单操作行收缩熔断与设备解绑规则】(DZY-R13) | 订单解押或平仓后状态转为 RELEASED，业务办理类按钮全面熔断隐藏，设备彻底解绑 | 04-解押, 10-平仓 | 操作列仅保留【查看详情】与【操作记录】 |
| **系统约束** | 【乐观锁并发版本控制】(DZY-C01) | 订单与资产表维护版本号，版本不一致阻断更新 | 全局操作 | 浮窗提示：“数据已被他人更新，已自动刷新” |
| **系统约束** | 【幂等性提交防护机制】(DZY-C02) | 表单提交携带唯一请求标识，服务端防重缓存 60 秒 | 全局表单 | 拦截连击与网络重发导致的重复处理 |
| **系统约束** | 【不可变存证快照固化】(DZY-C03) | 终审通过后生成 SHA-256 哈希存证，后续字典变更不回写历史 | 全局审批 | 详情页提供司法存证哈希校验串与出证凭单 |
| **权限控制** | 【严格自审禁止红线】(DZY-P01) | 经办人与审批人相同时，强制隐藏通过/驳回按钮 | 审批流 | 杜绝本人自审本人单据 |
| **权限控制** | 【实体仓库数据权限隔离】(DZY-P02) | 现场监管操作强制校验账号绑定的实体仓库数据权限 | 盘点、移库、监管 | 无权仓库直接拦截并提示越权访问 |
| **权限控制** | 【全路径审计留痕规范】(DZY-P03) | 全量操作记录时间戳、IP、操作人账号及前后数据镜像快照 | 操作记录 | 提供完整操作记录时间轴回溯 |

---

## 6. 权限与安全控制 (RBAC 矩阵)

| 角色标识 | 角色名称 | 核心页面与操作权限 | 仓库数据权限约束 |
| :--- | :--- | :--- | :--- |
| `R-ORDER-OPER` | 业务经办人 | 列表查询、查看详情、发起置换/加补仓/出库/移库/估值申请 | 受组织架构与借款主体授权隔离 |
| `R-WH-SUPERVISOR`| 现场监管员 | 日常监管打卡、现场盘点、出入库现场扫码核销、查看监控与云台 PTZ | **强制校验**：必须分配订单所在实体监管仓数据权限 |
| `R-RISK-AUDIT` | 银行/资方风控员 | 审批中心审核各变更申请、调整安全线、审批风险处置 | 受出资金融机构数据范围隔离，**严格遵守自审禁止** |
| `R-FIN-SETTLE` | 财务结算专员 | 录入贷款结果、办理部分结清还款、对账流水导出 | 仅能维护已授权的资金结算账户与借据凭单 |
| `R-RISK-MGR` | 风险管理总监 | 审批平仓方案、对外发布风险公示、合规取消公示 | 全租户风险管理权限，取消公示强制录入理由 |
| `R-ORDER-ADMIN` | 系统超级管理员 | 异常流程撤销补偿、中仓登接口重发、全路径审计追溯 | 超级权限，所有敏感操作全程留存不可变快照 |

---

## 7. 验收标准与测试用例要点

1. **当前两标签页与看板联动**：
   - 仅展示并可切换【抵押订单】与【质押订单】，看板数值、列表列名（抵押方式/质押方式）与行操作文案（解除抵押/解除质押）实时准确切换；
   - 监管订单不展示标签页、卡片或菜单入口；历史监管订单通过旧链接访问时仅可只读查询、审计和资料查看，所有写操作均被服务端拒绝；
   - 存在未定价商品时，看板货值旁正确渲染警告图标 `⚠️`，鼠标悬停展示规范文案。
2. **互斥锁与并发拦截**：
   - 当订单存在在途审批时，点击其他变更操作，必须 100% 弹出排他阻断提示模态弹窗，禁止打开新申请；
   - 同类操作再次点击时，正确提供【查看审批】与【撤回申请】入口。
3. **风控红线硬拦截**：
   - 货物置换输入新货物总值小于换出货物时，提交按钮置灰禁用或弹出倒挂拦截阻断；
   - 货物出库勾选全部在押存货时，拦截并弹出全额提货阻断模态弹窗，引导办理全面解押；
   - 货物移库表单中，移入仓库必须置灰禁用且等于移出仓库；
   - 风险处置买受人统一社会信用代码输入原货主统一信用代码时，系统必须强校验拦截；
   - 部分结清还款金额录入 0 元或大于等于当前贷款余额时，系统必须强校验拦截。
4. **终态收缩与设备解绑**：
   - 订单解押终审通过后，列表行操作立即收缩熔断，仅展示【查看详情】与【操作记录】；
   - 详情中 IoT 设备状态显示已解绑，中仓登登记系统显示已注销。

---

## 8. 核心行操作独立三件套规格索引 (24 大操作业务)

系统严格按照真实行操作菜单项，将 **24 大核心行操作** 全面解耦，分别建立独立的需求三件套。各操作规格书完全继承并服从本主 PRD 定义的全局状态机与排他互斥锁框架：

👉 **全景导航入口**：[【操作业务全景导航 README.md】](./操作业务/README.md)

1. [01-货物置换](./操作业务/01-货物置换/货物置换主PRD.md)
2. [02-货物补仓](./操作业务/02-货物补仓/货物补仓主PRD.md)
3. [03-货物出库](./操作业务/03-货物出库/货物出库主PRD.md)
4. [04-解抵质押](./操作业务/04-解抵质押/解抵质押主PRD.md)
5. [05-货物估值](./操作业务/05-货物估值/货物估值主PRD.md)
6. [06-货物盘点](./操作业务/06-货物盘点/货物盘点主PRD.md)
7. [07-盘点报告](./操作业务/07-盘点报告/盘点报告主PRD.md)
8. [08-货物移库](./操作业务/08-货物移库/货物移库主PRD.md)
9. [09-查看监控](./操作业务/09-查看监控/查看监控主PRD.md)
10. [10-平仓](./操作业务/10-平仓/平仓主PRD.md)
11. [11-查看风险](./操作业务/11-查看风险/查看风险主PRD.md)
12. [12-设备管理](./操作业务/12-设备管理/设备管理主PRD.md)
13. [13-操作记录](./操作业务/13-操作记录/操作记录主PRD.md)
14. [14-贷款结果录入](./操作业务/14-贷款结果录入/贷款结果录入主PRD.md)
15. [15-查看详情](./操作业务/15-查看详情/查看详情主PRD.md)
16. [16-监管报告](./操作业务/16-监管报告/监管报告主PRD.md)
17. [17-部分结清](./操作业务/17-部分结清/部分结清主PRD.md)
18. [18-安全线管理](./操作业务/18-安全线管理/安全线管理主PRD.md)
19. [19-货物异动](./操作业务/19-货物异动/货物异动主PRD.md)
20. [20-抵质押入库](./操作业务/20-抵质押入库/抵质押入库主PRD.md)
21. [21-日常监管](./操作业务/21-日常监管/日常监管主PRD.md)
22. [22-资料管理](./操作业务/22-资料管理/资料管理主PRD.md)
23. [23-风险公示](./操作业务/23-风险公示/风险公示主PRD.md)
24. [24-加工管理](./操作业务/24-加工管理/加工管理主PRD.md)

---

## 9. 修订记录

| 日期 | 版本 | 变更内容 | 责任人 |
| :--- | :---: | :--- | :--- |
| 2026-09-10 | V1.0 | 梳理抵质押业务管理初始需求规格 | 森云科技 |
| 2026-09-11 | V1.1 | 建立三标签页架构与行操作子清单初稿 | 森云科技 |
| 2026-09-11 | V2.0 | 深度重构全面对齐 6.2 标杆 PRD 规范：建立完整的 R01~R13 业务规则与 RBAC 矩阵映射 | 森云科技 |
| 2026-09-11 | V2.1 | 关闭监管订单当前运营链路：监管订单降为历史只读兼容数据 | 森云科技 |
| 2026-09-11 | V2.2 | 全面补齐各核心行操作子业务 Mermaid 流程图与规则闭环 | 森云科技 |
| 2026-09-11 | V2.5 | 100% 严丝合缝 24 大操作业务一对一三件套全面定稿 | 森云科技 |
| 2026-09-16 | V2.6 | 全面升级规则语义自解释编码、建立核心业务规则追溯表、汉化所有交互控件术语并完善三件套对齐 | 森云科技 |
