# 操作记录字段清单

> **业务场景**：全流程操作留痕 / 敏感业务审计 / 数据版本差异追溯  
> **版本**：V1.0 定稿版 | **更新时间**：2026-09-11 | **责任人**：产品团队

---

## 一、操作记录字段 (14 维标准字典)

| 字段名称 | 所属对象 | 数据层级 | 字段分组 | 字段来源 | 数据类型 | 取值、单位与精度 | 必填规则 | 列表展示 | 可筛选 | 表单交互 | 详情展示 | 字段说明与校验规则 | 敏感与脱敏/备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **操作流水号** | 审计日志 | 主表 | 基础识别 | 系统生成 | 文本 | 字符串，格式 `LOG+YYYYMMDD+6位流水` | 系统自动 | 是 (时间轴) | 是 (模糊) | 只读 | 是 (标题) | 全局唯一审计流水单号 | 否 |
| **关联抵质押单号** | 审计日志 | 主表 | 基础识别 | 订单快照 | 文本 | 19位雪花单据编号 | 系统自动 | 隐藏 | 否 | 只读 | 是 (顶栏) | 关联主订单编号 | 否 |
| **操作行为类型** | 审计日志 | 主表 | 业务属性 | 系统捕获 | 枚举 | `货物置换`、`加补仓`、`货物出库`、`货物移库`、`部分结清`、`安全线调整`、`解抵质押`、`平仓` | 系统自动 | 是 (时间轴) | 是 (下拉) | 只读 | 是 (网格) | 执行的业务动作类别 | 否 |
| **操作人姓名工号**| 审计日志 | 主表 | 人员信息 | 登录会话 | 文本 | 长度 $\le 50$ 字符，如 `张伟 (0089)` | 系统自动 | 是 (时间轴) | 是 (精准) | 只读 | 是 (网格) | 经办操作人真实身份 | 否 |
| **操作人角色** | 审计日志 | 主表 | 人员信息 | RBAC系统 | 文本 | 如 `资方风控主管`、`现场监管员` | 系统自动 | 是 (时间轴) | 是 (下拉) | 只读 | 是 (网格) | 触发操作时所处业务角色 | 否 |
| **操作时间戳** | 审计日志 | 主表 | 时间属性 | 系统生成 | 日期时间 | 格式 `YYYY-MM-DD HH:mm:ss` | 系统自动 | 是 (时间轴) | 是 (范围) | 只读 | 是 (网格) | 服务器精确到秒的时间戳 | 否 |
| **客户端IP** | 审计日志 | 主表 | 网络审计 | 客户端请求 | 文本 | IPv4/IPv6 地址 | 系统自动 | 隐藏 | 否 | 只读 | 是 (网格) | 操作人所用网络出口 IP | 否 |
| **关联审批单号** | 审计日志 | 主表 | 关联业务 | 流程中心 | 文本 | 审批任务编号，如 `APPR-ZH-001` | 系统自动 | 是 (时间轴) | 是 (精准) | 只读 | 是 (网格) | 驱动此操作的底层审批单号 | 支持Deep link |
| **操作摘要说明** | 审计日志 | 主表 | 业务属性 | 系统拼接 | 文本 | 长度 $\le 200$ 字符 | 系统自动 | 是 (时间轴) | 否 | 只读 | 是 (网格) | 人类可读的行为摘要陈述 | 否 |
| **变更前数据镜像**| 审计日志 | 子表 | 版本快照 | 系统底层 | JSON | 完整 JSON 结构快照 | 系统自动 | 隐藏 | 否 | 只读 | 是 (Diff区) | 业务发生前数据版本全集 | 不可变快照 |
| **变更后数据镜像**| 审计日志 | 子表 | 版本快照 | 系统底层 | JSON | 完整 JSON 结构快照 | 系统自动 | 隐藏 | 否 | 只读 | 是 (Diff区) | 业务发生后数据版本全集 | 不可变快照 |

---

## 二、不可变快照与司法存证说明

| 快照节点 | 快照固化字段 | 原始数据源 | 目的与合规依据 |
| :--- | :--- | :--- | :--- |
| **所有写操作落账瞬间** | 前后版本完整 JSON、操作人、IP、时间戳、关联审批任务、操作结果 | 拦截器 Filter、分布式事务监听 | 形成物理只读、严禁删改的金融级审计日志，满足商业银行稽核与法律诉讼举证要求 |
