# 查看监控字段清单

> **业务场景**：远程视频监控拉流 / 云台转向变焦 / 现场一键抓拍存证  
> **版本**：V1.0 定稿版 | **更新时间**：2026-09-11 | **责任人**：产品团队

---

## 一、查看监控字段 (14 维标准字典)

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 筛选控件与匹配 | 详情展示 | 字段说明与关联规则 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **设备编码** | IoT平台 | 文本；字符串，如 `CAM-WX-01-088` | 系统自动 | 只读 | 只读 | 是 (第1列) | 是 (模糊) | 是 (标题) | 摄像头全局唯一编号 | 否 |
| **关联抵质押单号** | 订单快照 | 文本；19位雪花单据编号 | 系统自动 | 只读 | 只读 | 隐藏 | 不可筛选 | 是 (顶栏) | 监控所属在押主订单 | 否 |
| **直视货位编码** | 绑定映射 | 文本；货位号，如 `A-01-01` | 系统自动 | 不适用 | 不涉及 | 是 (第2列) | 是 (精准) | 是 (网格) | 该摄像头直视监控的物理货位 | 否 |
| **设备运行工况** | 实时心跳 | 枚举；`在线`、`离线`、`故障` | 系统自动 | 只读 | 只读 | 是 (第3列) | 是 (下拉) | 是 (状态Tag) | 硬件心跳在线状态；离线不可播放 | 绿在红离 |
| **流协议类型** | 流媒体服务 | 枚举；`WebRTC`、`HLS`、`FLV` | 系统自动 | 是 | 是 | 隐藏 | 不可筛选 | 是 (网格) | 默认 WebRTC 低延迟拉流 | 延迟 < 500ms |
| **视频清晰度** | 用户选择 | 枚举；`标清 (480P)`、`高清 (720P)`、`超清 (1080P)` | 必填 | 不适用 | 不涉及 | 隐藏 | 不可筛选 | 是 (网格) | 播放分辨率画质 | 否 |
| **实时下行码率** | 流媒体服务 | 数字；单位：kbps，如 `2048 kbps` | 系统自动 | 只读 | 只读 | 隐藏 | 不可筛选 | 是 (网格) | 实时数据流下行吞吐量 | 否 |
| **PTZ步长档位** | 用户选择 | 整数；$1 \sim 8$ 档，默认 4 档 | 必填 | 不适用 | 不涉及 | 隐藏 | 不可筛选 | 是 (控制台) | 云台转动速度灵敏度 | 否 |
| **抓拍图片编码** | 系统生成 | 文本；字符串，格式 `ZP+YYYYMMDD+6位流水` | 系统自动 | 只读 | 只读 | 隐藏 | 不可筛选 | 是 (网格) | 抓拍照片唯一文件编号 | 否 |
| **抓拍存证快照** | 视频截屏 | 图片；JPG/PNG，内嵌精确毫秒时钟防伪水印 | 必填 | 不适用 | 不涉及 | 隐藏 | 不可筛选 | 是 (相册区) | 司法存证级现场高清抓拍截图 | 司法有效证据 |

---

## 二、不可变快照与司法存证说明

| 快照节点 | 快照固化字段 | 原始数据源 | 目的与合规依据 |
| :--- | :--- | :--- | :--- |
| **一键抓拍存证** | 图片二进制数据、毫秒时间戳、摄像头编码、货位号、操作员工号、网络 IP、SHA-256 哈希 | 客户端截屏组件、流媒体安全水印层 | 固化特定时刻质押物在库实时监控画面，防范质押物被私自挪移 |
