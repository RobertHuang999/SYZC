# 加工管理字段清单

> **业务场景**：原料领用深加工 / 加工在途倒计时监控 / 产成品入库核销 / 货值保全  
> **版本**：V1.0 定稿版 | **更新时间**：2026-09-11 | **责任人**：产品团队

---

## 一、加工管理字段 (14 维标准字典)

| 字段名称 | 字段来源 | 取值说明 | 必填性 | 新增页 | 编辑页 | 列表展示 | 筛选控件与匹配 | 详情展示 | 字段说明与关联规则 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **加工任务编号** | 系统生成 | 文本；字符串，格式 `JG+YYYYMMDD+6位流水` | 系统自动 | 只读 | 只读 | 是 (第1列) | 是 (模糊) | 是 (标题) | 加工业务唯一审批流水号 | 否 |
| **关联抵质押单号** | 订单快照 | 文本；19位雪花单据编号 | 系统自动 | 只读 | 只读 | 隐藏 | 不可筛选 | 是 (顶栏) | 关联在押主订单编号 | 否 |
| **领用原料品名** | 仓储在押 | 文本；长度 $\le 100$ 字符，如 `热轧卷板` | 必填 | 不适用 | 不涉及 | 是 (第2列) | 是 (下拉) | 是 (网格) | 拟出仓加工的原在押材料 | 否 |
| **原料领用数量** | 用户填写 | 数字；3位小数；附单位，如 `100.000 吨` | 必填 | 是 | 是 | 是 (第3列) | 不可筛选 | 是 (网格) | 本次投入加工的原料净重 | 核心数量 |
| **原料金额估值** | 订单台账 | 金额；单位：元；保留 2 位小数 | 系统自动 | 不适用 | 不涉及 | 是 (第4列) | 不可筛选 | 是 (网格) | 原材料在押账面估值总额 | 货值基准 |
| **预期成品品名** | 用户填写 | 文本；长度 $\le 100$ 字符，如 `镀锌薄板` | 必填 | 是 | 是 | 隐藏 | 不可筛选 | 是 (网格) | 拟产出的深加工产成品 | 否 |
| **约定得品率** | 用户填写 | 百分比；保留 2 位小数，如 `95.00%` | 必填 | 是 | 是 | 隐藏 | 不可筛选 | 是 (网格) | 行业公允工艺产出率 | 否 |
| **加工截止期限** | 用户选择 | 日期；格式 `YYYY-MM-DD`，默认 $\le 15$ 天 | 必填 | 是 | 是 | 是 (第5列) | 是 (范围) | 是 (网格) | 逾期未交割将触发高危预警 | 倒计时监控 |
| **产成品入库数量** | 用户填写 | 数字；3位小数；附单位 | 条件必填 | 是 | 是 | 隐藏 | 不可筛选 | 是 (网格) | 生产完成实际验收交库数量 | 否 |
| **产成品公允货值** | 系统计算 | 金额；单位：元；保留 2 位小数 | 系统自动 | 不适用 | 不涉及 | 是 (第6列) | 不可筛选 | 是 (网格) | 成品单价 $\times$ 成品数量；必须 $\ge$ 原料估值（DZY-R24） | 货值保全红线 |
| **加工任务状态** | 流程引擎 | 枚举；`待审批`、`加工在途`、`核销待审`、`已完工核销`、`逾期报警` | 系统自动 | 只读 | 只读 | 是 (第7列) | 是 (下拉) | 是 (状态Tag) | 全生命周期闭环状态 | 否 |
| **成品赋码二维码** | 系统生成 | 文本；二维码编码，如 `CP-20260925-01` | 系统自动 | 不适用 | 不涉及 | 隐藏 | 不可筛选 | 是 (标签区) | 产成品新并入在押资产包防伪标签 | 物理挂牌 |

---

## 二、不可变快照与司法存证说明

| 快照节点 | 快照固化字段 | 原始数据源 | 目的与合规依据 |
| :--- | :--- | :--- | :--- |
| **成品核销入库落账** | 原料领用明细、加工车间证明、产成品检验入库单、产成品公允估值、核销审批签名、时间戳 | 车间加工单、现场过磅质检报告、资方审批流 | 证明质押财产形态合法转化且担保价值未受贬损，确保金融机构担保物权优先受偿权不受影响 |
