# 加工管理 业务规则规格

> **文档定位**：加工管理/加工记录模块状态机、动作约束、前置校验规则、计算公式、货权继承算法、原子事务与并发防重规则的**唯一定义来源**。主 PRD、字段清单及端侧原型只引用本文件规则 ID 与流程章节，不重复定义规则本体。
>
> **模块类型**：业务单据 / 流转执行类（审批流挂载通用审批任务，仓储现场执行入库核销）
>
> **参考文档**：[《加工管理主PRD》](./加工管理主PRD.md)、[《加工管理字段清单》](./加工管理字段清单.md)、[《14-货品管理字段清单》](../../08-配置管理/14-货品规格管理-货品管理/货品管理字段清单.md)
>
> **版本**：V3.1 | **更新时间**：2026-09-09 | **责任人**：森云科技（Senyun Technology）

---

## 一、单据与能力定位

| 项目 | 内容 |
| :--- | :--- |
| **单据层级** | **【第2层-订单/执行控制层】**（库存业务层——加工记录控制单据） |
| **核心职责** | 规范存货在仓储监管期间因切割、拆分、熔炼、组装等产生的加工业务，锁定投入库存，记录实际消耗与余量释放，入库产出存货并生成新存货条码，实现抵/质押货权的无缝继承与全路径追溯。 |
| **单据来源** | 业务操作人员或仓库监管人员在 PC 端或移动端（App/PDA）点击【新增加工】发起。 |
| **上游依赖** | **货主档案**：提供货主主体 ID、名称和数据权限；<br/>**可用库存明细**：提供库存批次/条码、仓库/库房/分区、可用数量和货权状态；<br/>**抵押单、质押单**：提供有效单据 ID、货权状态和物料清单，业务规则由下游融资单据维护；<br/>**货品基础信息**：读取大类、品类、规格、计量单位和参考单价（继承自 [14-货品管理字段清单](../../08-配置管理/14-货品规格管理-货品管理/货品管理字段清单.md)）。 |
| **下游影响** | **库存锁定与扣减**：提交时锁定投入库存流水，完成入库时原子扣减实际消耗库存；<br/>**余量释放**：完成时自动释放未消耗余量，单据作废时全额释放投入锁定库存；<br/>**产出入库与条码**：生成产出库存并由系统生成全局唯一新存货条码；<br/>**货权继承**：更新原抵质押单物料清单（原条码置为已出库，新条码追加入单）；<br/>**全链路审计**：写入全生命周期操作与库存审计流水。 |
| **审核流** | **【走系统审批中心任务】**：创建单据点击【提交审核】后，进入系统通用审批任务流程，本页面不体现审批中过程态；审批通过后单据正式进入加工记录列表（状态为“未完成”），现场监管员可执行【完成加工并入库】或【作废】。 |

---

## 二、数量与金额口径隔离表

| 口径名称 | 存在位置 | 业务含义与计算公式 | 约束与边界条件 |
| :--- | :--- | :--- | :--- |
| **一级计划投入量** | 投入明细（头部汇总） | 同一规格（大类+品类+规格）下各货位计划投入量之和 | 必须等于同规格各货位计划量之和；公式：$\text{一级计划量} = \sum \text{二级计划量}$ |
| **计划投入数量** | 投入明细（货位行） | 针对特定库存批次/货位填写的计划投入数量 | $0 < \text{计划投入数量} \le \text{提交时实时可用库存}$ |
| **锁定数量** | 完成加工弹窗（实际投入） | 取自申请审批通过的计划投入量，作为现场消耗依据 | 系统自动带出只读显示，带计量单位 |
| **实际消耗数量** | 完成加工弹窗/详情投入 | 完成加工时填写的实际物理消耗数量 | $0 < \text{实际消耗数量} \le \text{锁定数量}$（严禁为 0；某条完全未使用需作废单据） |
| **自动释放数量** | 完成加工弹窗/详情投入 | 完成加工时系统自动释放的库存锁定量 | 公式：$\text{自动释放数量} = \text{锁定数量} - \text{实际消耗数量}$；详情表头为“释放数量” |
| **预计产出数量** | 产出明细（行级） | 申请时填写的预计产出数量 | $\text{预计产出数量} > 0$；提交审核后产出 SKU 结构强行冻结 |
| **实际产出数量** | 完成加工弹窗/详情产出 | 完成加工时填写的实际产出入库物理数量 | $\text{实际产出数量} > 0$；允许大于或小于预计产出数量 |
| **投入货品品类数** | 新增页投入明细底部 | 新增页已添加的投入货品去重规格种数 | 系统自动汇总；格式：`投入货品品类数：{N}` |
| **投入货品总数量** | 新增页投入明细底部 | 新增页全部明细行计划投入数量算术累加之和 | 系统自动汇总；格式：`投入货品总数量：{M}` |
| **预计产出总金额** | 新增页产出明细底部 | $\sum (\text{预计产出数量} \times \text{行业参考单价})$ | 仅汇总有价行；展示格式：`¥{金额}`；缺价提示但不阻断提交 |
| **实际产出总金额** | 完成加工弹窗右下角 | $\sum (\text{实际产出数量} \times \text{行业参考单价})$ | 仅汇总有价行；未输入数量或缺价显示 `--`，不阻断完成入库 |
| **加工损耗** | 完成加工弹窗/已完成详情 | 手动录入的实际物理损耗百分比；范围 $0\% \sim 100\%$ | 仅做整单记录留痕，严禁参与库存扣减与货值/安全线计算 |

---

## 三、状态定义

> 本页面状态严格收敛为 3 态，审批流在系统通用审批中心流转，本页面不展示“审批中/已驳回/已撤回”等审批中间态。

| 状态名称 | 枚举值 (`Enum`) | 业务含义 | 是否终态 | 进入条件 | 离开条件 |
| :--- | :--- | :--- | :---: | :--- | :--- |
| **未完成** | `PENDING_COMPLETION` | 申请已在审批中心审核通过，计划投入库存保持锁定，等待现场监管员登记实际加工结果 | 否 | 系统审批任务流“审批通过”节点执行成功 | 完成加工并入库成功 / 授权人员执行作废 |
| **已完成** | `COMPLETED` | 实际消耗扣减、余量释放、产出入库、新条码生成及货权继承事务执行成功，单据锁定 | **是** | 完成加工原子事务整体执行成功 | — （终态不可逆，不支持业务冲正或作废；详情右上角展示绿色圆形印章） |
| **作废** | `VOIDED` | 未完成状态下授权人员录入作废原因执行作废，计划投入库存全额解锁释放 | **是** | 授权人员在【作废确认】弹窗输入作废原因并确认 | — （终态留痕；详情右上角展示灰色圆形印章） |

---

## 四、状态流转图

```mermaid
stateDiagram-v2
    [*] --> 审批中心任务 : 提交审核 (校验通过 + 锁定计划投入库存)
    审批中心任务 --> 审批中心任务 : 审批流转中 (本页面不体现)
    审批中心任务 --> [*] : 审批驳回 / 撤回 (审批中心终止，释放锁定库存)
    审批中心任务 --> 未完成 : 审批通过 (流转至加工记录列表，保持库存锁定)

    未完成 --> 已完成 : 完成加工并入库 (原子扣减消耗 + 释放余量 + 产出入库 + 生成新条码 + 货权继承)
    未完成 --> 未完成 : 入库事务失败 (全局回滚 + 保持未完成)
    未完成 --> 作废 : 授权作废 (输入500字原因 + 全额释放投入锁定库存)

    已完成 --> [*] : 业务终态 (绿色印章，只读追溯)
    作废 --> [*] : 业务终态 (灰色印章，释放库存留痕)
```

---

## 五、状态流转表（核心规则矩阵）

| 当前状态 | 触发动作 | 前置校验条件 | 结果状态 | 弹窗与交互口径 | 后置级联影响 | 失败阻断提示 (Toast) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 未提交 | 提交审核 | R01: 加工日期 $\le$ 当前时间<br/>R02: 必选货主/类型<br/>R03: 普通货仅选“存货中”可用库存<br/>R04: 抵质押货关联有效单据且过滤仓单货<br/>R05: 投入货物满足“三同”<br/>R06: $0 < \text{计划量} \le \text{实时可用量}$<br/>R07: 预计产出量 $> 0$<br/>R08: 备注 $\le 140$ 字 | 审批中心受理（通过后进入未完成） | 新增页右上角【提交审核】按钮，无二次确认弹窗 | ① 生成 19 位纯数字加工单号<br/>② 锁定投入库存数量（标记为“加工锁定”）<br/>③ 异步抓取 IoT 现场/设备影像（失败记日志不阻断）<br/>④ 推送系统审批任务流 | Toast: 「提交审核失败：{具体未满足项，如“可用库存不足”/“投入货物必须同仓”/“无法对仓单货物发起加工”}」 |
| 未完成 | 完成加工并入库 | R21: 实际产出量 $> 0$<br/>R22: 产出仓库必须与主表仓库强一致，货物位置/具体位置必填<br/>R23: 实际消耗量 $> 0$ 且 $\le$ 锁定量（严禁为0）<br/>R25: 损耗比例($0\% \sim 100\%$)及损耗说明($\le 500$字)必填 | 已完成 | 唤起【完成加工】弹窗：<br/>- 灰色提示：`实际结果确认后不支持业务冲正`<br/>- 蓝色提示：`ⓘ 当前投入库存将被锁定，完成后全部产出将自动归类至 {关联单号}`<br/>- 点击【完成加工并入库】按钮 | 全局原子事务：<br/>① 扣减实际消耗库存<br/>② 释放计划余量 ($\text{锁定数量} - \text{实际消耗数量}$)<br/>③ 生成产出库存及全局唯一新存货条码<br/>④ 抵质押产出继承主单；原单旧条码置“已出库”，追加新条码<br/>⑤ 详情展示绿色“已完成”印章，回写【修改记录】 | Toast: 「完成加工失败：{具体错误，如“实际消耗不可超过锁定数量”/“产出仓库与主表仓库不一致”/“事务执行失败全局回滚”}」 |
| 未完成 | 单据作废 | 操作人具备作废权限；录入必填作废原因 | 作废 | 唤起【作废确认】弹窗：<br/>- 黄色警示：`! 作废后释放全部锁定库存，请填写作废原因`<br/>- 录入作废原因（最多500字）<br/>- 点击【确定】 | ① 状态更新为“作废”<br/>② 调用库存引擎全额释放计划投入锁定库存<br/>③ 详情展示灰色“作废”印章，作废人、时间、原因回写至【修改记录】Tab | Toast: 「作废失败：单据已被完成处理」 |
| 全状态 | 查看详情 | 任何授权人员可查看 | 不变 | 唤起单据详情页，展示主表及四大 Tab 标签页 | 只读追溯展示，单号支持一键复制，关联单号支持跳转查看 | 无 |

---

## 六、核心业务规则（R01 ~ R35）

### 1. 申请与前置校验规则

#### R01：业务时间合规性
- 加工日期只能选择不晚于当前系统时间的日期时间，默认当前时间，精确到秒；禁止录入未来时间。

#### R02：货主主体与明细联动
- 支持企业货主与个人货主；货主变更时，前端清空已选的投入货物与关联单号，防止出现跨主体数据污染；个人货主展示脱敏手机号与身份证号。

#### R03：加工类型与库存状态匹配
- **普通加工**：仅允许选择状态为 `存货中` 且无任何锁定的在库可用库存；
- **抵押加工 / 质押加工**：必须关联对应的有效抵押单或质押单，且仅能选择该单据名下的抵质押在库货物。

#### R04：抵质押关联与仓单货物强过滤
- 抵押或质押加工仅允许关联处于正常在押状态的单据；
- **仓单强过滤**：若抵质押单中的货物已开具数字仓单，系统强行过滤并禁止选择，阻断提示「仓单货物不可发起加工，请先解单」。

#### R05：投入货物“三同”校验与仓库锁定
- 单张加工单内所有的投入明细，必须同时满足：**同一货主**、**同一实体仓库**、**同一加工类型**。
- 主表无独立仓库选择框，用户添加第一条投入货物后，系统自动确定并锁定主表仓库名称；清空投入明细后方可解锁。

#### R06：计划投入量与即时锁库
- 每条货位计划投入量必须满足 $0 < \text{计划量} \le \text{提交时实时可用量}$；
- 点击【提交审核】成功后，系统即时锁定对应库存数量，扣减可用量，打上“加工锁定”标记，防止其他业务并发争抢。

#### R07：产出有效性、规格带出与结构冻结
- 产出货物选择后，系统自动带出其 `规格型号`（如散装）与 `计量单位`（如颗）；
- 每条产出货物的预计产出数量必须 $> 0$；
- **无效操作拦截**：若整张加工单的投入与产出 SKU 完全相同且数量完全相等，系统判定为无效操作并阻断提交；
- 提交审核成功后，产出货物的 SKU 结构强行冻结，后续完成加工时不允许新增、删除或替换产出 SKU。

#### R08：备注字数上限约束
- 新增申请页备注输入框支持最多输入 **140 个字符**（`0/140`），超出字符长度禁止输入并拦截提交。

---

### 2. 审批中心与作废控制规则

#### R11：审批流通用任务流转
- 加工申请点击【提交审核】后，直接推送到系统的通用审批任务中心；在加工记录业务页面中不体现审批中状态，亦不提供审批节点操作。

#### R12：审批通过与未完成单据生成
- 审批流所有节点通过后，加工单正式进入仓储执行层「加工记录」列表，状态为 `PENDING_COMPLETION`（未完成）；计划投入库存继续保持锁定状态。

#### R13：审批终止与库存解锁
- 审批流中若执行驳回或撤回，审批任务终止，系统在同一事务内全额解锁并释放该单据锁定的全部计划投入库存。

#### R14：未完成单据作废与审计留痕
- 单据处于 `未完成` 状态时，具备作废权限的人员可点击【作废】；
- 必须唤起【作废确认】弹窗，展示黄色警示条 `! 作废后释放全部锁定库存，请填写作废原因`；
- **作废原因必填且上限 500 字**（`0/500`）；确认作废后状态更新为 `作废`，全额释放锁定库存，并将作废人、作废时间及作废原因落账至详情页【修改记录】Tab。

---

### 3. 完成加工与产出入库原子事务

#### R21：实际产出数量合规性
- 完成加工登记时，每条产出货物的实际产出数量必须 $> 0$；允许大于或小于预计产出数量，但不得为 0 或负数。

#### R22：产出入库位置与同仓强约束
- 产出货物的入库仓库必须与单据主表仓库**强一致**（严禁跨仓产出）；允许在同仓下选择不同的库房、分区（`货物位置`）；
- 产出 `具体位置` 为必填项，缺失强行阻断完成登记。

#### R23：实际消耗数量超额强拦截
- 现场填写的每条实际消耗数量必须满足 $0 < \text{实际消耗} \le \text{锁定数量}$；
- **零消耗阻断**：每条实际消耗必须大于 0 且不得超过锁定数量；若某条投入货物完全未使用，不允许填 0，必须作废当前加工单后重新发起申请。

#### R24：计划余量自动释放机制
- 完成加工登记提交后，系统自动计算每条投入明细的释放数量：$\text{自动释放数量} = \text{锁定数量} - \text{实际消耗数量}$；
- 系统在完成加工原子事务内自动生成库存解锁流水，将余量无缝释放回可用库存。

#### R25：加工损耗信息留痕规则
- 完成加工必须录入加工损耗比例（$0\% \sim 100\%$）及损耗说明（上限 500 字，`0/500`）；无损耗填 `0%`；
- 损耗数据仅作为单据技术指标留痕（仅在已完成详情展示），严禁参与库存数量扣减、产出货值核算或抵质押安全线计算。

#### R26：新存货条码生成与货权无缝继承
- 产出货物入库成功后，系统自动为每条产出货物生成全局唯一的**新存货条码**；
- **抵质押货权继承**：若本次加工为抵押加工或质押加工：
  1. 系统将原抵押单/质押单物料清单中被消耗的原存货条码状态置为 `已出库(加工)`；
  2. 将新生成的产出存货条码、规格、数量追加写入原抵押单/质押单物料清单中，实现货权链路不中断；
  3. 并在完成加工弹窗中明确展示蓝色 Alert 提示：`ⓘ 当前投入库存将被锁定，完成后全部产出将自动归类至 {关联单号}`。

---

### 4. 价格计算、影像抓拍与并发审计

#### R31：缺价统一提示口径
- 产出货物单价统一继承自货品管理中的行业参考单价；
- 若货品未设置单价，单价列展示 `--`，汇总区域提示「存在未定价货物」，系统不阻断加工申请提交或完成入库登记。

#### R32：产出金额试算公式
- $\text{预计产出总金额} = \sum (\text{预计产出数量} \times \text{行业参考单价})$（展示带货币符号 `¥`）；
- $\text{实际产出总金额} = \sum (\text{实际产出数量} \times \text{行业参考单价})$（仅汇总有价行）。

#### R33：IoT 现场抓拍与降级容错
- 提交申请时系统自动调用投入仓库摄像头与联网设备抓拍现场图像；
- 若现场无监控设备或调用超时/失败，系统在详情页展示文本 `抓图失败/无监控设备`，不阻断单据正常流转。

#### R34：并发乐观锁与排他控制
- 单据操作基于 `version` 乐观锁；多用户并发操作同一单据或同一库存批次时，后提交者直接提示「数据已被修改，请刷新后重试」；
- 完成加工接口采用严格的分布式幂等防重锁，防止网络重发导致重复入库。

#### R35：全生命周期操作审计与详情四 Tab 体系
- 详情页分为四大标准 Tab 展现：
  - **Tab 1 投入货物**：展示原投入货物快照、锁定数量、实际消耗与释放数量；
  - **Tab 2 产出货物**：展示产出货物、预计数量、实际数量、行业参考价、入库位置、其他信息与新存货条码；
  - **Tab 3 审核记录**：回显总流程名称、申请提交、分配、任务节点及【任务详情 >】超链；
  - **Tab 4 修改记录**：已完成单据展示完成人与完成时间；作废单据展示作废人、作废时间与作废原因。

---

## 七、权限与风控约束

| 角色 | 新增加工 / 提交审核 | 查看列表/详情 | 完成加工并入库 | 作废单据 | 审批中心流转 | 数据范围 |
| :--- | :---: | :---: | :---: | :---: | :---: | :--- |
| **仓库监管员 / 业务操作员** | ✅ | ✅ | ✅ | ❌ | ❌ (非审批节点) | 授权仓库范围内 |
| **仓储主管 / 作废授权人员** | ❌ | ✅ | ✅ | ✅ | ❌ | 授权仓库范围内 |
| **风控管理人员** | ❌ | ✅ | ❌ | ✅ (特权) | ❌ | 当前租户全量（只读+特权作废） |
| **审批中心人员** | ❌ | ✅ | ❌ | ❌ | ✅ (审批中心处理) | 授权审批流范围内 |
| **系统管理员** | ✅ | ✅ | ✅ | ✅ | ✅ | 当前租户全量 |

---

## 八、跨模块业务流程图

```mermaid
flowchart TD
    Start([用户点击新增加工]) --> CheckThreeSame{校验三同:<br/>同货主/同仓库/同类型?}
    CheckThreeSame -- 否 --> ToastErr1[Toast: 投入货物必须满足三同] --> Start
    CheckThreeSame -- 是 --> CheckStock{校验在库实时可用量<br/>计划投入量 <= 可用量?}
    CheckStock -- 否 --> ToastErr2[Toast: 可用库存不足] --> Start
    CheckStock -- 是 --> LockStock[即时锁定计划投入库存<br/>生成 19 位纯数字加工单号]
    LockStock --> SubmitApp[点击右上角提交审核<br/>报文推入系统审批中心]

    SubmitApp --> AppCenter{系统审批中心裁决}
    AppCenter -- 审批驳回/撤回 --> RejRelease[全额释放计划投入库存<br/>单据终止留痕] --> End([结束])
    AppCenter -- 审批通过 --> WaitComp[状态进入 未完成 PENDING_COMPLETION<br/>流转至加工记录列表]

    WaitComp --> CompOrVoid{现场执行动作}
    CompOrVoid -- 点击作废 --> VoidDialog[弹出作废确认黄色警示弹窗<br/>强制录入 500 字作废原因]
    VoidDialog --> VoidRelease[全额释放投入锁定库存<br/>状态置为 作废 VOIDED<br/>落账至修改记录] --> End

    CompOrVoid -- 点击完成加工 --> CompDialog[弹出完成加工弹窗<br/>录入实际消耗/实际产出/货位/损耗]
    CompDialog --> CheckCompData{校验消耗 <= 锁定量<br/>消耗严禁为0<br/>产出入库同仓?}
    CheckCompData -- 校验未通过 --> ToastErr3[Toast: 阻断提交并提示具体原因] --> CompDialog
    CheckCompData -- 校验通过 --> AtomicTx[点击完成加工并入库<br/>执行全局数据库原子事务]

    subgraph 原子事务执行
        AtomicTx --> Step1[1. 扣减实际消耗库存流水]
        Step1 --> Step2[2. 自动释放未消耗计划余量]
        Step2 --> Step3[3. 产出货物正式入库上架]
        Step3 --> Step4[4. 生成全局唯一新存货条码]
        Step4 --> Step5[5. 抵质押货权继承: 旧条码出库/新条码追加]
    end

    AtomicTx --> ResultState[单据置为 已完成 COMPLETED<br/>右上角盖绿色印章，全字段锁定只读<br/>落账完成人与时间至修改记录] --> End
```

---

## 九、核心操作时序图

### 1. 加工申请提交与库存锁定时序

```mermaid
sequenceDiagram
    autonumber
    actor U as 业务人员/监管员
    participant F as 前端界面 (新增加工)
    participant B as 加工管理服务
    participant W as WMS库存服务
    participant IoT as 物联网监控
    participant AC as 审批任务中心
    participant DB as 数据库

    U->>F: 录入日期、货主、类型、在库货物及预计产出，点击【提交审核】
    F->>B: 提交加工申请报文（投入明细、预计产出、备注<=140字）
    B->>DB: 校验货主、三同规则及抵质押单有效性
    B->>W: 校验可用库存并请求锁定计划投入量
    W-->>B: 库存锁定成功流水（打上加工锁定标记）
    par 异步抓取监控
        B->>IoT: 触发投入仓库摄像头现场抓拍
        IoT-->>B: 返回抓拍状态（图片URL或抓图失败/无监控设备）
    end
    B->>DB: 生成 19 位纯数字单号，落库主表与明细
    B->>AC: 推送系统审批流任务
    B-->>F: 返回提交审核成功
    F-->>U: Toast「提交审核成功」并跳转「加工记录」列表
```

### 2. 完成加工原子事务与货权继承时序

```mermaid
sequenceDiagram
    autonumber
    actor U as 现场监管员
    participant F as 前端界面 (完成加工弹窗)
    participant B as 加工管理服务
    participant W as WMS库存服务
    participant P as 抵质押货权服务
    participant DB as 数据库

    U->>F: 录入实际消耗数量、实际产出数量、位置及损耗，点击【完成加工并入库】
    F->>B: 提交完成加工登记报文
    B->>DB: 校验单据状态为未完成与 Version 乐观锁
    B->>DB: 校验 0 < 实际消耗 <= 锁定数量，产出仓库强一致
    B->>DB: 开启全局数据库原子事务
    B->>W: 扣减实际消耗库存，自动释放未消耗余量
    B->>W: 执行产出货物入库上架
    B->>W: 生成全局唯一新存货条码
    opt 若为抵押加工或质押加工
        B->>P: 更新原抵质押单物料清单：原条码置已出库，新条码追加如单
    end
    B->>DB: 更新加工单状态 = COMPLETED，记录完成人与时间至【修改记录】
    B->>DB: 写入底层操作审计流水
    DB-->>B: 事务整体提交成功
    B-->>F: 返回完成加工入库成功
    F-->>U: Toast「完成加工并入库成功」并刷新单据为已完成只读详情（盖绿色印章）
```

---

## 十、修订记录

| 日期 | 版本 | 变更摘要 | 变更人 |
| :--- | :---: | :--- | :--- |
| 2026-08-14 | V1.0 | 初版加工管理业务规则规格 | 产品团队 |
| 2026-09-02 | V3.0 | **对齐 6.2 标杆架构基准版**：规范 R01~R35 标准规则编码、口径隔离表、状态流转矩阵、跨模块流程图与双时序图，作为全模块规则唯一数据源 | AI PM / 森云科技 |
| 2026-09-09 | **V3.1** | **真实系统全生命周期交互与规则闭环校准**：<br/>1. 单据状态重构为真实 3 态（未完成、已完成、作废），审批流剥离至系统审批任务中心；<br/>2. 单号规则对齐为 19 位纯数字，主表仓库由首条投入货物带出并锁定；<br/>3. 对齐新增页右上角【提交审核】、备注 140 字限制、产出规格/单位带出及品类数/总量汇总；<br/>4. 对齐【完成加工并入库】锁定与自动释放机制、继承提示条及 500 字损耗说明；<br/>5. 对齐【作废确认】黄色警示弹窗、500 字作废原因与【修改记录】Tab 回显。 | AI PM / 森云科技 |
**普通存货**：仅允许选择状态为 `存货中` 且无任何锁定的在库可用库存；
- **抵押货 / 质押货**：必须关联对应的有效抵押单或质押单，且仅能选择该单据名下的抵质押在库货物。

#### R04：抵质押关联与仓单货物强过滤
- 抵押或质押加工仅允许关联处于正常在押状态的单据；
- **仓单强过滤**：若抵质押单中的货物已开具数字仓单，系统强行过滤并禁止选择，阻断提示「仓单货物不可发起加工，请先解单」。

#### R05：投入货物“三同”校验
- 单张加工单内所有的投入明细，必须同时满足：**同一货主**、**同一实体仓库**、**同一加工类型**。
- 用户选择第一条投入货物后，系统自动锁定单据的货主、仓库与加工类型。

#### R06：计划投入量与即时锁库
- 每条二级计划投入量必须满足 $0 < \text{计划量} \le \text{提交时实时可用量}$；
- 提交成功后，系统即时锁定对应库存数量，扣减可用量，打上“加工锁定”标记，防止其他业务并发争抢。

#### R07：产出有效性与结构冻结
- 每条产出货物的预计产出数量必须 $> 0$；
- **无效操作拦截**：若整张加工单的投入与产出 SKU 完全相同且数量完全相等，系统判定为无效操作并阻断提交；
- 提交成功后，产出货物的 SKU 结构强行冻结，后续完成加工时不允许新增、删除或替换产出 SKU。

---

### 2. 审批与作废控制规则

#### R11：审批通过流转规则
- 审批流所有节点通过后，单据状态变更为 `PENDING_COMPLETION`（待完成）；计划投入库存继续保持锁定状态。

#### R12：审批驳回与库存释放规则
- 审批流中任一节点执行驳回，单据状态变更为 `REJECTED`（已驳回）；系统在同一事务内全额解锁并释放该单据锁定的全部计划投入库存。

#### R13：申请人撤回与库存释放规则
- 仅单据申请人在单据处于 `PENDING_APPROVAL`（审批中）状态时可执行撤回；
- 撤回后单据变更为 `WITHDRAWN`（已撤回），终止审批流并全额释放锁定库存。

#### R14：待完成单据作废规则
- 单据处于 `PENDING_COMPLETION`（待完成）状态时，具备作废权限的人员可执行作废；
- 必须填写作废原因；作废成功后单据变更为 `VOIDED`（已作废），全额释放锁定库存并记录审计日志。

---

### 3. 完成加工与产出入库原子事务

#### R21：实际产出数量合规性
- 完成加工登记时，每条产出货物的实际产出数量必须 $> 0$；允许大于或小于预计产出数量，但不得为 0 或负数。

#### R22：产出入库位置与同仓强约束
- 产出货物的入库仓库必须与单据主表仓库**强一致**（严禁跨仓产出）；允许在同仓下选择不同的库房、分区及具体位置；
- 产出具体位置为必填项，缺失强行阻断完成登记。

#### R23：实际消耗数量超额强拦截
- 现场填写的每条二级实际消耗数量必须满足 $0 < \text{实际消耗} \le \text{二级计划投入锁定量}$；
- 若录入的实际消耗大于锁定量，强行阻断提交并提示「实际消耗数量不得大于计划投入锁定量」；
- 若某条投入货物完全未使用，不允许填 0，应作废当前加工单后重新发起申请。

#### R24：计划余量自动释放机制
- 完成加工登记提交后，系统自动计算每条投入明细的余量：$\text{释放数量} = \text{计划投入量} - \text{实际消耗量}$；
- 系统在完成加工事务内自动生成库存解锁流水，将余量无缝释放回可用库存。

#### R25：加工损耗留痕规则
- 完成加工必须录入加工损耗比例（$0\% \sim 100\%$）及损耗说明；无损耗录入 `0%`；
- 损耗数据仅作为单据技术指标留痕，严禁参与库存数量扣减、产出货值核算或抵质押安全线计算。

#### R26：存货条码生成与货权无缝继承
- 产出货物入库成功后，系统自动为每条产出货物生成全局唯一的**存货条码**；
- **抵质押货权继承**：若本次加工为抵押货或质押货：
  1. 系统将原抵押单/质押单物料清单中被消耗的原存货条码状态置为 `已出库(加工)`；
  2. 将新生成的产出存货条码、规格、数量追加写入原抵押单/质押单物料清单中，实现货权链路不中断。

---

### 4. 价格计算、影像抓拍与并发审计

#### R31：缺价统一提示口径
- 产出货物单价统一继承自货品管理中的行业参考单价；
- 若货品未设置单价，单价列展示 `--`，汇总区域提示「存在未定价货物」，系统不阻断加工申请提交或完成登记。

#### R32：产出金额试算公式
- $\text{预计产出总金额} = \sum (\text{预计产出数量} \times \text{行业参考单价})$（仅汇总有价行）；
- $\text{实际产出总金额} = \sum (\text{实际产出数量} \times \text{行业参考单价})$（仅汇总有价行）。

#### R33：IoT 现场抓拍与降级容错
- 提交申请时系统自动调用投入仓库摄像头与联网设备抓拍现场图像；
- 若现场无监控设备或调用超时/失败，系统记录异常状态（“无监控设备”或“抓图失败”），不阻断加工申请的正常提交。

#### R34：并发乐观锁与排他控制
- 单据操作基于 `version` 乐观锁；多用户并发操作同一单据或同一库存批次时，后提交者直接提示「数据已被修改，请刷新后重试」；
- 完成加工接口采用严格的分布式幂等防重锁，防止网络重发导致重复入库。

#### R35：全生命周期操作审计
- 提交、审批、撤回、作废、完成及异常失败操作，全量记录操作人、角色、IP、时间戳、请求标识及前后数据快照。

---

## 七、权限与风控约束

| 角色 | 新增申请 | 查看列表/详情 | 审批通过/驳回 | 申请人撤回 | 完成加工登记 | 单据作废 | 数据范围 |
| :--- | :---: | :---: | :---: | :---: | :---: | :---: | :--- |
| **仓库监管员 / 业务操作员** | ✅ | ✅ | ❌ | ✅ (限本人) | ✅ | ❌ | 授权仓库范围内 |
| **业务主管 / 审批人员** | ❌ | ✅ | ✅ | ❌ | ❌ | ❌ | 授权机构范围内 |
| **风控管理人员** | ❌ | ✅ | ❌ | ❌ | ❌ | ✅ (特权) | 当前租户全量（只读+风控作废） |
| **系统管理员** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 当前租户全量 |

---

## 八、跨模块业务流程图

```mermaid
flowchart TD
    Start([用户发起加工申请]) --> CheckThreeSame{校验三同:<br/>同货主/同仓库/同类型?}
    CheckThreeSame -- 否 --> ToastErr1[Toast: 投入货物必须满足三同] --> Start
    CheckThreeSame -- 是 --> CheckStock{校验在库实时可用量<br/>计划投入量 <= 可用量?}
    CheckStock -- 否 --> ToastErr2[Toast: 可用库存不足] --> Start
    CheckStock -- 是 --> LockStock[即时锁定计划投入库存<br/>生成加工单号 PRO...]
    LockStock --> SubmitApp[进入审批流 PENDING_APPROVAL]

    SubmitApp --> AppDecision{审批裁决}
    AppDecision -- 审批驳回 --> RejRelease[全额释放计划投入库存<br/>单据置为 REJECTED] --> End([结束])
    AppDecision -- 申请人撤回 --> WithRelease[全额释放计划投入库存<br/>单据置为 WITHDRAWN] --> End
    AppDecision -- 审批通过 --> WaitComp[状态置为 PENDING_COMPLETION<br/>保持库存锁定]

    WaitComp --> CompOrVoid{执行动作}
    CompOrVoid -- 授权作废 --> VoidRelease[全额释放计划投入库存<br/>单据置为 VOIDED] --> End
    CompOrVoid -- 完成加工登记 --> CheckCompData{校验实际消耗 <= 计划量<br/>产出数量 > 0<br/>产出入库同仓?}
    CheckCompData -- 校验未通过 --> ToastErr3[Toast: 阻断提交并提示具体原因] --> WaitComp
    CheckCompData -- 校验通过 --> AtomicTx[执行完成加工原子事务]

    subgraph 原子事务执行
        AtomicTx --> Step1[1. 扣减实际消耗库存流水]
        Step1 --> Step2[2. 释放未消耗计划余量]
        Step2 --> Step3[3. 产出货物正式入库]
        Step3 --> Step4[4. 生成全局唯一存货条码]
        Step4 --> Step5[5. 抵质押货权继承: 旧条码出库/新条码追加]
    end

    AtomicTx --> ResultState[单据置为 COMPLETED<br/>全字段锁定只读] --> End
```

---

## 九、核心操作时序图

### 1. 加工申请提交与库存锁定时序

```mermaid
sequenceDiagram
    autonumber
    actor U as 业务人员/监管员
    participant F as 前端界面
    participant B as 加工管理服务
    participant W as WMS库存服务
    participant IoT as 物联网监控
    participant DB as 数据库

    U->>F: 选择货主、类型、在库货物及预计产出，点击【提交申请】
    F->>B: 提交加工申请报文（投入明细、预计产出）
    B->>DB: 校验货主、三同规则及抵质押单有效性
    B->>W: 校验可用库存并请求锁定计划投入量
    W-->>B: 库存锁定成功流水
    par 异步抓取监控
        B->>IoT: 触发投入仓库摄像头现场抓拍
        IoT-->>B: 返回抓拍图片URL或失败状态
    end
    B->>DB: 生成 19 位加工单号，落库加工单主表与明细表（状态：审批中）
    B->>DB: 启动审批流实例
    B-->>F: 返回提交成功，展示单号
    F-->>U: Toast「提交成功」并跳转列表
```

### 2. 完成加工原子事务与货权继承时序

```mermaid
sequenceDiagram
    autonumber
    actor U as 现场监管员
    participant F as 前端界面
    participant B as 加工管理服务
    participant W as WMS库存服务
    participant P as 抵质押货权服务
    participant DB as 数据库

    U->>F: 录入实际消耗量、实际产出量、入库位置及损耗，点击【确认完成】
    F->>B: 提交完成加工登记报文
    B->>DB: 校验单据状态为 PENDING_COMPLETION 与 Version 乐观锁
    B->>DB: 校验实际消耗 <= 计划投入锁定量，产出仓库强一致
    B->>DB: 开启全局数据库原子事务
    B->>W: 扣减实际消耗库存，释放未消耗余量
    B->>W: 执行产出货物正式入库上架
    B->>W: 生成产出存货条码
    opt 若为抵押货或质押货
        B->>P: 更新原抵质押单物料清单：原条码置已出库，新条码追加入单
    end
    B->>DB: 更新加工单状态 = COMPLETED，记录完成人与时间
    B->>DB: 写入操作审计流水
    DB-->>B: 事务整体提交成功
    B-->>F: 返回完成加工成功
    F-->>U: Toast「加工已完成」并刷新单据为只读详情
```

---

## 十、修订记录

| 日期 | 版本 | 变更摘要 | 变更人 |
| :--- | :---: | :--- | :--- |
| 2026-08-14 | V1.0 | 初版加工管理业务规则规格 | 产品团队 |
| 2026-09-02 | **V3.0** | **对齐 6.2 标杆架构基准版**：规范 R01~R35 标准规则编码、口径隔离表、状态流转矩阵、跨模块流程图与双时序图，作为全模块规则唯一数据源 | AI PM / 森云科技 |
