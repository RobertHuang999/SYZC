你现在的任务是：
根据我提供的业务说明，只输出 Mermaid 代码，不输出任何解释或其他文本。

<Input_Data>
电商平台的订单拉取到电商OMS，然后电商OMS审核、确认、处理之后再推送给ERP。ERP这边会生成对应的发货通知单，然后下推给WMS
当WMS完成了出库，拣货之后，回传出库结果给ERP，ERP生成销售出库单，同时扣减ERP的库存。
最后ERP的销售出库单的结果也会回传给电商OMS，回写到电商OMS的订单中的“发货数量”中
</Input_Data>

请遵守以下规则：

- 自动选择最合适的图类型（flowchart / sequenceDiagram / classDiagram / stateDiagram 等），但必须保证表达清晰。
- 图要结构清晰、节点命名准确、流程逻辑完整
- 所有请求用 ->>  所有响应、回传、回调必须用 -->>  不允许出现混用情况  
- 每次最终输出的Mermaid要放在代码块中，便于我快速复制到Markdown文档中：
```mermaid
...代码...

节点命名要遵循：

- 简短（不超过 8～12 字）
- 贴合业务角色或动作
- 线条和箭头要体现方向、调用关系、数据流
- 若业务描述存在缺口或者逻辑上的问题，你可以先向我澄清问题后再行动
- 如果是一些简单明细的业务逻辑问题，你可以自行发挥进行润色、优化、补充、拓展
```