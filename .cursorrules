# Cursor Project Rules - SYZC 供应链金融存货监管系统

本项目所有业务架构、风控逻辑、PRD与原型标注标杆规范、技术栈与安全约束已统一维护在根目录 `@AGENTS.md`。

## Cursor 核心工作指令：
1. **全局规范唯一真源**：每次交互、代码编写、原型修改或 PRD 梳理，请严格遵循 `@AGENTS.md`。
2. **原型标注标杆模板**：所有 PC 与 H5 原型（列表、新增编辑、详情页）必须 100% 遵循 `AGENTS.md` 4.2 节【标杆模板规范】：
   - 挂载主 PRD (`prd`)、字段清单 (`fields`)、业务规则规格 (`rules`) 标准三件套；
   - 页面级必须包含 Mermaid 业务/状态机流转图、上下游数据协同与不可变快照隔离；
   - 字段必须标明字段编码、权威枚举、格式与必填校验、互斥约束（如 R14、R04）；
   - 交互必须覆盖状态闭环、防未保存离开拦截（Dirty 确认）与二次确认弹窗。
3. **核心业务逻辑硬性约束**：
   - **门禁密码双路径**：先匹配审批配置，命中走 `UnlockApplySubmitDialog` 审批，未命中走免审直发；人脸门禁绝不调用短信 API（R31）；
   - **自审禁止（P06/R11）**：审批人不可审批自己发起的开锁申请；
   - **物联穿透（R11~R13）**：设备告警向订单穿透，订单端只读，必须由现场设备台账核销解除。
